# 第 04 讲 树（中）：二叉搜索树、平衡二叉树与链表逆转

> 本讲依据视频 040–050（4.1 二叉搜索树、4.2 平衡二叉树、小白专场是否同一棵二叉搜索树、附：线性结构习题选讲）整理。
> 学习目标：掌握二叉搜索树的查找/插入/删除操作；理解 AVL 树的平衡因子与四种旋转调整；能用"标记法"判别两个插入序列是否生成同一棵 BST；理解抽象链表概念与 K 个一组链表逆转的标准模板。

## 一、二叉搜索树（BST）

### 1. 定义与查找

二叉搜索树（BST，又称二叉排序树）：可以为空；非空时左子树所有关键字小于根、右子树所有关键字大于根，且左右子树本身也是 BST。

- **按值查找**：与根比较，小则递归左、大则递归右、相等即命中。这是尾递归，可直接改写成循环。
- **找最小/最大**：最小沿左指针一路到底，最大沿右指针一路到底。

查找效率取决于树高；最坏退化为斜树时 $O(n)$，这正是引入平衡树的动机。

### 2. 插入

按查找路径向下走，直到空指针处挂上新结点。关键是要记住"待挂的父亲位置"——递归版本巧妙地利用返回值：每层把递归结果重新赋给 `Left/Right`，到空树时申请新结点并返回其地址，上层自然接回。相同关键字通常忽略或计数。

### 3. 删除

分三种情况：

- **叶结点**：直接把父亲对应指针置空；
- **只有一个孩子**：把孩子直接接到父亲上；
- **左右皆非空**：用**右子树最小值**（或左子树最大值）替换被删结点的值，再去删除那个替补结点。替补结点一定在子树最左/最右端，只有至多一个孩子，从而把复杂情况化为前两种。

【进阶】BST 的完整操作与性质见 OI-Wiki：[数据结构/二叉搜索树](../OI-Wiki/数据结构/二叉搜索树.md)。

## 二、平衡二叉树（AVL）

### 1. 为什么需要平衡

同样 $n$ 个结点，插入顺序不同会得到不同形态的 BST，平均查找长度差异巨大（如 3.0 vs 6.5）。若树退化为链，查找从 $O(\log n)$ 退化为 $O(n)$。

**平衡因子**：结点左子树高度减右子树高度。**AVL 树**定义：空树，或任意结点平衡因子绝对值不超过 1。

高度为 $h$ 的 AVL 树最少结点数 $N(h)=N(h-1)+N(h-2)+1$，与斐波那契序列相关，可推出 $N(h)=\Theta(\phi^h)$，反之 AVL 树高 $h=O(\log n)$，从而保证查找、插入、删除均为 $O(\log n)$。

### 2. 四种旋转调整

插入新结点后，从插入点向上找第一个平衡因子绝对值为 2 的结点（"被破坏者"），看新结点（"麻烦结点"）位于它的哪一侧：

| 麻烦结点位置 | 旋转类型 | 操作 |
|---|---|---|
| 被破坏者的右子树的右子树 | RR（左单旋） | 右孩子上提，被破坏者作左孩子 |
| 被破坏者的左子树的左子树 | LL（右单旋） | 左孩子上提，被破坏者作右孩子 |
| 被破坏者的左子树的右子树 | LR（左右双旋） | 先在左子树做 RR，再对整体做 LL |
| 被破坏者的右子树的左子树 | RL（右左双旋） | 先在右子树做 LL，再对整体做 RR |

旋转时必须保持 BST 性质：中间值结点上提，四块子树按"左小右大"重新挂接。只需调整**最下面**那个失衡点，上层会自动恢复平衡。若插入未引起失衡，树结构不变，但沿路径的平衡因子仍需更新。

【进阶】AVL 树的详细推导与实现见 OI-Wiki：[数据结构/AVL树](../OI-Wiki/数据结构/AVL树.md)。

## 三、小白专场：是否同一棵二叉搜索树

- **题意**：给定 $N$ 与 $L$，第一行是基准插入序列；后面 $L$ 行各给一个序列，判别它们是否与基准序列生成同一棵 BST。输入以 $N=0$ 结束。
- **三种思路**：
  1. 分别建两棵树，递归比较——思路直白但要建两棵树；
  2. 不建树：以首元素为根，把剩余元素按"比根小/大"分两组，递归比较两组是否同构；
  3. **建一棵基准树，其余序列"走查"**：本节采用。
- **标记法**：每个 BST 结点加一个 `flag` 字段（0/1），表示该结点在本次走查中是否被访问过。
  1. 用第一行序列建基准树 T；
  2. 对待判序列逐个元素，按 BST 查找路径在 T 中走查：
     - 若当前结点 `flag==1`（已访问过），按大小继续向左/右；
     - 若当前结点 `flag==0` 且值等于待查值 → 标记为 1，继续；
     - 若当前结点 `flag==0` 且值不等于待查值 → 说明走查路径碰到了一个"新结点"，矛盾，该序列不一致；
  3. 第一个元素必须等于树根，否则直接不一致。
- **实现细节**：
  - 比较新序列前调用 `Reset`，把 T 上所有 `flag` 清零；
  - **发现不一致不能立即 return**，必须把当前行剩余整数全部读完，否则会把下一个序列的开头读错；可用一个程序级 `flag` 标记"已矛盾但继续读输入"；
  - 一组数据用完后 `FreeTree` 释放空间，再读下一组 $N$。

## 四、附：线性结构习题选讲——Reversing Linked List

### 1. 什么是抽象的链表

链表是**抽象数据结构**：每个结点存数据 + 存"下一个结点位置"。C/C++ 用原生指针实现，Java 没有指针并不代表没有链表——任何能表示下一结点位置的方式（整数下标、引用）都可以。本题用结构数组模拟内存：数组下标即"地址"，每个分量存 `(地址, 数据, 下一字节下标)`，缺省用 `-1` 表示链尾。

### 2. K 个一组逆转的标准模板

- 链表前先加一个空的头结点（牺牲一点空间换取操作统一）。
- 三个指针：`new`（已逆转部分的新头）、`old`（待逆转部分的旧头）、`tmp`（记住 old 的下一结点，否则改向会丢链）。
- 重复 $K$ 次：`tmp = old->Next; old->Next = new; new = old; old = tmp;`（即把 old 的指向前逆转头）。
- 结束后：原头结点的下一结点（现在是逆转段的尾）接到 `old`（剩余段头），头结点指向 `new`（新段头）。
- 循环次数用计数器 `cnt` 控制；`K=1` 时不动；剩余不足 $K$ 个结点不逆转。

### 3. 为什么不能偷懒

PAT 是黑盒测试。若直接把所有结点按地址排序再每 $K$ 个逆序输出，或用栈模拟，题目会在"内存"里混入若干不在链表上的废结点，使排序/栈做法直接挂掉——这正是"排序者必挂"。

### 4. 边界测试意识

设计/自测一组题必须覆盖：一般情形、地址上下界（00000 与 99999）、结点数恰为 $K$ 的整数倍、$K=N$（整表反转）、$K=1$（不反转）、$N$ 取最大且尾巴长度恰为 $K-1$、以及卡投机取巧的废结点数据。循环多一次少一次、跳出处漏处理，几乎都在这些边界上出错。

【进阶】链表的常见操作与反转技巧见 OI-Wiki：[数据结构/链表](../OI-Wiki/数据结构/链表.md)。

## 本节习题

| 题号 | 标题 | 原题（2026夏，9-13截止） | 同题 |
|---|---|---|---|
| 04-树4 | 是否同一棵二叉搜索树 | [原题](https://pintia.cn/problem-sets/2061393626681053184/exam/problems/type/7?problemSetProblemId=2061393626756550667) | [浙大题集·基础实验4-2.4（看题需购书码）](https://pintia.cn/problem-sets/988034414048743424/exam/problems/type/7?problemSetProblemId=988040258131861504) |
| 04-树5 | Root of AVL Tree | [原题](https://pintia.cn/problem-sets/2061393626681053184/exam/problems/type/7?problemSetProblemId=2061393626756550668) | [浙大题集·练习4.2（看题需购书码）](https://pintia.cn/problem-sets/434/exam/problems/type/7?problemSetProblemId=6103) ／ [PAT 1066（免费可作答）](https://pintia.cn/problem-sets/994805342720868352/exam/problems/type/7?problemSetProblemId=994805404939173888) |
| 04-树6 | Complete Binary Search Tree | [原题](https://pintia.cn/problem-sets/2061393626681053184/exam/problems/type/7?problemSetProblemId=2061393626756550669) | [浙大题集·进阶实验4-3.3（看题需购书码）](https://pintia.cn/problem-sets/988034414048743424/exam/problems/type/7?problemSetProblemId=988041097655119872) ／ [PAT 1064（免费可作答）](https://pintia.cn/problem-sets/994805342720868352/exam/problems/type/7?problemSetProblemId=994805407749357568) |
