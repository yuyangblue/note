1 还有一种方法呢 就是我们前面提到的一种思想 把所有有冲突的元素全部串在一起啊 这所谓的所有是在一个位置上面 比方说我们的2号位置 那么有三个元素都将放在2号位置 然后我们就把这三个元素呢 用链表串在一起 在2号位置上面有个指针 指向这样的一个链表 指向这三个元素 所以呢这种方法呢叫分离链接法 也就是把 一个位置上有冲突的所有关键词 都放在一个单项链表当中 我们来看这样的一个例子 散列表大小是等于11 当然这么多元素都放在11里面 肯定是不行的 但是我们采用的是分离链接法 或者叫链地址法 他就把有冲突的都放在链表里面 并不是放在这个数组里面 我们这个结构跟原来结构一样的 这个是对不上 然后有个指针指向一个数组 这个数组呢 他并不是用来放元素的了 他是一个指针指向了单项链表 加22 11 他的哈希值都等于0的 我就放在一个单项链表里面 37  92 他的哈希函数值都等于4的 那么我们就把它放在一起 这个指针往这一指 然后这个地方呢没元素 那么这个NULL 空指针 这也是NULL 那我们就把这么多元素呢 就放在这里面了 虽然表是11 但实际上我们放进去了很多元素 我们总共有14个元素 我们有9个元素 只要找一次就可以找到了 就找到这个元素了 而有5个元素 是要找两次先比较一次不对 再往后找就找到了 所以他的平均成功查找次数呢是9 加上5乘以2除以14 是等于1.36 链地址法呢 实际上就是用链表来进行实现 那对应的Find函数比较简单 用一个哈希表表示 就是我们前面看到那个结构 他有一个 TableSize 还有一个指针指向一个数组 然后我们有个 Key 那我们要做的第一件事情 当然是要算哈希函数值啊 所以我们根据这个Key 根据这个TableSize 我们算出了哈奇函数啊 散列函数值散列地址 散列地址算出来之后呢 那就代表了他在这个数组里的位置 TheLists是这样的一个数组 指针指出去 然后这个节点呢 串在一起 首先通过散列函数啊 一算算出这个位置就这个下标了 TheLists 的是个数组 所以就找着了这个数组的分量 然后把这个分量的 Next 赋给 P 这个结构有个分量叫Next 他赋给P 所以呢 P就指针 指向了单项链表的第一个元素 接下来是一个循环 这个循环是很典型的一种循环 是遍历单项链表的循环 只要P不等于NULL P所指向的这个元素 跟我要找的这个元素不相等 就一个个往后找一个个 往后找是什么呢 P 的 Next 的赋给P 所以只要这个 P 不空 也就是说后面还有元素 那么循环就一直做 同时循环的另外条件是 当前的这个 元素值不等于我要找的元素值 如果不等于链表又不空 再找下一个再找下一个呢 就 P 的 Next 的赋给P 所以这是很标准的 很典型的遍历链表的 这样的一个循环 等循环退出来要么P空了 那么就Return NULL 就没找着 要么是这个strcmp返回值等于0 等于0就相等了 这个时候 所在的这个节点就是我们找到的 这个节点 也是Return P 所以不管哪个条件不满足 都是Return P