1 下面我们来讲一下树的表示 那么来看一下 树能不能用数组或者用链表来实现 我们来看一个例子 是不是可以用数组来实现呢 所谓用数组来实现 就是把 这些结点按顺序存储在数组里面 看起来这样难度还是比较大的 因为如果我们只看到了一个结点的一个顺序 我们很难判别一个结点它的父亲是谁 儿子是谁 因为我们树的结构 它存在着很多种形式 任何一个结点都可能有一个儿子或者是多个儿子 很难判别 那么来看一下有没有可能用链表来实现 如果用链表来实现呢 那就是每个结点用一个结构来表示 就像我们现在看到的 A这个结点有3个儿子 所以我们可以用个结构 包含了3个指针域 分别指向BCD B这个结点呢 有2个儿子 所以我们可以构造两个指针分别指向E跟F C 这个结点呢只有一个儿子 所以我们用一个指针指向G 看起来我们好像可以用 结构加链表的方式来表示树 但是我们再仔细看看 就可以发现这样的一种树的结构 每个结点的结构的样子是不一样的 有的结点有3个指针 有的结点有1个指针 有的有2个指针 有的没有指针 所以的话这样的一种树的结构 它的整个的这种结构形式不一样 会给我们后面的程序实现带来困难 因为我们访问到一个结点的时候 我们事先是没法知道它到底有多少个儿子的 所以给程序的设计会带来困难 所以这个好像是可以实现链表表示的一种形式 但是感觉不太完美 那么另外一种途径呢 就是我们把每一个结点 都设计成是一样的结构 比方说我们把所有的结点 都设计成像A这样的结构 那么如果我们所有结点 都设计成A这样的结构 意味着B我们也要采用同样的结构 也就是说 B虽然只有2个儿子 但是我们也要给它设计一个结点是有3个指针 其中一个指针是空的 指向NULL 是个NULL 同样Ｃ也是一样的 也设计成一个结点是有３个指针 实际上只用了１个指针 这样的一种设计方法 它的一个好处呢 树当中所有结点结构是统一的 像程序处理起来比较方便 但是它带来的一个问题就是 如果这个树有n个结点 那意味着每个结点有三个指针域 所以整个树就会有3n个指针域 而我们实际上我们的边只有n-1条 也就是只有n-1个指针它是非零的 那么这样的话就会有2n+1个指针域是空的 会造成一种空间上的浪费 我们有没有更好的方法呢 下面就给大家介绍一种比较好的 一种实现方法 那就叫做儿子－兄弟表示法 它采用的一种方法是什么呢 首先一个　这个树上的每个结点 结构是统一的 就是两个指针域 一个叫FirstChild　一个叫NextSibling 这两个指针域呢 第一个指针指向的是它的第一个儿子 右边这个指针呢指向的是它的下一个兄弟 所有的结点都以这种方式来指向儿子跟兄弟 这样就可以把整棵树的结点把它串起来了 比方说针对我们前面看到这样的一棵树 对应的实现方法就是这样 每个结点都一样的　都包含了两个指针 比方说Ａ这个结点 左边这个指针指向它的第一个儿子　就是Ｂ 右边这个结点呢　指向兄弟　这个是空的 Ｂ这个结点呢 左边这个指针指向它的第一个儿子Ｅ 右边这个指针指向它的下一个兄弟 Ｂ的兄弟有两个　Ｃ跟Ｄ 它的下一个兄弟是Ｃ　那么对Ｃ来讲呢 左边这个指针指向它的第一个儿子 是Ｇ 下一个兄弟就是D 而对D来讲 左边这个指针指向它的第一个儿子H 右边这个指针它的下一个兄弟 没有 所以就是NULL 所以这样的一种表现方法呢 我们可以看得到 有这么几个优点 一个的话 树当中每个结点结构都是统一的 都是两个指针域 同时它的空间浪费也不大 n 个结点 2n个指针域 其中n-1条边 所以意味着有n-1个域是非空的 真正空的域是n+1 所以这种表示方法呢就是儿子-兄弟表示方法 就是把一般的数变成结构上面比较一致的 都是两个指针的 这样的一种表示方法 我们把这样的一个表示方法 这样的一个链表实现方法把它旋转45度 就变成这样 这个时候我们看到的是什么呢 看到的是一棵树 这个树特点是每一个节点 都有两个指针 一个指向左边 一个指向右边 每个结点最多是两个儿子 这种树呢 我们叫做是二叉树 二叉树就是 度为2的这样的一种树 就是每个结点指针最多是两个 所以从这里面我们可以看到的 就是一般的树我们都可以用儿子-兄弟这样的一种表示方法 把它用二叉树这种形式来实现 二叉树链表这种形式来实现 所以我们想研究一般树的这样的表示 跟它的操作的实现其实最核心的 最基础的 如果我们搞清楚了 二叉树是怎么表示 怎么实现的 实际上就解决了一般树的许多问题 所以二叉树是在树结构研究里面 是最重要的 也是最主要的内容 接下来这部分内容将围绕着二叉树的有关存储 它的操作实现以及它的主要应用而展开