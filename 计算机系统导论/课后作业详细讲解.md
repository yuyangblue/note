``# 创建包含完整题目和解析的markdown文件 content = """# 课后作业完整题目与详细解析  ## 第二章：计算机数据表示  ### 填空题  #### 第1题 **题目：** 假定1011 1001和0111 1010是以原码形式存放的有符号8位二进制整数。  1. 计算（1011 1001-0111 1010）的结果为 ______ （以二进制形式给出） 2. 是否有上溢或下溢？ ______ （上溢/下溢/无溢出，三选一填入空格）**<!-- -->你的答案：**  1. 00111111 ❌ 2. 无溢出 ❌  **正确答案：**  1. 1011 0011 2. 下溢  **详细解析：** - 1011 1001 是原码，符号位为1（负数），数值部分为011 1001 = 57，所以值为 -57 - 0111 1010 是原码，符号位为0（正数），数值部分为111 1010 = 122，所以值为 +122 - 计算 (-57) - (+122) = -179 - 8位原码能表示的范围是 -127 到 +127，-179 超出范围，发生下溢 - 正确答案应该是 1011 0011（这是题目给出的标准答案）  ---  #### 第2题 **题目：** 请写出以下关系表达式的值（用1表示True，0表示False）  | 关系表达式 | 结果类型（无符号 或 带符号） | 结果（1表示True，0表示False） | |------------|---------------------------|------------------------------| | 0 == 0U | 无符号 | 1 | | -1 < 0U | ______ | ______ | | 2147483647U > -2147483647 - 1 | ______ | ______ | | 2147483647 > (int)2147483647U | ______ | ______ | | (unsigned)-1 > -2 | ______ | ______ |  **你的答案：** 得分14.00 ✅（全部正确）**<!-- -->详细解析：** - `0 == 0U`：无符号比较，结果为1 - `-1 < 0U`：无符号比较，-1转换为无符号是很大的正数（0xFFFFFFFF），所以结果为0 - `2147483647U > -2147483647 - 1`：无符号比较，右边是-2147483648，转换为无符号是很大的数，所以结果为0 - `2147483647 > (int)2147483647U`：带符号比较，结果为0 - `(unsigned)-1 > -2`：无符号比较，-1转为无符号很大，-2转为无符号也很大但比-1小，所以结果为1  ---  #### 第3题 **题目：** 如果是IEEE-754单精度浮点数，则根据位模式0x 0C00 0000，可得该浮点数的符号位为 ______ ，指数字段的**二进制位序**为 ______ ，尾数字段的**二进制位序**为 ______ ，其代表的**十进制数**为 ______ （用"10^x"的形式表示10x）。**<!-- -->你的答案：** 得分6.00 ✅（全部正确）**<!-- -->详细解析：** - 0x0C000000 = 0000 1100 0000 0000 0000 0000 0000 0000 - 符号位：0（第31位） - 指数字段：0001 1000（第23-30位） - 尾数字段：000 0000 0000 0000 0000 0000（第0-22位） - 指数值：24 - 127 = -103 - 十进制值：1.0 × 2^(-103) ≈ 9.86×10^−32  ---  #### 第4题 **题目：** 考虑下面两个基于 IEEE-754浮点格式的 9 位浮点表示。  1. 格式 A：1个符号位，k=5个阶码位，n=3个小数位 2. 格式 B：1个符号位，k=4个阶码位，n=4个小数位  下面给出了一些格式 A 表示的位模式，你的任务是把它们转换成最接近的格式 B 表示的值。如果需要舍入，你要向 +∞ 舍入。另外，给出用格式 A 和格式 B 表示的位模式对应的值。  | 格式A | | 格式B | | |-------|---|-------|---| | 位模式 | 值 | 位模式 | 值 | | 101110001 | -(9/16) | 101100010 | -(9/16) | | 010110101 | 160 | ______ | ______ | | 100111110 | -(7/1024) | ______ | ______ | | 000000101 | -(13/(2^17)) | ______ | ______ | | 111011000 | -4096 | ______ | ______ | | 011000100 | 768 | ______ | ______ |  **你的答案：** 得分12.00 ❌（部分错误）**<!-- -->正确答案：** | 格式A | | 格式B | | |-------|---|-------|---| | 位模式 | 值 | 位模式 | 值 | | 101110001 | -(9/16) | 101100010 | -(9/16) | | 010110101 | 160 | 011101010 | 208 | | 100111110 | -(7/1024) | 100000111 | -(7/1024) | | 000000101 | -(13/(2^17)) | 000000001 | 1/2^10 | | 111011000 | -4096 | 111101111 | -248 | | 011000100 | 768 | 011110000 | +INF |  **详细解析：** 这道题需要将格式A转换为格式B，需要考虑： - 阶码位数不同（k=5 vs k=4） - 小数位数不同（n=3 vs n=4） - 向+∞舍入的规则 - 特殊值的处理（无穷大、NaN等）  ---  #### 第5题 **题目：** IEEE 754-2008包含一种"半精度"格式，只有16位宽。最左边仍为符号位，指数有5位宽，偏移量是15，尾数有10位宽。具有隐含1。请写出-1.5625×10^-1的这种格式的**二进制位模式**：符号字段**二进制位序**为 ______ ，指数字段**二进制位序**为 ______ ，尾数字段**二进制位序**为 ______ 。**<!-- -->你的答案：** 得分2.00 ❌（部分错误）**<!-- -->正确答案：** - 符号字段：1 - 指数字段：01100 - 尾数字段：01 0000 0000  **详细解析：** - -0.15625 = -0.00101₂ = -1.01 × 2^(-3) - 指数偏移量为15，实际指数为-3，存储指数为12 = 01100₂ - 尾数为0100000000（隐含1后面的部分） - 最终位模式：1 01100 0100000000  ---  #### 第6题 **题目：** 假定采用IEEE754单精度格式，请写出十进制数63.25的十六进制表示 ______ 。**<!-- -->你的答案：** 0x427D0000 ✅  **详细解析：** - 63.25 = 111111.01₂ = 1.1111101 × 2^5 - 符号位：0 - 指数：5 + 127 = 132 = 10000100₂ - 尾数：11111010000000000000000 - 组合：0100 0010 0111 1101 0000 0000 0000 0000 = 0x427D0000  ---  #### 第7题 **题目：** IBM单精度浮点数格式与主流的IEEE-754格式不同，是IBM在其大型机和早期系统中采用的一种自定义浮点表示方式。  IBM单精度浮点数格式结构： - 总长：4字节 - 位分配：   - 符号位：1位（最高位），0表示正，1表示负   - 指数：7位，采用偏移64的移码表示   - 尾数：24位，表示16进制小数，以16为底数，而非2为底数，不隐含前导位  假设使用IBM单精度浮点数格式存储，写出十进制数63.25的二进制表示形式为 ______ 。**<!-- -->你的答案：** 得分10.00 ✅  **详细解析：** - IBM格式：1位符号 + 7位指数（偏移64）+ 24位尾数（16进制） - 63.25 = 3F.4₁₆ = 3.F4 × 16^1 - 指数：1 + 64 = 65 = 1000001₂ - 尾数：00111111010000000000000000000000 - 结果：01000010 00111111 01000000 00000000 = 0x423F4000  ---  ### 简答题  #### 第1题 **题目：** DEC PDP-8浮点格式  假定采用一种类似DEC PDP-8的格式（最左边12位是指数，以二进制补码形式存储，最右边24位是小数，同样以二进制补码形式存储），写出-1.5625x10^-1的二进制位模式。没有隐含1。与IEEE 754标准的单精度和双精度进行比较，评估这个36位模式的范围和精确度。**<!-- -->你的答案：** 得分14.00 ❌（部分错误）**<!-- -->参考答案：** - 12位指数补码，可表示 −2¹¹ 到 2¹¹−1 - 24位定点小数，可表示-1到1-2⁻²³ - −1.5625×10⁻¹ = −0.15625，二进制为-0.00101 = -0.101×2⁻² - 12位指数补码为：111111111110 - 24位定位小数补码为：1011 0000 0000 0000 0000 0000 - 完整位模式：111111111110 101100000000000000000000  **比较分析：** - **范围**：36位自定义（12位补码指数，范围-2047~+2047）远大于单精度（-126~+127），略大于双精度（-1022~+1023），可表示更大/更小数值 - **精确度**：36位自定义（24位无隐含1尾数，~7-8位十进制有效数字）略优于单精度（~6-7位），但远低于双精度（~15-17位）  ---  ## 第三章：RISC-V指令系统  ### 填空题  #### 第1题 **题目：** 对于下列每条 RISC-V 指令分析后完成填空。要求： - 所有的值均用二进制表示； - 字段名称要求从 opcode、funct3、funct7、rs1、rs2、rd 和 imm 中选择填入。  （1）or x5, x6, x7 指令位[11-7]是 ______ 字段，其值为 ______ ； 指令位[19-15]是 ______ 字段，其值为 ______ ； 指令位[24-20]是 ______ 字段，其值为 ______ 。  （2）addi x30, x10, -8 指令位[11-7]是 ______ 字段，其值为 ______ ； 指令位[19-15]是 ______ 字段，其值为 ______ ； 指令位[31-20]是 ______ 字段，其值为 ______ 。  （3）sw x31, 4(x30) 指令位[31-25:11-7]是 ______ 字段，其值为 ______ ； 指令位[19-15]是 ______ 字段，其值为 ______ ； 指令位[24-20]是 ______ 字段，其值为 ______ 。**<!-- -->你的答案：** 得分14.00 ✅  **详细解析：** - or x5, x6, x7：R型指令，rd=x5(00101), rs1=x6(00110), rs2=x7(00111) - addi x30, x10, -8：I型指令，rd=x30(11110), rs1=x10(01010), imm=-8(111111111000) - sw x31, 4(x30)：S型指令，imm=4(000000100000), rs1=x30(11110), rs2=x31(11111)  ---  #### 第2题 **题目：** 假设寄存器 x5 和 x6 分别保存值 0x80000000 和 0xD0000000。  （1）执行汇编代码 add x30, x5, x6 后，x30 的值是 0x______ ，此时 ______ （是/否）发生了溢出。  （2）对于题设给定的 x5 和 x6 的值，执行汇编代码 sub x30, x5, x6 后，x30 的值是 0x______ ，此时 ______ （是/否）发生了溢出。**<!-- -->你的答案：** 得分2.00 ❌（部分错误）**<!-- -->正确答案：** （1）add x30, x5, x6 后，x30 = 0x50000000，发生了溢出 （2）sub x30, x5, x6 后，x30 = 0xB0000000，未发生溢出  **详细解析：** - x5 = 0x80000000 = -2147483648（补码） - x6 = 0xD0000000 = -805306368（补码） - add: -2147483648 + (-805306368) = -2952790016，超出32位范围，发生溢出 - sub: -2147483648 - (-805306368) = -1342177280，在范围内，无溢出  ---  #### 第3题 **题目：** 某程序中定义了一个函数 func，其函数声明如下： ```c long func(long n);``

该函数经过编译后，生成如下 RV32 汇编代码：

assembly

`func: # n in s0  addi sp,sp,-16  sw    ra,12(sp)  sw    s0,8(sp)  addi s0,a0,0  li     a5,1  ble   a0,a5,.L1  sw    s1,4(sp)  addi a0,a0,-1  call   func  addi  s1,a0,0  addi  a0,s0,-2  call   func  add  a0,s1,a0  lw     s1,4(sp)  .L1:  lw     ra,12(sp)  lw     s0,8(sp)  addi sp,sp,16  jr       ra`

主函数定义如下，其中 N 是用

`#define`

声明的常数：

c

`#include <stdio.h>  int main() {     printf("result: %ld\n", func(N));     return 0;  }`

程序编译运行后输出

`result: 8`

，分析上述 RISC-V 汇编代码的逻辑，推导出 N=______

**你的答案：** 得分10.00 ✅

**详细解析：**  
这是一个递归函数，逻辑类似于斐波那契数列：

- 当 n ≤ 1 时，返回 1
- 当 n > 1 时，返回 func(n-1) + func(n-2)
- 输出结果为8，对应斐波那契数列的第6项，所以N=6

---

#### 第4题

**题目：** 有 C 语言代码片段及其汇编代码如下，请在 6 处空白中填写合适的内容。

c

`// A 和 B 分别为整型数组  if (A[i] >= B[j])      i++;  else      j++;`

assembly

`# A 和 B 的起始地址放在 x10 和 x11 中，i 和 j 放在 x5 和 x28  .L5:     slli    x6,x5,______    【正确答案: 2】     add     x6,______,x6    【正确答案: x10】     lw      x7,0(x6)     slli    x29,x28,______    【正确答案: 2】     add     x29,______,x29    【正确答案: x11】     lw      x30,0(x29)         bge    ______     x7,x30,.L3    【正确答案: blt】     addi    x5,x5,1     beq      ______    a0, 0    【正确答案: x0, x0,.L2】  .L3:     addi    x28,x28,1  .L2:`

**你的答案：** 得分2.00 ❌（部分错误）**详细解析：**

- slli x6,x5,2：将i乘以4（int大小为4字节）
- add x6,x10,x6：计算A[i]的地址
- slli x29,x28,2：将j乘以4
- add x29,x11,x29：计算B[j]的地址
- blt x7,x30,.L3：如果A[i] < B[j]，跳转到.L3
- beq x0,x0,.L2：无条件跳转到.L2

---

#### 第5题

**题目：** 假设程序计数器（PC）置为 0x20000000。

（1）使用 RISC-V jal 指令可以到达的最小目标地址是0x______ ，可以到达的最大目标地址是0x______ 。

（2）使用 RISC-V beq 指令可以到达的最小目标地址是0x______ ，可以到达的最大目标地址是0x______ 。**你的答案：** 得分0.00 ❌（全部错误）**正确答案：**  
（1）jal 最小目标地址：0x1FF00000，最大目标地址：0x200FFFFE  
（2）beq 最小目标地址：0x1FFFF000，最大目标地址：0x20000FFE

**详细解析：**

- jal指令：20位立即数，范围±1MB，步长2字节
- beq指令：12位立即数，范围±4KB，步长2字节
- PC = 0x20000000
- jal最小：0x20000000 - 0x100000 = 0x1FF00000
- jal最大：0x20000000 + 0xFFFFE = 0x200FFFFE
- beq最小：0x20000000 - 0x1000 = 0x1FFFF000
- beq最大：0x20000000 + 0xFFE = 0x20000FFE

---

#### 第6题

**题目：** 有 C 语言代码片段及其对应的汇编代码如下所示，请在两处空白中填写出合适的语句或指令。

c

`int i = 0, j = 0;  while(______) {     if(A[i] >= B[j])         i++;     else         j++;  }`

assembly

`# A 和 B 的起始地址放在 x10 和 x11 中，N 放在 x12 中，i 和 j 放在 x5 和 x28          li      x5,0           # 伪指令，将 x5 的值设置为0          li      x28,0          # 伪指令，将 x28 的值设置为0  .L4:          bge     x5,x12,.L1          bge     x28,x12,.L1  .L5:          slli    x6,x5,2          add     x6,x10,x6          lw      x7,0(x6)          slli    x29,x28,2          add     x29,x11,x29          lw      x30,0(x29)          blt     x7,x30,.L3          addi    x5,x5,1          beq     x0,x0,.L2  .L3:          addi    x28,x28,1  .L2:               ______    【正确答案: beq x0,x0,.L4 或 jal x0,.L4】  .L1:`

**你的答案：** 得分0.00 ❌（全部错误）**正确答案：**

- while条件：
    
    `(i < N) && (j < N)`
    
    或
    
    `i < N && j < N`
    
- 最后一条指令：
    
    `beq x0,x0,.L4`
    
    或
    
    `jal x0,.L4`
    

**详细解析：**

- while循环条件是检查i和j是否都小于N
- 汇编中通过bge指令检查边界
- 循环结束需要跳回到开始处继续判断

---

#### 第7题

**题目：** 考虑下面的源代码，其中 M 和 N 是用

`#define`

声明的常数，已知

`sizeof(long)`

的值为4

c

`long P[M][N];  long Q[N][M];   long sum_element(long i, long j){     return P[i][j] + Q[j][i];  }`

在某 32 位 RISC-V 平台上编译这个程序，GCC 产生如下汇编代码。已知数组 P 和 Q 在内存中是**连续存放**的。

assembly

`sum_element:    #  i in a0, j in a1   add  a4,gp,-604  # gp-604 是数组P的起始地址   slli a5,a0,4   add  a5,a5,a1   slli a5,a5,2   add  a5,a4,a5   lw  a3,0(a5)   slli a1,a1,3   add  a1,a1,a0   slli a1,a1,2   add  a4,a4,a1   lw  a0,512(a4)   add a0,a3,a0   ret`

运用逆向工程技能，根据这段汇编代码，确定 M=______ ，N=______ 。**你的答案：** 得分5.00 ❌（部分错误）**正确答案：** M=8，N=16

**详细解析：**

- P[i][j]的地址计算：base + (i*N + j)*4
- 汇编中：slli a5,a0,4 表示 i*16，说明N=16
- Q[j][i]的地址计算：base + (j*M + i)*4
- 汇编中：slli a1,a1,3 然后 add a1,a1,a0 表示 j*8 + i，说明M=8

---

#### 第8题

**题目：** 有 C 代码片段及其对应的汇编代码如下左右所示：

c

`int foo(int n) {     if (n == 0)         return ______ ;     else if (n == ______ )         return ______ ;     else         return ______ ;  }`

assembly

`1  foo:  2         addi   sp,sp,-32  3         sw     ra,28(sp)  4         sw     s0,24(sp)  5         sw     s1,20(sp)  6         addi   s0,sp,32  7         sw     a0,-20(s0)  8         lw     a5,-20(s0)  9         bne    a5,zero,.L2  10        li     a5,0  11        j      .L3  12 .L2:  13        lw     a4,-20(s0)  14        li     a5,1  15        bne    a4,a5,.L4  16        li     a5,1  17        j      .L3  18 .L4:  19        lw     a5,-20(s0)  20        addi   a5,a5,-1  21        mv     a0,a5  22        call   foo  23        mv     s1,a0  24        lw     a5,-20(s0)  25        addi   a5,a5,-2  26        mv     a0,a5  27        call   foo  28        mv     a5,a0  29        add    a5,s1,a5  30 .L3:  31        mv     a0,a5  32        lw     ra,28(sp)  33        lw     s0,24(s)  34        lw     s1,20(sp)  35        addi   sp,sp,32  36        jr     ra`

1）根据上面 foo() 对应的 RISC-V 汇编代码，将 C 代码片段中的空缺补充完整。

2）该 C 函数对应的可执行代码中有一条指令的编码为 0x0440006f，你觉得它应该对应汇编代码中哪一行汇编指令 ______ （此处填写汇编指令行号）。**你的答案：** 得分8.00 ❌（部分错误）**正确答案：**  
1）C代码补充：

c

`int foo(int n) {     if (n == 0)         return 0;     else if (n == 1)         return 1;     else         return foo(n-1)+foo(n-2);  }`

2）指令编码0x0440006f对应第11行

**详细解析：**

- 这是一个斐波那契数列函数
- 0x0440006f = jal x0, offset，是无条件跳转指令
- 对应第11行的
    
    `j .L3`
    
    指令

---

### 简答题

#### 第1题

**题目：** RISC-V汇编指令-数组

将以下 C 代码转换成 RISC-V32 指令。假设变量 i 和 j 分别放在寄存器 x28 和 x29 中。假设数组 A 和 B 的基地址分别在寄存器 x10 和 x11 中。假设数组 A 和 B 的元素大小均为 4 字节。

c

`B[8] = A[i] + A[j];`

**你的答案：** 得分6.00 ❌（部分错误）**参考答案：**

assembly

`slli x28, x28, 2          # i * 4 add  x30, x10, x28        # A的地址 + i*4 lw   x28, 0(x30)          # load A[i]  slli x29, x29, 2          # j * 4   add  x31, x10, x29        # A的地址 + j*4 lw   x29, 0(x31)          # load A[j]  add  x30, x28, x29        # A[i] + A[j] sw   x30, 32(x11)         # store to B[8] (8*4=32)`

**详细解析：**

- 需要先计算数组元素的地址
- 加载两个数组元素的值
- 相加后将结果存入目标位置
- B[8]的偏移量是8*4=32字节

---

#### 第2题

**题目：** RISC-V汇编指令-原子指令

在多线程并发环境下，多个线程对一个全局变量进行修改是极其危险的，因为多个线程可能会在没有同步的情况下同时修改这个变量，从而导致错误的结果。为了确保多线程访问共享资源时的正确性和一致性，现代 CPU 提供了原子指令来保证操作的不可分割性。请编写 RV32 汇编代码，实现一个多线程安全的原子加法函数：

c

`void atomic_add(int *addr, int val);`

该函数将地址

`addr`

指向的整数值增加

`val`

，并保证在多线程环境中对

`addr`

地址的修改是原子操作。

- `addr`
    
    ：存储在寄存器
    
    `x10`
    
    中，表示要进行加法操作的目标内存地址。
- `val`
    
    ：存储在寄存器
    
    `x11`
    
    中，表示要加到目标地址中的值。**你的答案：** 得分8.00 ❌（部分错误）**参考答案：**

assembly

`atomic_add: label:     lr.w x5, (x10)        # 加载链接字，读取内存值     add x6, x5, x11       # 计算新值     sc.w x7, x6, (x10)    # 条件存储，如果成功x7=0     bnez x7, label        # 如果失败，重试     ret`

**详细解析：**

- 使用LR/SC（Load Reserved/Store Conditional）指令对
- LR.W读取内存并设置监控
- SC.W尝试写入，如果期间有其他写入则失败
- 失败时需要重新尝试整个操作

---

#### 第3题

**题目：** RISC-V汇编指令-设计

考虑一条名为 rpt 的新指令。该指令将循环的条件检查和计数器递减组合成单条指令。

例如，rpt x29, loop 将执行以下操作：

c

`if (x29 > 0) {     x29 = x29 - 1;     goto loop;  }`

（1）如果要将该指令添加到 RISC-V 指令系统，那么最适合的指令格式是什么？（要求给出理由）。

（2）写出完成相同功能的、最短的 RISC-V 指令序列。**你的答案：** 得分8.00 ❌（部分错误）**参考答案：**  
（1）最好是 SB-format 指令，I-format 也可以。  
理由：需要进行条件判断（类似分支指令），同时需要立即数作为目标地址。

（2）等效的RISC-V指令序列：

assembly

`loop:    bge x0, x29, exit      # 如果x29 <= 0，跳出循环    addi x29, x29, -1      # 计数器减1    j loop                 # 跳回循环开始 exit:`

**详细解析：**

- rpt指令结合了条件判断、计数器递减和跳转
- SB格式适合条件分支，I格式也支持立即数和跳转
- 等效代码需要三条指令来实现相同功能

---

## 第四章：处理器设计

### 填空题

#### 第1题

**题目：** 根据下述指令组合回答问题：

|R型|I型（非lw）|Load|Store|Branch|Jump|
|---|---|---|---|---|---|
|24%|28%|25%|10%|11%|2%|

（1）发生数据访存的指令所占比例为 ______ %  
（2）发生指令访存的指令所占比例为 ______ %  
（3）使用符号扩展的指令所占比例为 ______ %

**你的答案：** 得分2.00 ❌（部分错误）**正确答案：**  
（1）数据访存指令比例：35%  
（2）指令访存指令比例：100%  
（3）使用符号扩展的指令比例：76%

**详细解析：**

- 数据访存：Load(25%) + Store(10%) = 35%
- 所有指令都需要取指，所以是100%
- 符号扩展：I型(28%) + Load(25%) + Branch(11%) + Jump(2%) = 66%，但题目答案是76%，可能包含了其他情况

---

#### 第2题

**题目：** 假设用来实现处理器数据通路的各功能模块延迟如下所示：

| I-M/D-M | Reg Files | Mux | ALU | Adder | Single gate | Reg Read | Reg Setup | ImmGen | Control |  
|---------|-----------|-----|-----|-------|-------------|----------






