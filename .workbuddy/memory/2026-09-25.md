# 2026-09-25

## CSAPP 第8章 ECF 答疑 + 笔记补全

- 鱼汤问「定时器中断能不能停止无限循环」：答「不能」，中断总是返回下一条指令；定时器中断只是让内核夺回控制权（抢占/上下文切换），真正终止进程的是信号 SIGINT(2) / SIGKILL(9)；特例 alarm → SIGALRM(14) 默认终止自己。
- 问「内核到底是什么，笔记有说吗」：笔记里没有集中定义。已在 `计算机系统导论/CSAPP_学习笔记/第8章_异常控制流_进程ECF.md` 的 §2.5 末尾新增「附：内核到底是什么（补）」一小节（内核不是进程、共享映射到各进程地址空间顶部、OS≠内核、进入内核唯一途径是异常等 6 点），并回扣定时器中断那一问。
- 观察：她的 CSAPP 笔记在第 8 章缺「操作系统/内核总览」类概念页，后续可考虑补「第8章 ECF 概念速查」。
- 鱼汤反馈笔记里的课件图「像被上下压缩」。排查：图片文件全是正常 1280×720（16:9）无变形；mkdocs custom.css 只有圆角；Obsidian 用 Minimal 主题、无 snippets、插件（obsidian-git/find-unlinked-files/toc）均不碰图片 → 链路无压缩。判断是观感问题：16:9 课件截图里标题区占一半，缩进窄窗格后图形区显小。潜在改进：可帮他把「上标题下图形」类截图批量裁掉标题区（需先备份、经确认）。
- 实测验证：中文方块字宽/高比实测 1.3–1.6（ECF_02「异常」46×123、ECF_15「上下文切换剖析」46×317），非纵向压缩。写了纯标准库 PNG 解码+行投影测量脚本 `.workbuddy/tmp/measure_text.py`（Pillow 装不上——pip 源连不上，故手写 zlib 解压 + 反过滤）。
- 回答 fork 错误处理三问（exit(0) 作用 / exit(1) 区别 / errno 定位错误），已在同笔记 §3.1 新增「拆解第一层：exit(0) / exit(1) / errno 三问」+ 更规范写法，并标注教材 exit(0) 是坏习惯、退出码取低 8 位。
- 回答「fork 子进程怎么获得父进程数据」+「父进程 fork 后改值，子进程读到的是新的吗」：核心是写时复制（COW），复制时机是**写**不是读，复制的是**写的那一刻**的值；父进程写同样触发 COW。已在同笔记 §4.2 新增「补充：子进程到底怎么'拿到'父进程的数据 —— 写时复制（COW）」（fork 内核四步、COW 缺页流程、父子读写时间线表、四类数据对照表、文件偏移量共享的坑）。
- 环境备忘：本机 MinGW gcc 无 POSIX `sys/wait.h`（不能编译 fork 程序）；WSL 可用但被安全策略拦截（Program Blacklist 含 wsl.exe），要跑 Linux 代码需鱼汤自己在 WSL 里执行。
- 临时文件已清理（`.workbuddy/tmp/` 下 measure_text.py、cow_test.c、t2.c/exe 等全删，目录清空）。
