# -*- coding: utf-8 -*-
"""批量补写遗漏：L05/L07/L09/L11 补充段、L20 显著性检验、L26 连续时间 MC"""
import io
import os

NOTES = r'D:\29469\Documents\notes\概率论MIT\笔记'
ANCHOR = '## 5. 本节重点'

inserts = {}

# ---------- L05：补充 函数 ----------
inserts['L05 离散随机变量PMF与期望.md'] = """## 5. 补充：函数（S05.1，p60）

> 随机变量是"样本空间上的实值函数"，所以先把"函数"本身说清楚。本段是补充视频内容，讲义未展开。

- **定义**：函数 $f$ 是从集合 $A$（定义域）到集合 $B$（值域所在集合）的规则：$A$ 中**每个元素恰好**映射到 $B$ 中**一个**元素。允许 $A$ 中多个元素映射到同一个 $B$ 元素（如 $-1$ 和 $+1$ 都平方成 $1$），但不允许某个 $x$ 没有像或多个像。
- **形式定义**：函数 = 由有序对 $(x, y)$ 组成的集合，其中 $x$ 取自 $A$、$y$ 取自 $B$，且 $A$ 中每个 $x$ 恰好出现在一个对里。这个集合就是函数的**图像**——"函数就是整个图像"。
- **无限定义域**：当 $A$ 是无限集合时，"无限表格"含义不清，但"有序对集合"的定义依然适用（如平方函数 $= \\{(x, x^2) \\mid x \\in \\mathbb{R}\\}$）。
- **符号与口语**：$f: A \\to B$ 表示函数；$f(x)$ 是函数在点 $x$ 的**取值**（一个数值），$f$ 才是函数本身。老师原话：把函数想成"盒子"，$f(x)$ 是盒子对输入 $x$ 的输出；说"函数 $f(x)$"是常见的松散用法，正确说法是"函数 $f$"；$f(x) = x^2$ 与 $f(z) = z^2$ 是**同一个函数**（集合与规则相同，变量名只是占位符）。
- **与概率论的连接**：随机变量正是"样本空间 $\\Omega \\to \\mathbb{R}$ 的实值函数"（L05.2）；之后"随机变量的函数"（L11 导出分布）也依赖这个定义。

---

"""

# ---------- L07：补充 容斥/几何方差/独立 ----------
inserts['L07 随机变量条件作用与独立性.md'] = """## 5. 补充：容斥公式、几何方差、事件独立 vs 变量独立（S07.1–S07.3，p77–p79）

### 5.1 容斥公式（S07.1）
两事件并集（"加法多了，减回交集"）：
$$P(A_1 \\cup A_2) = P(A_1) + P(A_2) - P(A_1 \\cap A_2)$$
三事件（两两交减多了，加回三交）：
$$P(A_1 \\cup A_2 \\cup A_3) = \\sum_i P(A_i) - \\sum_{i<j} P(A_i \\cap A_j) + P(A_1 \\cap A_2 \\cap A_3)$$
一般形式（符号交替）：
$$P\\left(\\bigcup_{i=1}^n A_i\\right) = \\sum_i P(A_i) - \\sum_{i<j} P(A_i \\cap A_j) + \\sum_{i<j<k} P(A_i \\cap A_j \\cap A_k) - \\cdots + (-1)^{n-1} P\\left(\\bigcap_{i=1}^n A_i\\right)$$
- **推导（指示函数法，老师称之为"优美技巧"）**：$\\mathbf{1}_{A^c} = 1 - \\mathbf{1}_A$，$\\mathbf{1}_{A \\cap B} = \\mathbf{1}_A \\mathbf{1}_B$，De Morgan 律给
$$\\mathbf{1}_{\\cup_i A_i} = 1 - \\prod_{i=1}^n (1 - \\mathbf{1}_{A_i})$$
两边取期望，利用 $E[\\mathbf{1}_A] = P(A)$、$E[\\mathbf{1}_{A_i}\\mathbf{1}_{A_j}] = P(A_i \\cap A_j)$，展开即得容斥公式。
- **意义**：计算"至少一个事件发生"的概率，从并集上界升级为**精确**公式；帽子问题中"无人拿到自己帽子"等错排计数也源于它。

### 5.2 几何分布方差（S07.2）
$X \\sim \\text{Geom}(p)$（直到首次正面所需抛掷次数）。用**无记忆性 + 总期望定理**解出二阶矩：
$$E[X^2] = p \\cdot 1 + (1-p) \\cdot E[(X+1)^2] = p + (1-p)\\big(E[X^2] + 2E[X] + 1\\big)$$
代入 $E[X] = 1/p$，解方程得 $E[X^2] = \\dfrac{2}{p^2} - \\dfrac{1}{p}$，于是
$$\\text{var}(X) = E[X^2] - (E[X])^2 = \\frac{1-p}{p^2}$$
- **直觉**：$X \\ge 1$ 时"浪费的一次抛掷 + 重新开始的几何变量"在平方意义下也成立（$(X \\mid X>1) \\sim 1 + X$），所以二阶矩也能用首步分解。
- 验证：$p = 1/2$ 时 $\\text{var}(X) = 2$，与"均值 2、几何分布比较分散"的印象一致。

### 5.3 事件独立 vs 随机变量独立（S07.3）
- **桥梁**：事件 $A$ 的指示变量 $\\mathbf{1}_A$（$A$ 发生时取 1，否则取 0）。
- **等价性**：事件 $A, B$ 独立 $\\iff$ 指示变量 $\\mathbf{1}_A, \\mathbf{1}_B$ 独立；一般地，$n$ 个事件独立 $\\iff$ $n$ 个指示变量独立。
- **证明思路（两事件）**：$\\Rightarrow$ 方向直接看 $(1,1)$ 组合：$P(\\mathbf{1}_A = 1, \\mathbf{1}_B = 1) = P(A \\cap B) = P(A)P(B)$；$\\Leftarrow$ 方向要补足 $(1,0)$、$(0,1)$、$(0,0)$ 三个组合，用"独立事件的补集也独立"（L03.4）逐个完成。
- **意义**：两种独立性是同一件事的两种说法，哪个方便用哪个——算期望用变量视角（可拆 $E[XY]$），说人话用事件视角（"知道 $A$ 发生不会改变对 $B$ 的信念"）。

---

"""

# ---------- L09：补充 Buffon 针 ----------
inserts['L09 事件条件作用与联合连续随机变量.md'] = """## 5. 补充：Buffon 针与蒙特卡洛模拟（S09.1，p99）

- **问题（几何概率的著名开端，18 世纪 Buffon 伯爵）**：平面上画间距 $d$ 的平行线，随机投掷一根长 $l$（$l < d$）的针，求针与某条线相交的概率。
- **建模**：$X$ = 针心到最近线的垂直距离，$X \\sim U(0, d/2)$；$\\Theta$ = 针与线的夹角（取锐角），$\\Theta \\sim U(0, \\pi/2)$；假设 $X, \\Theta$ 独立，联合 PDF $f(x, \\theta) = \\frac{2}{d} \\cdot \\frac{2}{\\pi} = \\frac{4}{\\pi d}$（均匀性 = "完全随机"）。
- **相交条件**：针的垂直延伸 $\\frac{l}{2} \\sin\\theta$ 超过距离 $x$，即 $x \\le \\frac{l}{2} \\sin\\theta$。
- **计算**：
$$P = \\int_0^{\\pi/2} \\int_0^{(l/2)\\sin\\theta} \\frac{4}{\\pi d}\\, dx\\, d\\theta = \\frac{4}{\\pi d} \\cdot \\frac{l}{2} \\int_0^{\\pi/2} \\sin\\theta\\, d\\theta = \\frac{2l}{\\pi d}$$
- **蒙特卡洛应用**：若不知 $\\pi$，随机投针 $N$ 次、统计相交频率 $\\hat p$，由 $\\pi \\approx \\frac{2l}{d \\hat p}$ 反推 $\\pi$——这是"用随机模拟估计难算的量"的经典例子。推广：在 $n$ 维单位立方体内均匀投点，落入复杂子集的比例 ≈ 子集体积（体积 = 概率 × 总体积），是工程/物理中难以解析积分时的标准手段。

---

"""

# ---------- L11：补充 模拟 ----------
inserts['L11 导出分布.md'] = """## 5. 补充：模拟（S11.1，p120）

- **目的**：当 $g(X_1, \\dots, X_n)$ 的导出分布解析求不出时，生成服从已知分布的随机样本、评估 $g$、画直方图，用频率了解统计特性。
- **离散情形（区间法）**：把 $[0,1]$ 按概率质量切成与 $p_i$ 等长的区间，$U \\sim U(0,1)$ 落在哪个区间就输出对应取值——输出的变量有正确的 PMF。图形上等价于"取 $U$，在 CDF 的跳跃处对齐，报告对应的 $x$"。
- **连续情形（逆变换采样）**：设 $F$ 连续且严格单调，令
$$X = F^{-1}(U), \\qquad U \\sim U(0,1)$$
则 $X$ 的 CDF 恰为 $F$。验证：$P(X \\le c) = P(F^{-1}(U) \\le c) = P(U \\le F(c)) = F(c)$（均匀变量的 CDF 是 $u$ 本身）。
- **图形直觉**：从 $y$ 轴取 $U \\in (0,1)$，水平走到 CDF 曲线，再垂直落到 $x$ 轴，读出的 $x$ 就是样本。
- **例（指数分布）**：$F(x) = 1 - e^{-x}$（参数 1），解 $u = 1 - e^{-x}$ 得 $x = -\\ln(1-u)$，即 $X = -\\ln(1-U) \\sim \\text{Exp}(1)$——计算机生成指数随机变量的标准做法（随机数生成器通常只给均匀分布）。

---

"""

# ---------- L20：显著性检验 ----------
inserts['L20 经典统计学导论.md'] = """## 5. 补充：假设检验与显著性检验（依据教材第 9.3–9.4 节；视频 L20 只到估计/置信区间，未专讲检验）

> 经典统计的两大任务：**估计**（L20 已讲）与**假设检验**。本节按教材《Introduction to Probability》第 9.3–9.4 节整理，6.041SC 视频没有专门一讲。

### 5.1 基本设定
- **零假设 $H_0$**：要检验的"默认"命题，如"硬币均匀 $p = 1/2$"；备择假设 $H_1$ 是其反面。
- 基于观测 $X_1, \\dots, X_n$ 决定**拒绝或不拒绝 $H_0$**（"不拒绝"≠"证明 $H_0$ 正确"，只是证据不足）。

### 5.2 显著性检验的步骤（数据观测前确定好）
1. 选一个**统计量** $S = h(X_1, \\dots, X_n)$：把 $n$ 个数据压缩成一个标量（如样本均值 $\bar X$）；
2. 确定**拒绝域的形状**：$S$ 取哪些值就拒绝 $H_0$（作为待定临界值 $\\xi$ 的函数，如"$\bar X > \\xi$"）；
3. 选**显著性水平** $\\alpha$：期望的"错误拒绝 $H_0$"概率（常用 0.05）；
4. 定**临界值** $\\xi$：使 $P(\\text{拒绝} \\mid H_0 \\text{ 为真}) = \\alpha$（或近似）。
- 观测后：算 $s = h(x_1, \\dots, x_n)$，若 $s$ 落入拒绝域则拒绝 $H_0$，否则不拒绝。
- **p 值**（常用等价说法）：在 $H_0$ 下，观察到"像实际数据这样极端或更极端"的统计量的概率；p 值 $< \\alpha$ 就拒绝。

### 5.3 两类错误与检验势
- **第一类错误**（误拒）：$H_0$ 为真却被拒绝，概率 $= \\alpha$；
- **第二类错误**（漏检）：$H_0$ 为假却未拒绝，概率记为 $\\beta$；检验的**势**（power）$= 1 - \\beta$；
- 权衡：$\\alpha$ 压得越低（更保守），$\\beta$ 通常越高（更难发现 $H_0$ 为假），需按后果权衡。

### 5.4 似然比检验（LRT）与 Neyman-Pearson 引理
- **LRT**：构造似然比 $L(X) = \\dfrac{p(X \\mid H_1)}{p(X \\mid H_0)}$，选定 $\\xi$ 使错误拒绝概率为 $\\alpha$，观测后 $L(x) > \\xi$ 则拒绝 $H_0$——"数据更支持哪一边"。
- **Neyman-Pearson 引理**：在固定第一类错误 $\\alpha$ 的所有检验中，LRT 使第二类错误 $\\beta$ 最小（即势最大）。直观：似然比是给定 $\\alpha$ 下"最灵敏"的检验准则。

---

"""

# ---------- L26：连续时间马尔可夫链 ----------
inserts['L26 马尔可夫链吸收概率与时间.md'] = """## 5. 扩展：连续时间马尔可夫链（依据教材第 7.5 节；视频 L26 未讲，超出 6.041SC 范围）

- **定义**：状态 $i$ 的**停留时间**服从指数分布 $\\text{Exp}(\\nu_i)$（参数只依赖当前状态），离开后以概率 $p_{ij}$ 跳到状态 $j$；指数分布的无记忆性保证"未来只依赖当前状态"，即马尔可夫性在连续时间下成立。
- **速率描述（等价形式）**：给定当前状态 $i$，$\\delta$ 时间后处于 $j$（$j \\neq i$）的概率为 $q_{ij}\\delta + o(\\delta)$，其中 $q_{ij} = \\nu_i p_{ij}$ 称为**转移速率**；$\\nu_i = \\sum_{j \\neq i} q_{ij}$。
- **稳态方程（与离散链类比）**：单回归类时，稳态概率 $\\pi_j$ 满足"流出速率 = 流入速率"：
$$\\pi_j \\sum_{k \\neq j} q_{jk} = \\sum_{k \\neq j} \\pi_k q_{kj}, \\qquad \\sum_{k=1}^m \\pi_k = 1$$
暂态状态 $\\pi_j = 0$，回归状态 $\\pi_j > 0$。离散链的平衡方程 $\\pi_j = \\sum_k \\pi_k p_{kj}$ 对应这里的"速率平衡"版本。
- **应用**：排队系统（服务台忙/闲）、电话线路占用数、生灭过程都常用 CTMC 建模——L26 例 4 的 Erlang 公式正是这类模型的产物。

---

"""

for fname, block in inserts.items():
    path = os.path.join(NOTES, fname)
    s = io.open(path, encoding='utf-8').read()
    assert s.count(ANCHOR) == 1, fname + ' anchor count != 1'
    s = s.replace(ANCHOR, block + ANCHOR, 1)
    io.open(path, 'w', encoding='utf-8', newline='').write(s)
    print('OK', fname)
