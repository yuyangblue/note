(\tau_i)}\right)\Delta t_i$

$\leq \sum_{i=1}^n \sqrt{[\dot{x}(\xi_i) - \dot{x}(\tau_i)]^2 + [\dot{y}(\eta_i) - \dot{y}(\tau_i)]^2 + [\dot{z}(\zeta_i) - \dot{z}(\tau_i)]^2}\,\Delta t_i$

下证 $\lim_{d \to 0} \sigma = 0$

因为 $\dot{r}(t)$ 在 $[\alpha, \beta]$ 上连续，且 $\dot{r}(t) \neq 0$，所以存在常数 $m &gt; 0$，

s.t. $\sqrt{\dot{x}^2(\xi_i) + \dot{y}^2(\eta_i) + \dot{z}^2(\zeta_i)} \geq m$，$\|P_{i-1}P_i\| \geq m\,\Delta t_i$

令 $\Delta t = \max_{1 \leq i \leq n} \Delta t_i$，则 $\Delta t \leq \frac{d}{m}$，当 $d \to 0$ 时，$\Delta t \to 0$

依条件 $\dot{x}(t), \dot{y}(t), \dot{z}(t)$ 在 $[\alpha, \beta]$ 连续 $\to$ 一致连续

对任何 $\varepsilon &gt; 0$，存在 $\delta &gt; 0$，当 $t, t' \in [\alpha, \beta]$ 且 $|t - t'| &lt; \delta$ 时，

有 $|\dot{x}(t) - \dot{x}(t')| &lt; \varepsilon$，$|\dot{y}(t) - \dot{y}(t')| &lt; \varepsilon$，$|\dot{z}(t) - \dot{z}(t')| &lt; \varepsilon$

当 $d &lt; m\delta$ 时，$\Delta t &lt; \frac{d}{m} &lt; \delta$，此时 $\xi_i, \eta_i, \zeta_i, \tau_i \in [t_{i-1}, t_i]$

所以 $0 \leq \sigma \leq \sum_{i=1}^n \sqrt{3\varepsilon^2}\,\Delta t_i = \sqrt{3}\varepsilon \cdot \sum_{i=1}^n \Delta t_i = \sqrt{3}\varepsilon(\beta - \alpha)$

因此 $\lim_{d \to 0} S_n = \lim_{d \to 0} \sum_{i=1}^n \|\dot{r}(\tau_i)\|\Delta t_i + 0$

$= \lim_{d \to 0} \sum_{i=1}^n \|\dot{r}(\tau_i)\|\Delta t_i$（$\Delta t &lt; \frac{d}{m}$）

$= \int_\alpha^\beta \|\dot{r}(t)\|\,dt$

##### 2. 弧微分与自然参数

因为 $dS = \sqrt{(dx)^2 + (dy)^2 + (dz)^2}$，所以用弧长 $S$ 为参数时

$\frac{dr}{dS} = \left(\frac{dx}{dS}, \frac{dy}{dS}, \frac{dz}{dS}\right)$ 是单位切向量，设它与 $x$ 轴、$y$ 轴、$z$ 轴正方向的夹角分别为 $\alpha, \beta, \gamma$，则 $\frac{dr}{dS} = (\cos\alpha, \cos\beta, \cos\gamma)$

$dx = \cos\alpha\,dS$，$dy = \cos\beta\,dS$，$dz = \cos\gamma\,dS$

**例6.5.** 求螺旋线 $\begin{cases} x = a\cos\theta \\ y = a\sin\theta \\ z = k\theta \end{cases}$ 一个螺距 $(0 \leq \theta \leq 2\pi)$ 之间的长度

**解：** $\dot{r}(\theta) = (-a\sin\theta, a\cos\theta, k)$

$\|\dot{r}(\theta)\| = \sqrt{a^2 + k^2}$，弧长 $S = \int_0^{2\pi} \|\dot{r}(\theta)\|\,d\theta = 2\pi\sqrt{a^2 + k^2}$

---

#### §5.6.3 曲面的切平面和法线

##### 1. 曲面的参数方程

球面的参数方程：设 $P(x,y,z)$ 为球面上一点，在 $xOy$ 平面投影为 $M(x,y,0)$

$P(\|OP\|\sin\varphi\cos\theta, \|OP\|\sin\varphi\sin\theta, \|OP\|\cos\varphi)$，$\varphi \in [0, \pi]$，$\theta \in [0, 2\pi]$

$\varphi$ 取长度是纬线，$\theta$ 取长度是经线

一般地，曲面 $S$ 的参数方程 $r(u,v) = (x(u,v), y(u,v), z(u,v))$，$(u,v) \in \Delta$

$r: \Delta \subset \mathbb{R}^2 \to \mathbb{R}^3$

##### 2. 曲面上的曲线表示

在曲面 $S$ 上，考虑两类曲线：
- (1) $v = v_0$，$u$ 变化，称为 $u$ 曲线
- (2) $u = u_0$，$v$ 变化，称为 $v$ 曲线

它们的切向量分别为 $r_u = (x_u, y_u, z_u)$，$r_v = (x_v, y_v, z_v)$

##### 3. 曲面的切平面与法线

设曲面 $S$ 的参数方程为 $r(u,v) = (x(u,v), y(u,v), z(u,v))$，$(u,v) \in \Delta$

其中 $r(u,v)$ 在 $\Delta$ 上连续，在点 $(u_0, v_0) \in \Delta$ 可微

过曲面 $S$ 上任一点 $r_0 = r(u_0, v_0)$ 的任一光滑曲线 $\Gamma$ 的参数方程为

$r = r(u(t), v(t))$，$t \in I$

其中 $u(t_0) = u_0$，$v(t_0) = v_0$，在上式中对 $t$ 求导得

$\dot{r}(t_0) = r_u(u_0, v_0)\dot{u}(t_0) + r_v(u_0, v_0)\dot{v}(t_0)$

右端是切向量 $r_u(u_0, v_0)$ 与 $r_v(u_0, v_0)$ 的线性组合

若 $r_u \times r_v\big|_{(u_0, v_0)} \neq 0$，说明 $r_u(u_0, v_0)$ 与 $r_v(u_0, v_0)$ 不共线

从而可张成一个平面 $\Pi$，称之为曲面 $S$ 在正则点 $r_0$ 的**切平面**

它的法向量为 $r_u \times r_v = \begin{vmatrix} i & j & k \\ x_u & y_u & z_u \\ x_v & y_v & z_v \end{vmatrix} = (A, B, C)$

其中 $A = \frac{\partial(y,z)}{\partial(u,v)}\big|_{(u_0,v_0)}$，$B = \frac{\partial(z,x)}{\partial(u,v)}\big|_{(u_0,v_0)}$，$C = \frac{\partial(x,y)}{\partial(u,v)}\big|_{(u_0,v_0)}$

切平面方程为 $A(x-x_0) + B(y-y_0) + C(z-z_0) = 0$

法线方程 $\frac{x-x_0}{A} = \frac{y-y_0}{B} = \frac{z-z_0}{C}$

若 $r_u = (x_u, y_u, z_u)$，$r_v = (x_v, y_v, z_v)$ 在 $\Delta$ 内连续，称曲面**光滑**

对曲面 $F(x,y,z) = 0$，设在点 $P_0(x_0, y_0, z_0)$ 处满足隐函数定理的条件

$F_z(P_0) \neq 0$，隐函数 $z = z(x,y)$，曲面 $S: r(x,y) = (x, y, z(x,y))$

$r_x = (1, 0, z_x) = (1, 0, -\frac{F_x}{F_z})$，$r_y = (0, 1, z_y) = (0, 1, -\frac{F_y}{F_z})$

$r_x \times r_y = \begin{vmatrix} i & j & k \\ 1 & 0 & -\frac{F_x}{F_z} \\ 0 & 1 & -\frac{F_y}{F_z} \end{vmatrix} = (\frac{F_x}{F_z}, \frac{F_y}{F_z}, 1)$

法向量 $\vec{n} = (F_x, F_y, F_z) = \nabla F$

切平面方程 $F_x(P_0)(x-x_0) + F_y(P_0)(y-y_0) + F_z(P_0)(z-z_0) = 0$

法线方程 $\frac{x-x_0}{F_x(P_0)} = \frac{y-y_0}{F_y(P_0)} = \frac{z-z_0}{F_z(P_0)}$

**例6.8.** 求曲面 $\begin{cases} x = u\cos v \\ y = u\sin v \\ z = av \end{cases}$ 在 $u = \sqrt{2}$，$v = \frac{\pi}{4}$ 处的切平面与法线方程

**解：** $r = (u\cos v, u\sin v, av)$

$r_u = (\cos v, \sin v, 0)$，$u = \sqrt{2}$，$r_u = (\frac{1}{\sqrt{2}}, \frac{1}{\sqrt{2}}, 0)$

$r_v = (-u\sin v, u\cos v, a)$，$v = \frac{\pi}{4}$，$r_v = (-1, 1, a)$

$\therefore r_u \times r_v\big|_{(\sqrt{2}, \frac{\pi}{4})} = \begin{vmatrix} i & j & k \\ \frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} & 0 \\ -1 & 1 & a \end{vmatrix} = (\frac{a}{\sqrt{2}}, -\frac{a}{\sqrt{2}}, \sqrt{2}) \parallel (a, -a, 2)$

取法向量 $n = (a, -a, 2)$，切平面 $a(x-1) - a(y-1) + 2(z - \frac{\pi}{4}a) = 0$

$ax - ay + 2z = \frac{\pi}{2}a$

**例1.** 环面的参数方程：考虑 $yOz$ 平面上的圆周 $C: (y-a)^2 + z^2 = b^2$

其中 $a &gt; b &gt; 0$，该曲面绕 $z$ 轴一周形成环面，求曲面方程

**解：** (1) 圆周 $C$ 的参数方程为 $y = a + b\cos u$，$z = b\sin u$，$u \in [0, 2\pi]$

$\begin{cases} z = b\sin u \\ x = r\cos v = (a + b\cos u)\cos v \\ y = r\sin v = (a + b\cos u)\sin v \end{cases}$

$u \in [0, 2\pi]$，$v \in [0, 2\pi]$

**例2.** Möbius 带：矩形一组对边反向粘合

细杆的两种旋转的合成，绕自身中心和绕 $z$ 轴

开始时细杆 $L$ 占据 $\{(a, 0, u) | |u| \leq l\}$

细杆中点 $C$ 绕 $xOy$ 面上的圆周 $x^2 + y^2 = a^2$ 逆时针

细杆绕 $C$ 自转，s.t. $L$ 与 $z$ 轴所在平面与 $xOy$ 面垂直

且当 $C$ 转 $\theta$ 角时，细杆绕 $C$ 转 $\frac{\theta}{2}$ 角

细杆端点 $z = u\cos\frac{\theta}{2}$，$|u| \leq l$

$x = OP'\cos\theta = (a + u\sin\frac{\theta}{2})\cos\theta$，$\theta \in [0, 2\pi]$

$y = OP'\sin\theta = (a + u\sin\frac{\theta}{2})\sin\theta$

---

### §5.7 空间曲线的曲率与挠率

#### §5.7.1 Frenet 标架

设曲线 $\Gamma$ 的自然参数方程为 $r = r(s)$

一般参数方程为 $r = r(t)$

求导：$\frac{dr}{ds} = r'$，$\frac{d^2r}{ds^2} = r''$

$\frac{dr}{dt} = \dot{r}$，$\frac{d^2r}{dt^2} = \ddot{r}$

##### 1. 切线与法平面

与曲线 $\Gamma: r = r(s)$ 正向一致的单位切向量 $T(s) = r'(s)$

$dr = (dx, dy, dz)$，$ds^2 = dx^2 + dy^2 + dz^2 = \|dr\|^2$

$\therefore \|dr/ds\| = 1$，$\therefore r'(s)$ 是单位切向量

于是 $\Gamma$ 在点 $r(s_0)$ 的切线方程为 $\rho - r(s_0) = \lambda r'(s_0)$

法平面方程 $[\rho - r(s_0)] \cdot r'(s_0) = 0$

##### 2. 主法线和次法线

因为 $\|r'(s)\| = 1 = \langle r'(s), r'(s) \rangle$

对 $s$ 求导 $0 = r''(s) \cdot r'(s) + r'(s) \cdot r''(s) = 2r'(s) \cdot r''(s)$

$\Rightarrow r'(s) \perp r''(s)$，$r'(s) \times r''(s) \neq 0$

称 $N(s_0) = \frac{r''(s_0)}{\|r''(s_0)\|}$ 为曲线 $\Gamma$ 在 $r(s_0)$ 的**主法向量**

对应法线为主法线

记 $B(s_0) = T(s_0) \times N(s_0)$

为曲线 $\Gamma$ 在 $r(s_0)$ 的**次法向量**

$B(s)$ 与 $N(s)$ 张成法平面

##### 3. 密切平面

过曲线 $\Gamma$ 上的点 $r(s_0)$ 的平面有无穷多个，欲知 which 与 $\Gamma$ 最贴近

当 $\Gamma$ 为空间曲线时，考虑 $r(s_0)$ 邻近点 $r(s_0 + \Delta s)$

到上述平面的距离，设 $\Sigma$ 是过 $r(s_0)$ 的平面，其单位法向量 $n$，则 $r(s_0 + \Delta s)$ 到平面 $\Sigma$ 的距离

$d = \lambda = n \cdot (r(s_0 + \Delta s) - r(s_0))$

由 Taylor 公式

$r(s_0 + \Delta s) - r(s_0) = r'(s_0)\Delta s + \frac{1}{2}r''(s_0)(\Delta s)^2 + \vec{o}((\Delta s)^2)$

于是 $d = n \cdot r'(s_0)\Delta s + n \cdot r''(s_0)\frac{(\Delta s)^2}{2} + \vec{o}((\Delta s)^2)$

(1) 若 $\Sigma$ 不含切线，则 $n \cdot r'(s_0) \neq 0$

---

## 第六章 多元函数积分学及其应用

### §6.1 多元积分的概念与性质

#### 6.1.1 物体质量

设物体 $\Omega$ 的密度函数 $f$ 连续，求质量 $M$

**分割：** 将 $\Omega$ 分成 $n$ 个小部分 $\Omega_i$（$1 \leq i \leq n$），得分割 $T = \{\Omega_i\}$

**近似：** 任取 $P_i \in \Omega_i$，因 $f$ 在 $\Omega$ 连续，故当 $\Omega_i$ 的度量 $\Delta\Omega_i$ 充分小时，$f(P) \approx f(P_i)$，$P \in \Omega_i$

$\Omega_i$ 的质量 $M_i \approx f(P_i)\Delta\Omega_i$

**求和：** $M = \sum_{i=1}^n M_i \approx \sum_{i=1}^n f(P_i)\Delta\Omega_i$

**取极限：** 当 $\|T\| = \max_{1 \leq i \leq n} \Omega_i$ 的直径 $\to 0$ 时

$M = \lim_{\|T\| \to 0} \sum_{i=1}^n f(P_i)\Delta\Omega_i$

#### 6.1.2 多元积分的概念

若有界的几何体 $\Omega$ 可求长、面积、体积...则称它**可度量**

**定理A.** 有界几何体 $\Omega$ 可度量 $\iff \Omega$ 的边界的度量为0

| 几何体 | 边界 |
|--------|------|
| 线段 $[a,b]$ | $\{a, b\}$ |
| 平面图形 | 曲线 |
| 立体 | 曲面 |

**定义6.1.** 设 $\Omega$ 是有界几何体，它是可度量的，再设 $f$ 是 $\Omega$ 上的有界函数，任取 $\Omega$ 的分割 $T = \{\Omega_i\}$ 以及介点集 $\{P_i\}$，当分割细度 $\|T\| = \max_{1 \leq i \leq n} \Omega_i$ 的直径 $\to 0$

若极限 $\lim_{\|T\| \to 0} \sum_{i=1}^n f(P_i)\Delta\Omega_i = J$ 存在，则称 $f$ 在 $\Omega$ 上**可积**，称 $J$ 为 $f$ 在 $\Omega$ 上的**积分**，记为 $J = \int_\Omega f(P)\,d\Omega$

$= \lim_{\|T\| \to 0} \sum_{i=1}^n f(P_i)\Delta\Omega_i$

(1) 若 $\Omega = [a,b]$ 且 $f$ 是 $\Omega$ 上的一元函数，则 $\int_\Omega f(P)\,d\Omega = \int_a^b f(x)\,dx$

(2) 若 $\Omega$ 是平面有界闭域 $D$ 且 $f$ 是 $D$ 上的二元函数，则 $\int_\Omega f(P)\,d\Omega = \iint_D f(x,y)\,d\sigma$ —— **二重积分**

(3) 若 $\Omega$ 是 $\mathbb{R}^3$ 中有界闭域（立体）且 $f$ 是 $\Omega$ 上的三元函数

则 $\int_\Omega f(P)\,d\Omega = \iiint_\Omega f(x,y,z)\,dV$ —— **三重积分**

(4) 若 $\Omega$ 是有界可求长曲线 $\Gamma$ 且 $f$ 是 $\Gamma$ 上的二元函数 or 三元函数

$\int_\Omega f(P)\,d\Omega = \int_\Gamma f(x,y)\,ds$ 或 $\int_\Gamma f(x,y,z)\,ds$ —— **曲线积分**

(5) 若 $\Omega$ 是曲面 $S$ 且 $f$ 是 $S$ 上的三元函数，则

$\int_\Omega f(P)\,d\Omega = \iint_S f(x,y,z)\,dS$ —— **曲面积分**

#### 6.1.3 积分的存在条件和性质

1. **存在条件：** 设 $\Omega$ 是有界闭域且可度量，则下列函数在 $\Omega$ 上可积：
   - (1) 连续函数
   - (2) 不连续点集度量为0

2. **线性性质：** $\int_\Omega [k_1 f(P) + k_2 g(P)]\,d\Omega = k_1\int_\Omega f(P)\,d\Omega + k_2\int_\Omega g(P)\,d\Omega$

3. **区域可加性：** 设 $\Omega = \Omega_1 \cup \Omega_2$，且 $\Omega_1, \Omega_2$ 内部不相交，则

   $\int_\Omega f(P)\,d\Omega = \int_{\Omega_1} f(P)\,d\Omega + \int_{\Omega_2} f(P)\,d\Omega$

4. **积分不等式：**
   - (1) $f(P) \leq g(P)$，$P \in \Omega \Rightarrow \int_\Omega f(P)\,d\Omega \leq \int_\Omega g(P)\,d\Omega$
   - (2) $\left|\int_\Omega f(P)\,d\Omega\right| \leq \int_\Omega |f(P)|\,d\Omega$
   - (3) 若 $m \leq f(P) \leq M$，$\forall P \in \Omega$ 则 $m\Delta\Omega \leq \int_\Omega f(P)\,d\Omega \leq M\Delta\Omega$

   其中 $\Delta\Omega$ 为 $\Omega$ 的度量

5. **中值定理：** 设 $\Omega$ 为可度量的有界闭域，$f$ 在 $\Omega$ 连续，则

   存在 $\xi \in \Omega$ 使 $\int_\Omega f(P)\,d\Omega = f(\xi)\Delta\Omega$

**$\Omega$ 可度量：** 以平面情形为例，平面图形的面积

设 $P$ 是平面有界图形，存在矩形 $R \supset P$，用平行于坐标轴的直线网将 $P$ 分割，此时 $T$ 的网眼——小闭矩形可分为三类：
- ① $\Delta_i$ 中全为 $P$ 内点
- ② $\Delta_i$ 中全为 $P$ 外点
- ③ $\Delta_i$ 中含 $P$ 的边界点

**定义：** 内和 $s_P(T)$ = ①类小矩形面积之和，若不存在则定义 $s_P(T) = 0$

外和 $S_P(T)$ = ①②类小矩形面积之和

则内和 $s_P(T) \leq$ 矩形 $R$ 的面积

外和 $S_P(T) \geq s_P(T)$

**定义：** 内面积 $\underline{I}_P = \sup_T s_P(T)$

外面积 $\bar{I}_P = \inf_T S_P(T)$

若 $\underline{I}_P = \bar{I}_P = I_P$，则称 $P$ 可求面积，称 $I_P$ 为 $P$ 的面积

**定理$\alpha_1$：** 平面有界图形可求面积 $\iff$ 对任何 $\varepsilon &gt; 0$，存在直线网 $T$ 使 $S_P(T) - s_P(T) &lt; \varepsilon$

**推论1：** 平面有界图形 $P$ 的面积为 $0 \iff \bar{I}_P = 0$，即 $\forall \varepsilon &gt; 0$，存在直线网 $T$，使 $S_P(T) &lt; \varepsilon$，或存在有限个小矩形覆盖 $P$，面积之和 $&lt; \varepsilon$

**定理$\alpha_2$：** 平面有界图形 $P$ 可求面积 $\iff P$ 的边界面积为0

**定理$\alpha_3$：** 若曲线 $K$ 是 $[a,b]$ 上的连续函数 $f(x)$ 图像，则 $K$ 的面积为0

**推论2：** 参数方程 $x = \varphi(t)$，$y = \psi(t)$，$\alpha \leq t \leq \beta$，所表示的光滑曲线（$\varphi'(t), \psi'(t)$ 连续且 $\varphi'^2(t) + \psi'^2(t) \neq 0$）$K$ 的面积为0

**推论3：** 平面上分段光滑曲线围成的有界闭区域可求面积

**注：** 存在不可求面积的平面有界图形，例如

$P = \{(x,y) | x, y \in [0,1] \cap \mathbb{Q}\}$

事实上，对任何直线网 $T$，因 $P$ 中点都为边界点，故含 $P$ 中点的小矩形均为③类，从而 $s_P(T) = 0$，$\underline{I}_P = 0$

下面证明③类小矩形全体覆盖正方形 $[0,1] \times [0,1]$，于是外面积是1

记③类小矩形的并集为 $E$，因为小矩形是闭集，所以 $E$ 是闭集，对任何 $Q(x_0, y_0) \in D$，由实数可用有理数列逼近，存在 $[0,1]$ 中的有理数列 $\{x_n\}$，$\{y_n\}$ 使

$x_n \to x_0$，$y_n \to y_0$，从而 $(x_n, y_n) \in P$ 且 $(x_n, y_n) \to (x_0, y_0)$

说明 $Q$ 是 $P$ 的聚点，又因 $P \subset E$，所以 $Q$ 是 $E$ 的聚点，又 $E$ 是闭集，$\therefore Q \in E$，$D \subset E$

---

### §6.2 二重积分的计算

#### 6.2.1 二重积分的几何意义

任取 $D$ 的分割 $T = \{\sigma_i\}$ 以及 $\{\xi_i, \eta_i\} \in \sigma_i$

则 $\sigma_i$ 对应的曲顶柱体体积 $\Delta V_i \approx f(\xi_i, \eta_i)\Delta\sigma_i$

于是 $\iint_D f(x,y)\,d\sigma = \lim_{\|T\| \to 0} \sum_{i=1}^n f(\xi_i, \eta_i)\Delta\sigma_i$

#### 6.2.2 直角坐标系下二重积分的计算

##### 一、矩形区域 $D = [a,b] \times [c,d]$

曲顶柱体与平面 $x = x_0$ 的截面

是曲边梯形，面积 $A(x_0) = \int_c^d f(x_0, y)\,dy$

曲顶柱体体积 $V = \int_a^b A(x)\,dx = \int_a^b dx \int_c^d f(x,y)\,dy$

**定理A：** 设 $f(x,y)$ 在 $D = [a,b] \times [c,d]$ 上可积，且对每个 $x \in [a,b]$ 积分 $\int_c^d f(x,y)\,dy$ 存在，则累次积分 $\int_a^b dx \int_c^d f(x,y)\,dy$ 存在且 $\iint_D f(x,y)\,d\sigma = \text{它}$

**分析：** 令 $F(x) = \int_c^d f(x,y)\,dy$，去证 $\iint_D f(x,y)\,d\sigma = \int_a^b F(x)\,dx$

右边 $= \lim_{\Delta x \to 0} \sum_{i=1}^n F(\xi_i)\Delta x_i$（$\Delta x = \max_{1 \leq i \leq n} \Delta x_i$）

$= \lim_{\Delta x \to 0} \sum_{i=1}^n \int_c^d f(\xi_i, y)\,dy \cdot \Delta x_i$

$= \lim_{\Delta x \to 0} \sum_{i=1}^n \sum_{k=1}^m \int_{y_{k-1}}^{y_k} f(\xi_i, y)\,dy \cdot \Delta x_i$

其中 $m_{ik} \leq f(\xi_i, y) \leq M_{ik}$

$m_{ik} = \inf_{\Delta_{ik}} f$，$M_{ik} = \sup_{\Delta_{ik}} f$

$\Delta_{ik} = [x_{i-1}, x_i] \times [y_{k-1}, y_k]$

$T = \{\Delta_{ik}\}$，$\|T\| = \max_{i,k} \Delta_{ik}$

$\sum_{i=1}^n \sum_{k=1}^m m_{ik}\Delta x_i \Delta y_k \leq \square \leq \sum_{i=1}^n \sum_{k=1}^m M_{ik}\Delta x_i \Delta y_k$

当 $\|T\| \to 0$ 时，左、右 $\to \iint_D f(x,y)\,d\sigma$

**定理B：** 若 $f(x,y)$ 在 $D = [a,b] \times [c,d]$ 上连续，则

$$\iint_D f(x,y)\,d\sigma = \int_a^b dx \int_c^d f(x,y)\,dy = \int_c^d dy \int_a^b f(x,y)\,dx$$

##### 二、一般区域

通常可分为

**X型区域：** 对任何 $x_0 \in (a,b)$，直线 $x = x_0$ 与区域边界的交点最多有2个，则此时区域可表示为

$D: a \leq x \leq b$，$y_1(x) \leq y \leq y_2(x)$

**Y型区域：** 对任何 $y_0 \in (c,d)$，直线 $y = y_0$ 与区域边界的交点至多2个，此时 $D: c \leq y \leq d$，$x_1(y) \leq x \leq x_2(y)$

**定理C：** 若 $f(x,y)$ 在X型区域 $D$ 上连续，其中 $D: a \leq x \leq b$，$y_1(x) \leq y \leq y_2(x)$，$y_1(x), y_2(x)$ 在 $[a,b]$ 连续，则

$$\iint_D f(x,y)\,d\sigma = \int_a^b dx \int_{y_1(x)}^{y_2(x)} f(x,y)\,dy$$

**证明：** 存在矩形 $R = [a,b] \times [c,d] \supset D$

令 $F(x,y) = \begin{cases} f(x,y) & (x,y) \in D \\ 0 & (x,y) \in R \setminus D \end{cases}$

因 $D$ 可求面积，故 $\partial D$ 面积为0

因 $f$ 在 $D$ 上连续，故 $F$ 的不连续点集 $K \subset \partial D$

从而 $K$ 的面积是0，因此 $F$ 在 $R$ 上可积，于是

$\iint_D f(x,y)\,d\sigma = \iint_R F(x,y)\,d\sigma = \int_a^b dx \int_c^d F(x,y)\,dy = \int_a^b dx \int_{y_1(x)}^{y_2(x)} f(x,y)\,dy$

##### 三、例题

**例2.1.** $\iint_D (1 - \frac{x}{3} - \frac{y}{4})\,d\sigma$，其中 $D = [-1,1] \times [-2,2]$

**解：** $I = \int_{-1}^1 dx \int_{-2}^2 (1 - \frac{x}{3} - \frac{y}{4})\,dy$

$= \int_{-1}^1 4(1 - \frac{x}{3})\,dx = 8$

**例2.2.** $\iint_D (x^2 + y^2)\,d\sigma$，$D$ 由 $y = 0$，$x = 1$ 及 $y = x^2$ 围成

**解：** $I = \int_0^1 dx \int_0^{x^2} (x^2 + y^2)\,dy$

$= \int_0^1 (x^4 + \frac{1}{3}x^6)\,dx$

$= \frac{1}{5} + \frac{1}{21} = \frac{26}{105}$

**例2.3.** $\iint_D xy\,d\sigma$，其中 $D$ 由 $y^2 = x$ 与 $y = x - 2$ 围成

看作Y形区域，不分区

$I = \int_{-1}^2 dy \int_{y^2}^{y+2} xy\,dx = \int_{-1}^2 y \cdot \frac{1}{2}[(2+y)^2 - y^4]\,dy = \frac{45}{8}$

**例2.4.** $\iint_D \frac{\sin x}{x}\,d\sigma$，其中 $D$ 由 $y = x$ 和 $y = x^2$ 围成

$I = \int_0^1 dx \int_{x^2}^x \frac{\sin x}{x}\,dy = \int_0^1 \frac{\sin x}{x}(x - x^2) = \int_0^1 (1-x)\sin x\,dx$

---

#### 分部积分的列表算法

$\int uv^{(n+1)}\,dx = \int u\,dv^{(n)} = uv^{(n)} - \int u'v^{(n)}\,dx$ —— ①

$= uv^{(n)} - \int u'\,dv^{(n-1)}$

$= uv^{(n)} - u'v^{(n-1)} + \int u''v^{(n-1)}\,dx$ —— ②

$= uv^{(n)} - u'v^{(n-1)} + u''v^{(n-2)} - \int u'''v^{(n-2)}\,dx$ —— ③

$= \cdots$

$= uv^{(n)} - u'v^{(n-1)} + \cdots + (-1)^n u^{(n)}v + (-1)^{n+1}\int u^{(n+1)}v\,dx$

| 求导 | $u$ | $u'$ | $u''$ | $u'''$ | $\cdots$ | $u^{(n)}$ | $u^{(n+1)}$ |
|------|-----|------|-------|--------|----------|-----------|-------------|
| 积分 | $v^{(n+1)}$ | $v^{(n)}$ | $v^{(n-1)}$ | $v^{(n-2)}$ | $\cdots$ | $v'$ | $v$ |

符号：$+$，$-$，$+$，$-$，$\cdots$，$(-1)^n$，$(-1)^{n+1}$ 积分

**e.g.** $\int x^4 e^x\,dx$：$x^4$，$4x^3$，$12x^2$，$24x$，$24$，$0$

$e^x$，$e^x$，$e^x$，$e^x$，$e^x$，$e^x$

原式 $= e^x(x^4 - 4x^3 + 12x^2 - 24x + 24) + C$

**e.g.** $\int x^3 \sin x\,dx$：

$\begin{pmatrix} x^3 & 3x^2 & 6x & 6 & 0 \\ \sin x & -\cos x & -\sin x & \cos x & \sin x \end{pmatrix}$

$= \cos x(-x^3 + 6x) + \sin x(3x^2 - 6) + C$

**e.g.** $\int_0^1 (1-x)\sin x\,dx = [(1-x)(-\cos x) - \sin x]_0^1 = 1 - \sin 1$

$1-x$：$-1$，$0$

$\sin x$：$-\cos x$，$-\sin x$

**例.** $\iint_D (xy^3 + xy^3 e^{x^2+y^2})\,d\sigma$，其中 $D$ 由 $y = x$，$y = -1$，$x = 1$ 围成

**解：** 令 $D_1 = \triangle OAB$，$D_2 = \triangle OBC$

$xy^3$ 在 $D_1$ 上关于 $x$ 是奇函数

$D_2$ 上关于 $y$ 是奇函数

$\iint_D xy^3 e^{x^2+y^2}\,d\sigma = \iint_{D_1} + \iint_{D_2} = 0$

而由对称性 $\iint_D xy^3\,d\sigma = 2\iint_{D_2} xy^3\,d\sigma = 4\iint_{\substack{x \geq 0 \\ y \leq 0 \\ x \geq y}} xy^3\,d\sigma = 2\iint_{\substack{0 \leq x \leq 1 \\ -x \leq y \leq 0}} xy^3\,d\sigma$

$= 2\int_0^1 x\,dx \times \int_{-x}^0 y^3\,dy = \frac{2}{9}$

**例2.** 交换累次积分的顺序

$I = \int_{-2}^0 dy \int_0^{\frac{2+y}{2}} f(x,y)\,dx + \int_0^2 dy \int_0^{\frac{2-y}{2}} f(x,y)\,dx$

**解：** $I = \iint_D f(x,y)\,d\sigma$

$= \int_0^1 dy \int_{2y-2}^{2-2y} f(x,y)\,dx$

---

#### 6.2.3 极坐标系下二重积分的计算

$$\iint_D f(x,y)\,d\sigma = \lim_{\|T\| \to 0} \sum_{i=1}^n f(\xi_i, \eta_i)\Delta\sigma_i$$

使用 $r = \text{const}$，$\theta = \text{const}$ 分割区域 $D$

$\Delta\sigma = \frac{1}{2}[(r+\Delta r)^2\Delta\theta - r^2\Delta\theta]$

$= r\Delta r\Delta\theta + \frac{1}{2}(\Delta r)^2\Delta\theta$

当 $|\Delta r|$，$|\Delta\theta|$ 充分小，$\Delta\sigma \approx r\Delta r\Delta\theta$

于是 $\lim_{\|T\| \to 0} \sum_{i=1}^n f(\xi_i, \eta_i)\Delta\sigma_i = \lim_{\|T\| \to 0} \sum_{i=1}^n f(\xi_i, \eta_i)r_i\Delta r_i\Delta\theta_i$

$$\iint_D f(x,y)\,d\sigma = \iint_\Delta f(r\cos\theta, r\sin\theta)\,r\,dr\,d\theta$$

其中 $\Delta$ 是 $D$ 在 $r$-$\theta$ 平面内的对应区域

##### 1. 原点在 $D$ 外

**$\theta$形区域：** $\theta = \theta_0$ 与 $D$ 的边界交点至多2个，

此时 $\Delta: \alpha \leq \theta \leq \beta$，$r_1(\theta) \leq r \leq r_2(\theta)$

$$\iint_D f(x,y)\,d\sigma = \int_\alpha^\beta d\theta \int_{r_1(\theta)}^{r_2(\theta)} f(r\sin\theta, r\cos\theta)\,r\,dr$$

**$r$形区域：** $r = r_0$ 与 $D$ 的边界交点至多2个，

此时 $\Delta: r_1 \leq r \leq r_2$，$\theta_1(r) \leq \theta \leq \theta_2(r)$

$$\iint_D f(x,y)\,d\sigma = \int_{r_1}^{r_2} dr \int_{\theta_1(r)}^{\theta_2(r)} f(r\cos\theta, r\sin\theta)\,r\,d\theta$$

##### 2. 原点在 $D$ 内

此时 $\Delta: 0 \leq \theta \leq 2\pi$，$0 \leq r \leq r(\theta)$

其中 $r = r(\theta)$ 是 $D$ 的边界曲线的极坐标方程

$$\iint_D f(x,y)\,d\sigma = \int_0^{2\pi} d\theta \int_0^{r(\theta)} f(r\cos\theta, r\sin\theta)\,r\,dr$$

##### 3. 原点在 $D$ 边界上

此时 $\Delta: \alpha \leq \theta \leq \beta$，$0 \leq r \leq r(\theta)$

$$\iint_D f(x,y)\,d\sigma = \int_\alpha^\beta d\theta \int_0^{r(\theta)} f(r\cos\theta, r\sin\theta)\,r\,dr$$

**例2.7.** $\iint_D (x^2 + y^2)\,d\sigma$，$D: a^2 \leq x^2 + y^2 \leq b^2$

**解：** $I = \iint_\Delta r^2 \cdot r\,dr\,d\theta = \int_0^{2\pi} d\theta \int_a^b r^3\,dr = 2\pi \cdot \frac{b^4 - a^4}{4}$

**例2.8.** 求由 $\begin{cases} x^2 + y^2 + z^2 \leq 4a^2 \\ x^2 + y^2 \leq 2ay \end{cases}$ 确定的立体的体积

**解：** $V = 4V_1 = 4\int_0^{\frac{\pi}{2}} d\theta \int_0^{2a\sin\theta} \sqrt{4a^2 - r^2}\,r\,dr$

$= 2\int_0^{\frac{\pi}{2}} d\theta \int_0^{2a\sin\theta} \sqrt{4a^2 - r^2}\,d(r^2)$

$= \frac{4}{3}\int_0^{\frac{\pi}{2}} (4a^2 - r^2)^{\frac{3}{2}}\big|_0^{2a\sin\theta}\,d\theta$

$= \frac{4}{3}\int_0^{\frac{\pi}{2}} (2a)^3(1 - \cos^3\theta)\,d\theta$

$= \frac{32a^3}{3}(\frac{\pi}{2} - \frac{2}{3})$

**例2.9.** 将累次积分 $I = \int_0^1 dx \int_{1-x}^{\sqrt{1-x^2}} f(x^2+y^2)\,dy$ 化为极坐标系的累次积分

**解：** $D: 0 \leq x \leq 1$，$1-x \leq y \leq \sqrt{1-x^2}$

$AB: x + y = 1$，$\xrightarrow{x = r\cos\theta, y = r\sin\theta} r = \frac{1}{\sin\theta + \cos\theta}$

$\widehat{AB}: y = \sqrt{1-x^2} \Rightarrow r = 1$，$0 \leq \theta \leq \frac{\pi}{2}$

$I = \int_0^{\frac{\pi}{2}} d\theta \int_{\frac{1}{\sin\theta+\cos\theta}}^1 f(r^2)\,r\,dr$

**例2.10.** $\iint_D e^{-(x^2+y^2)}\,d\sigma$，$D: x^2 + y^2 \leq R^2$

**解：** $I = \int_0^{2\pi} d\theta \int_0^R e^{-r^2}r\,dr = \pi \int_0^R e^{-r^2}\,d(r^2)$

$= \pi(-e^{-r^2})\big|_0^R = \pi(1 - e^{-R^2})$

**应用于 Gauss 积分：** $\int_0^{+\infty} e^{-x^2}\,dx = \frac{\sqrt{\pi}}{2} = I$

$I^2 = (\lim_{R \to +\infty} \int_0^R e^{-x^2}\,dx)^2$

$= \lim_{R \to +\infty} \int_0^R e^{-x^2}\,dx \int_0^R e^{-y^2}\,dy$

$= \lim_{R \to +\infty} \iint_D e^{-(x^2+y^2)}\,dx\,dy$，$D = [0,R] \times [0,R]$

**利用圆进行放缩：**

令 $B_r = \{(x,y) | x^2 + y^2 \leq r^2, x, y \geq 0\}$

则 $B_R \subset D \subset B_{\sqrt{2}R}$

$\frac{\pi}{4}(1-e^{-R^2}) = \iint_{B_R} e^{-(x^2+y^2)}\,dx\,dy \leq \square \leq \iint_{B_{\sqrt{2}R}} e^{-(x^2+y^2)}\,dx\,dy = \frac{\pi}{4}(1-e^{-2R^2}) \to \frac{\pi}{4}$

$\therefore \lim_{R \to +\infty} \square = \frac{\pi}{4}$，$\therefore I = \frac{\sqrt{\pi}}{2}$

**例2.11.** $\iint_D x^2\,d\sigma$，$D: x^2 + y^2 = R^2$ 与 $y = -x$ 围成的右上半区域

**解：** $I = \iint_D x^2\,d\sigma = \iint_D y^2\,d\sigma$

$I = \frac{1}{2}\iint_D (x^2+y^2)\,d\sigma = \frac{1}{2}\int_{-\frac{\pi}{4}}^{\frac{3\pi}{4}} d\theta \int_0^R r^3\,dr = \frac{\pi R^4}{8}$

**EX (1)** $\iint_D (\sin^2 x + \cos^2 y)\,d\sigma$，其中 $D = [0,1] \times [0,1]$

$I = \iint_D (\sin^2 y + \cos^2 x)\,d\sigma = \frac{1}{2}\iint_D 2\,d\sigma = 1$

**(2)** $\iint_D (\frac{x^2}{a^2} + \frac{y^2}{b^2})\,d\sigma$，$a \neq b$，$D: x^2 + y^2 \leq 1$

依然关于 $x, y$ 对称

$I = \iint_D (\frac{x^2}{a^2} + \frac{y^2}{b^2})\,d\sigma = \iint_D (\frac{y^2}{a^2} + \frac{x^2}{b^2})\,d\sigma$

$= \frac{1}{2}(\frac{1}{a^2} + \frac{1}{b^2})\iint_D (x^2 + y^2)\,d\sigma$

$= \frac{1}{2}(\frac{1}{a^2} + \frac{1}{b^2})\iint_D r^3\,dr\,d\theta$

$= \frac{1}{2}(\frac{1}{a^2} + \frac{1}{b^2})\int_0^{2\pi} d\theta \int_0^1 r^3\,dr = \frac{\pi}{4}(\frac{1}{a^2} + \frac{1}{b^2})$

---

#### 6.2.4 曲线坐标下二重积分的计算（二重积分的变量代换）

**例：** $\iint_D e^{\frac{x-y}{x+y}}\,dx\,dy$，令 $u = x - y$，$v = x + y$

变换 $T: u = u(x,y)$，$v = v(x,y)$，其中 $(x,y) \in D$，$(u,v) \in \Delta$

若下列条件满足：
- (1) $u(x,y), v(x,y) \in C^1(\Delta)$（一阶偏导连续）
- (2) Jacobi 行列式 $\frac{\partial(u,v)}{\partial(x,y)} = \begin{vmatrix} u_x & u_y \\ v_x & v_y \end{vmatrix} \neq 0$，$\forall (x,y) \in D$
- (3) 变换 $T: D \to \Delta$ 是一对一的

$xOy$ 平面 $\to$ $uOv$ 平面

则称变换 $T$ 为**正则变换**

① 正则变换 $T: D \to \Delta$ 有唯一逆变换 $T^{-1}: \Delta \to D$ —— (*)

由隐函数组定理，对每个点 $P \in \Delta$，方程组

$\begin{cases} F(x,y,u,v) = u(x,y) - u = 0 \\ G(x,y,u,v) = v(x,y) - v = 0 \end{cases}$ —— (*2)

唯一确定定义在邻域 $U(P)$ 上的函数组

$\begin{cases} x = x(u,v) \\ y = y(u,v) \end{cases}$ —— (*3)

从而唯一确定局部逆变换 $T^{-1}(u,v) = (x,y)$ —— (*4)

由唯一性，对任何 $p, q \in \Delta$，在 $U(P) \cap U(Q)$ 上，(*4) 相同

$\therefore$ 它们可拼成完整逆变换 (*)

② 逆变换 $T^{-1}$ 也是正则变换

设 $T: u = f(x,y)$，$v = g(x,y)$

$T^{-1}: x = \varphi(u,v)$，$y = \psi(u,v)$

则 $u = f(\varphi(u,v), \psi(u,v))$

$v = g(\varphi(u,v), \psi(u,v))$

或写成 $T \circ T^{-1}(u,v) = (u,v)$，$T \circ T^{-1} = id_\Delta$

求导得 $\begin{pmatrix} u_x & u_y \\ v_x & v_y \end{pmatrix} \begin{pmatrix} x_u & x_v \\ y_u & y_v \end{pmatrix} = \begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix}$

于是 $\frac{\partial(u,v)}{\partial(x,y)} \cdot \frac{\partial(x,y)}{\partial(u,v)} = 1$

正则变换：$\partial \to \partial$，$\text{int} \to \text{int}$

**EX2. 证明：**

(1) 若 $f(x,y)$ 连续，则原像 $f^{-1}[a,b] = \{(x,y) | a &lt; f(x,y) &lt; b\}$ 是开集

(2) 设映射 $T: \Delta \to D$，$(u,v) \to (x,y)$ 连续

若 $V \subset D$ 是开集，则原像 $T^{-1}(V) = \{P | T(P) \in V\}$ 是开集

(3) 设 (2) 中的函数组 $x = x(u,v)$，$y = y(u,v) \in C^1(\Delta)$ 且 $\frac{\partial(x,y)}{\partial(u,v)} \neq 0$，则 $T: \Delta \to D$ 将边界变成边界，内部变成内部

**证明：** (1)(2) 依连续性、开集的定义

函数 $f$ 连续 $\iff$ $\{x | f(x) &gt; c\}$ 与 $\{x | f(x) &lt; c\}$ 是开集

(3) 当 $\frac{\partial(x,y)}{\partial(u,v)} \neq 0$ 时，由隐函数组定理，映射 $T: \Delta \to D$

存在逆映射 $T^{-1}: D \to \Delta$，它们都连续且一对一

若 $P \in \partial\Delta$ 为边界点，$q = T(P) \in D$ 是内点，则存在邻域 $U(q) \subset D$，于是 $U(P) = T^{-1}(U(q))$ 是开集，$P$ 为 $\Delta$ 的内点，矛盾

若 $P \in \Delta$ 是内点，但 $q = T(P) \in \partial D$ 是边界点，则 $P \in T^{-1}(q)$

且存在邻域 $U(P) \subset \Delta$，于是 $U(q) = T(U(P)) \subset (T^{-1})^{-1}(U(P))$ 是开集，矛盾

---

**逆变换** $T^{-1}: \Delta \to D$

$uOv$ 平面内，直线 $v = v_0$，$v = v_0 + \Delta v$，$u = u_0$，$u = u_0 + \Delta u$

$\downarrow$ $\downarrow$ $\downarrow$ $\downarrow$

$xOy$ 平面内，曲线 $r = r(u_0, v)$，$r = r(u_0, v_0 + \Delta v)$，$r = r(u, v_0)$，$r = r(u_0 + \Delta u, v)$

于是 $uOv$ 平面内矩形 $P_0P_1P_2P_3 \xrightarrow{T^{-1}}$

$xOy$ 平面内曲边四边形 $Q_0Q_1Q_2Q_3$

当 $|\Delta u|$，$|\Delta v|$ 充分小时：

$\Delta\sigma = S_{Q_0Q_1Q_2Q_3} \approx \|\vec{Q_0Q_1} \times \vec{Q_0Q_2}\|$

$\vec{Q_0Q_1} = r(u_0 + \Delta u, v_0) - r(u_0, v_0)$

$= r_u(u_0, v_0)\Delta u + o(\Delta u)$

$\vec{Q_0Q_2} = r(u_0, v_0 + \Delta v) - r(u_0, v_0)$

$= r_v(u_0, v_0)\Delta v + o(\Delta v)$

$\Delta\sigma \approx \|r_u(u_0, v_0)\Delta u \times r_v(u_0, v_0)\Delta v + o(\cdots)\|$

$\approx \|r_u(u_0, v_0) \times r_v(u_0, v_0)\| \cdot |\Delta u \cdot \Delta v|$

$d\sigma = \|r_u \times r_v\|\,du\,dv$

$r_u \times r_v = (0, 0, \frac{\partial(x,y)}{\partial(u,v)})$

$d\sigma = \left|\frac{\partial(x,y)}{\partial(u,v)}\right|du\,dv$

$$\iint_D f(x,y)\,d\sigma = \iint_\Delta f(x(u,v), y(u,v))\left|\frac{\partial(x,y)}{\partial(u,v)}\right|du\,dv$$

**例2.12.** $\iint_D (y-x)\,d\sigma$，$D: y = x+1$，$y = x-3$，$y = -\frac{1}{3}x + 5$，$y = -\frac{1}{3}x + \frac{9}{7}$ 围成

令 $u = y - x$，$v = y + \frac{1}{3}x$，则 $u \in [-3, 1]$，$v \in [\frac{7}{3}, 5]$

$\frac{\partial(u,v)}{\partial(x,y)} = \begin{vmatrix} -1 & 1 \\ \frac{1}{3} & 1 \end{vmatrix} = -\frac{4}{3}$

$\frac{\partial(x,y)}{\partial(u,v)} = \frac{1}{\frac{\partial(u,v)}{\partial(x,y)}} = -\frac{3}{4}$

$I = \iint_\Delta u \cdot \frac{3}{4}\,du\,dv = \int_{-3}^1 \frac{3}{4}u\,du \int_{\frac{7}{3}}^5 dv = -\frac{48}{3}$

**EX3.** $\iint_D e^{\frac{x-y}{x+y}}\,d\sigma$，$D: x \geq 0$，$y \geq 0$ 及 $x + y = 1$ 围成

**解：** $u = x - y$，$v = x + y$，$x = \frac{u+v}{2}$，$y = \frac{v-u}{2}$

$\begin{cases} x = 0 \Rightarrow u + v = 0 \\ y = 0 \Rightarrow v = u \end{cases}$，$\frac{\partial(u,v)}{\partial(x,y)} = \begin{vmatrix} 1 & -1 \\ 1 & 1 \end{vmatrix} = 1 + 1 = 2$

$\therefore \frac{\partial(x,y)}{\partial(u,v)} = \frac{1}{2}$

$\begin{cases} x + y = 1 \Rightarrow v = 1 \end{cases}$，$I = \frac{1}{2}\iint_\Delta e^{\frac{u}{v}}\,du\,dv = \frac{1}{2}\int_0^1 dv \int_{-v}^v e^{\frac{u}{v}}\,du = \frac{e - e^{-1}}{4}$

**例13.** $\iint_D \sqrt{xy}\,d\sigma$，$D: xy = 1$，$xy = 2$，$y = x$，$y = 4x$（$x, y &gt; 0$）

**解：** 令 $u = xy$，$v = \frac{y}{x}$

$u \in [1, 2]$，$v \in [1, 4]$

$\frac{\partial(u,v)}{\partial(x,y)} = \begin{vmatrix} y & x \\ -\frac{y}{x^2} & \frac{1}{x} \end{vmatrix} = \frac{2y}{x} = 2v$，$\frac{\partial(x,y)}{\partial(u,v)} = \frac{1}{2v}$

$I = \iint_\Delta \frac{\sqrt{u}}{2v}\,du\,dv = \int_1^4 \frac{dv}{2v} \int_1^2 \sqrt{u}\,du = \frac{(2\sqrt{2}-1)\ln 2}{3}$

---

**注：** 极坐标变换 $T: \square \to \mathbb{R}^2$

$x = r\cos\theta$，$y = r\sin\theta$，$0 \leq r &lt; +\infty$，$0 \leq \theta \leq 2\pi$

从带状区域到全平面的满射

$\frac{\partial(x,y)}{\partial(r,\theta)} = r$，变换 $T$ 在原点和正实轴上非一对一

$$\iint_D f(x,y)\,d\sigma = \iint_\Delta f(r\cos\theta, r\sin\theta)\,r\,dr\,d\theta$$

① $xOy$ 平面上圆域 $D: x^2 + y^2 \leq R^2$

对应 $r\theta$ 平面是矩形区域 $\Delta = [0,R] \times [0, 2\pi]$

在 $D$ 中剔除 (a) 小圆盘 $x^2 + y^2 &lt; \varepsilon^2$（原点邻域）

(b) 圆心角为 $\varepsilon$ 的扇环 $ABB'A'$

余下部分为 $D_\varepsilon$

此时 $T: \Delta_\varepsilon \to D_\varepsilon$ 是一对一、正则，

且在 $\Delta_\varepsilon$ 上 $\frac{\partial(x,y)}{\partial(r,\theta)} = r &gt; 0$

$$\iint_{D_\varepsilon} f(x,y)\,d\sigma = \iint_{\Delta_\varepsilon} f(r\cos\theta, r\sin\theta)\,r\,dr\,d\theta$$

由可加性 左 $= \iint_D - \iint_{D \setminus D_\varepsilon} \xrightarrow{\varepsilon \to 0^+} 0$

右 $= \iint_\Delta - \iint_{\Delta \setminus \Delta_\varepsilon} \xrightarrow{\varepsilon \to 0^+} 0$

② $D$ 为一般有界闭域，则存在充分大的 $R &gt; 0$ s.t.

$D \subset B_R = \{(x,y) | x^2 + y^2 \leq R^2\}$

$F(x,y) = \begin{cases} f(x,y) & (x,y) \in D \\ 0 & (x,y) \in B_R \setminus D \end{cases}$

它在 $B_R \setminus D$ 上的不连续点集 $E \subset \partial D$，面积为0

从而 $F$ 在 $B_R$ 可积

$$\iint_{B_R} F(x,y)\,dx\,dy = \iint_\Delta F(r\cos\theta, r\sin\theta)\,r\,dr\,d\theta$$

$= \iint_D f(x,y)\,dx\,dy = \iint_\Delta f(r\cos\theta, r\sin\theta)\,r\,dr\,d\theta$

**广义极坐标变换** $\begin{cases} x = ar\cos\theta \\ y = br\sin\theta \end{cases}$，$0 \leq r &lt; +\infty$，$0 \leq \theta \leq 2\pi$

$\frac{\partial(x,y)}{\partial(r,\theta)} = \begin{vmatrix} a\cos\theta & -ar\sin\theta \\ b\sin\theta & br\cos\theta \end{vmatrix} = abr$

$$\iint_D f(x,y)\,dx\,dy = \iint_\Delta f(ar\cos\theta, br\sin\theta)\,abr\,dr\,d\theta$$

**例2.14.** $\iint_D x^2\,d\sigma$，$D: \frac{x^2}{4} + \frac{y^2}{9} \leq 1$

**解：** $x = 2r\cos\theta$，$y = 3r\sin\theta$，$r \geq 0$，$0 \leq \theta \leq 2\pi$

$I = \iint_\Delta (2r\cos\theta)^2 \cdot 6r\,dr\,d\theta = \int_0^{2\pi} \cos^2\theta\,d\theta \int_0^1 24r^3\,dr = 6\pi$

**EX4.** 求椭球 $\frac{x^2}{a^2} + \frac{y^2}{b^2} + \frac{z^2}{c^2} \leq 1$ 的体积

**解：** 令 $x = ar\cos\theta$，$y = br\sin\theta$，$r^2 + \frac{z^2}{c^2} = 1$，$z^2 = c^2(1-r^2)$

第一卦限 $V_1 = c\iint_D \sqrt{1-r^2}\,d\sigma = c\int_0^{\frac{\pi}{2}} d\theta \int_0^1 \sqrt{1-r^2} \cdot abr\,dr = \frac{\pi}{6}abc$

$V = 8V_1 = \frac{4}{3}\pi abc$

---

### §6.3 三重积分

#### 6.3.1 三重积分化为累次积分

##### 1. 截面法（先二后一）

已知平行截面质量求立体质量

$\sum_{i=1}^n f(P_i)\Delta V_i = \sum_{j=1}^n \left(\sum_{k=1}^m f(\xi_k, \eta_k, \zeta_j)\Delta\sigma_k\right)\Delta z_j$

对每个 $z \in [p,q]$ 有截面 $D(z)$，则

$$\iiint_V f(x,y,z)\,dV = \int_p^q dz \iint_{D(z)} f(x,y,z)\,dx\,dy$$ —— 先二后一

特别地，当 $f(x,y,z) = z$ 时，

则 $\iiint_V z\,dV = \int_p^q z \cdot A(z)\,dz$

其中 $A(z)$ 是截面 $D(z)$ 的面积

##### 2. 投影法（先一后二）

将 $V$ 投影至 $xOy$ 平面得区域 $D$

特征：$(x,y) \in D$，$z_1(x,y) \leq z \leq z_2(x,y)$

因此 $\sum_{i=1}^n f(P_i)\Delta V_i = \sum_{j=1}^n \left(\sum_{k=1}^m f(\xi_k, \eta_k, \zeta_j)\Delta\sigma_k\right)$

$$\iiint_V f(x,y,z)\,dV = \iint_D dx\,dy \int_{z_1(x,y)}^{z_2(x,y)} f(x,y,z)\,dz$$ —— 先一后二

**例3.1.** $\iiint_V xyz\,dV$，其中 $V$ 由平面 $x = 0$，$y = 0$，$z = 0$，$x + y + z = 1$ 围成

**解：** 截面法，对 $z \in [0,1]$ 截面 $D(z): x + y \leq 1-z$，$x, y \geq 0$

于是 $I = \int_0^1 z\,dz \iint_{D(z)} xy\,dx\,dy$

$= \int_0^1 z\,dz \int_0^{1-z} y\,dy \int_0^{1-z-y} x\,dx$

$= \int_0^1 x\,dx \int_0^{1-x} y\,dy \int_0^{1-x-y} z\,dz$

$= \int_0^1 x\,dx \int_0^{1-x} y \cdot \frac{1}{2}(1-x-y)^2\,dy$（$1-x-y = u$）

$= \frac{1}{2}\int_0^1 x\,dx \int_0^{1-x} [(1-x)u^2 - u^3]\,du$

$= \frac{1}{2}\int_0^1 \frac{1}{12}x(1-x)^4\,dx \xrightarrow{1-x=v} \frac{1}{24}\int_0^1 (1-v)v^4\,dv = \frac{1}{720}$

**例3.2.** $\iiint_V z\,dV$，其中 $V: x^2 + y^2 + z^2 \leq R^2$，$z \geq 0$

**解：** 将 $V$ 投影到 $xOy$ 平面得区域 $D: x^2 + y^2 \leq R^2$

$I = \iint_D dx\,dy \int_0^{\sqrt{R^2-x^2-y^2}} z\,dz$

$= \iint_D \frac{1}{2}(R^2 - x^2 - y^2)\,dx\,dy$

$= \frac{1}{2}\int_0^{2\pi} d\theta \int_0^R (R^2 - r^2)r\,dr = \frac{1}{4}\pi R^4$

**例3.3.** $\iiint_V z^2\,dV$，其中 $V: \frac{x^2}{a^2} + \frac{y^2}{b^2} + \frac{z^2}{c^2} \leq 1$

**解：** 截面法，对每个 $z \in [-c, c]$ 截面 $D(z): \frac{x^2}{a^2} + \frac{y^2}{b^2} \leq 1 - \frac{z^2}{c^2}$

$A(z) = \pi ab(1 - \frac{z^2}{c^2})$

$I = \int_{-c}^c z^2 \cdot A(z)\,dz = \int_{-c}^c z^2 \pi ab(1 - \frac{z^2}{c^2})\,dz = \frac{4}{15}\pi abc^3$

**例3.4.** $\iiint_V (x+y+z)\,dV$，$x^2 + y^2 \leq z \leq \sqrt{2 - x^2 - y^2}$

**解：** 两个曲面交线在 $xOy$ 平面投影为圆周 $x^2 + y^2 = 1$

因为 $V$ 关于 $yOz$ 平面对称，$f = x$ 是奇函数

所以 $\iiint_V x\,dV = 0$，类似地 $\iiint_V y\,dV = 0$

于是 $I = \iiint_V z\,dV = \iint_D dx\,dy \int_{x^2+y^2}^{\sqrt{2-x^2-y^2}} z\,dz$

$= \iint_D \frac{1}{2}[2 - x^2 - y^2 - (x^2+y^2)^2]\,dx\,dy$

$= \frac{1}{2}\int_0^{2\pi} d\theta \int_0^1 [2 - r^2 - r^4]r\,dr = \frac{7\pi}{12}$

---

#### 6.3.2 柱坐标与球坐标下三重积分的计算

##### 1. 三重积分的变量代换

取正则变换 $u = u(x,y,z)$，$v = v(x,y,z)$，$w = w(x,y,z)$

$(x,y,z) \in V$，$(u,v,w) \in V'$

在此变换下，点 $P_0(x_0, y_0, z_0)$ 可用三个曲面的交点确定：

$u(x,y,z) = u(x_0,我继续整理第六章和第七章的内容。

---


(\tau_i)}\right)\Delta t_i$

$&lt; \sum_{i=1}^n \sqrt{[\dot{x}(\xi_i) - \dot{x}(\tau_i)]^2 + [\dot{y}(\eta_i) - \dot{y}(\tau_i)]^2 + [\dot{z}(\zeta_i) - \dot{z}(\tau_i)]^2}\,\Delta t_i$

下证 $\lim_{d \to 0} \sigma = 0$

因为 $\dot{r}(t)$ 在 $[\alpha, \beta]$ 上连续，且 $\dot{r}(t) \neq 0$，所以存在常数 $m &gt; 0$，

s.t. $\sqrt{\dot{x}^2(\xi_i) + \dot{y}^2(\eta_i) + \dot{z}^2(\zeta_i)} \geq m$，$\|P_{i-1}P_i\| \geq m\,\Delta t_i$

令 $\Delta t = \max_{1 \leq i \leq n} \Delta t_i$，则 $\Delta t \leq \frac{d}{m}$，当 $d \to 0$ 时，$\Delta t \to 0$

依条件 $\dot{x}(t), \dot{y}(t), \dot{z}(t)$ 在 $[\alpha, \beta]$ 连续 $\to$ 一致连续

对任何 $\varepsilon &gt; 0$，存在 $\delta &gt; 0$，当 $t, t' \in [\alpha, \beta]$ 且 $|t - t'| &lt; \delta$ 时，

$|\dot{x}(t) - \dot{x}(t')| &lt; \varepsilon$，$|\dot{y}(t) - \dot{y}(t')| &lt; \varepsilon$，$|\dot{z}(t) - \dot{z}(t')| &lt; \varepsilon$

当 $d &lt; m\delta$ 时，$\Delta t &lt; \frac{d}{m} &lt; \delta$，此时 $\xi_i, \eta_i, \zeta_i, \tau_i \in [t_{i-1}, t_i]$

所以 $0 \leq \sum_{i=1}^n \sqrt{3\varepsilon^2}\,\Delta t_i = \sqrt{3}\varepsilon \cdot \sum_{i=1}^n \Delta t_i = \sqrt{3}\varepsilon(\beta - \alpha)$

因此 $\lim_{d \to 0} S_n = \lim_{d \to 0} \sum_{i=1}^n \|\dot{r}(\tau_i)\|\Delta t_i + 0$

$= \lim_{d \to 0} \sum_{i=1}^n \|\dot{r}(\tau_i)\|\Delta t_i$ （$\Delta t &lt; \frac{d}{m}$）

$= \int_\alpha^\beta \|\dot{r}(t)\|\,dt$

##### 2. 弧微分与自然参数

因为 $dS = \sqrt{(dx)^2 + (dy)^2 + (dz)^2}$，所以用弧长 $S$ 为参数时

$\frac{dr}{dS} = \left(\frac{dx}{dS}, \frac{dy}{dS}, \frac{dz}{dS}\right)$ 是单位切向量，设它与 $x$ 轴、$y$ 轴、$z$ 轴正方向的夹角分别为 $\alpha, \beta, \gamma$，则 $\frac{dr}{dS} = (\cos\alpha, \cos\beta, \cos\gamma)$

$dx = \cos\alpha\,dS$，$dy = \cos\beta\,dS$，$dz = \cos\gamma\,dS$

**例6.5.** 求螺旋线 $\begin{cases} x = a\cos\theta \\ y = a\sin\theta \\ z = k\theta \end{cases}$ 一个螺距 $(0 \leq \theta \leq 2\pi)$ 之间的长度

**解：** $\dot{r}(\theta) = (-a\sin\theta, a\cos\theta, k)$

$\|\dot{r}(\theta)\| = \sqrt{a^2 + k^2}$

弧长 $S = \int_0^{2\pi} \|\dot{r}(\theta)\|\,d\theta = 2\pi\sqrt{a^2 + k^2}$

---

#### §5.6.3 曲面的切平面和法线

##### 1. 曲面的参数方程

球面的参数方程：设 $P(x,y,z)$ 为球面上一点，在 $xOy$ 平面投影为 $M(x,y,0)$

$P(\|OP\|\sin\varphi\cos\theta, \|OP\|\sin\varphi\sin\theta, \|OP\|\cos\varphi)$，$\varphi \in [0, \pi]$，$\theta \in [0, 2\pi]$

$\varphi$ 取长度是纬线，$\theta$ 取长度是经线

一般地，曲面 $S$ 的参数方程 $r(u,v) = (x(u,v), y(u,v), z(u,v))$，$(u,v) \in \Delta$

$r: \Delta \subset \mathbb{R}^2 \to \mathbb{R}^3$

##### 2. 曲面上的曲线表示

在曲面 $S$ 上，考虑两类曲线：
- ① $v = v_0$，$u$ 变化，称为 $u$ 曲线
- ② $u = u_0$，$v$ 变化，称为 $v$ 曲线

它们的切向量分别为 $r_u = (x_u, y_u, z_u)$，$r_v = (x_v, y_v, z_v)$

##### 3. 曲面的切平面与法线

设曲面 $S$ 的参数方程为 $r(u,v) = (x(u,v), y(u,v), z(u,v))$，$(u,v) \in \Delta$

其中 $r(u,v)$ 在 $\Delta$ 上连续，在点 $(u_0, v_0) \in \Delta$ 可微

过曲面 $S$ 上任一点 $r_0 = r(u_0, v_0)$ 的任一光滑曲线 $\Gamma$ 的参数方程为

$r = r(u(t), v(t))$，$t \in I$

其中 $u(t_0) = u_0$，$v(t_0) = v_0$，在上式中对 $t$ 求导得

$\dot{r}(t_0) = r_u(u_0, v_0)\dot{u}(t_0) + r_v(u_0, v_0)\dot{v}(t_0)$

右端是切向量 $r_u(u_0, v_0)$ 与 $r_v(u_0, v_0)$ 的线性组合

若 $r_u \times r_v\big|_{(u_0, v_0)} \neq 0$，说明 $r_u(u_0, v_0)$ 与 $r_v(u_0, v_0)$ 不共线

从而可张成一个平面 $\Pi$，称之为曲面 $S$ 在正则点 $r_0$ 的切平面

它的法向量为 $r_u \times r_v = \begin{vmatrix} i & j & k \\ x_u & y_u & z_u \\ x_v & y_v & z_v \end{vmatrix} = (A, B, C)$

其中 $A = \frac{\partial(y,z)}{\partial(u,v)}\big|_{(u_0,v_0)}$，$B = \frac{\partial(z,x)}{\partial(u,v)}\big|_{(u_0,v_0)}$，$C = \frac{\partial(x,y)}{\partial(u,v)}\big|_{(u_0,v_0)}$

切平面方程为 $A(x-x_0) + B(y-y_0) + C(z-z_0) = 0$

法线方程 $\frac{x-x_0}{A} = \frac{y-y_0}{B} = \frac{z-z_0}{C}$

若 $r_u = (x_u, y_u, z_u)$，$r_v = (x_v, y_v, z_v)$ 在 $\Delta$ 内连续，称曲面光滑

对曲面 $F(x,y,z) = 0$，设在点 $P_0(x_0, y_0, z_0)$ 处满足隐函数定理的条件

$F_z(P_0) \neq 0$，隐函数 $z = z(x,y)$，曲面 $S: r(x,y) = (x, y, z(x,y))$

$r_x = (1, 0, z_x) = (1, 0, -\frac{F_x}{F_z})$，$r_y = (0, 1, z_y) = (0, 1, -\frac{F_y}{F_z})$

$r_x \times r_y = \begin{vmatrix} i & j & k \\ 1 & 0 & -\frac{F_x}{F_z} \\ 0 & 1 & -\frac{F_y}{F_z} \end{vmatrix} = (\frac{F_x}{F_z}, \frac{F_y}{F_z}, 1)$

法向量 $\vec{n} = (F_x, F_y, F_z) = \nabla F$

切平面方程 $F_x(P_0)(x-x_0) + F_y(P_0)(y-y_0) + F_z(P_0)(z-z_0) = 0$

法线方程 $\frac{x-x_0}{F_x(P_0)} = \frac{y-y_0}{F_y(P_0)} = \frac{z-z_0}{F_z(P_0)}$

**例6.8.** 求曲面 $\begin{cases} x = u\cos v \\ y = u\sin v \\ z = av \end{cases}$ 在 $u = \sqrt{2}$，$v = \frac{\pi}{4}$ 处的切平面与法线方程

**解：** $r = (u\cos v, u\sin v, av)$

$r_u = (\cos v, \sin v, 0)$，$u = \sqrt{2}$，$r_u = (\frac{1}{\sqrt{2}}, \frac{1}{\sqrt{2}}, 0)$

$r_v = (-u\sin v, u\cos v, a)$，$v = \frac{\pi}{4}$，$r_v = (-1, 1, a)$

$\therefore r_u \times r_v\big|_{(\sqrt{2}, \frac{\pi}{4})} = \begin{vmatrix} i & j & k \\ \frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} & 0 \\ -1 & 1 & a \end{vmatrix} = (\frac{a}{\sqrt{2}}, -\frac{a}{\sqrt{2}}, \sqrt{2}) \parallel (a, -a, 2)$

取法向量 $n = (a, -a, 2)$

切平面 $a(x-1) - a(y-1) + 2(z - \frac{\pi}{4}a) = 0$

$ax - ay + 2z = \frac{\pi}{2}a$

**例1.** 环面的参数方程：考虑 $yOz$ 平面上的圆周 $C: (y-a)^2 + z^2 = b^2$

其中 $a &gt; b &gt; 0$，该曲面绕 $z$ 轴一周形成环面，求曲面方程

**解：** ① 圆周 $C$ 的参数方程为 $y = a + b\cos u$，$z = b\sin u$，$u \in [0, 2\pi]$

$\begin{cases} z = b\sin u \\ x = r\cos v = (a + b\cos u)\cos v \\ y = r\sin v = (a + b\cos u)\sin v \end{cases}$

$0 \leq u \leq 2\pi$，$0 \leq v \leq 2\pi$

**例2.** Möbius 带：矩形一组对边反向粘合

细杆的两种旋转的合成，绕自身中心和绕 $z$ 轴

开始时细杆 $L$ 占据 $\{(a, 0, u) | |u| \leq \varepsilon\}$

细杆中点 $C$ 绕 $xOy$ 面上的圆周 $x^2 + y^2 = a^2$ 逆时针

细杆绕 $C$ 自转 s.t. $L$ 与 $z$ 轴所在平面与 $xOy$ 面垂直

且当 $C$ 转 $\theta$ 角时，细杆绕 $C$ 转 $\frac{\theta}{2}$ 角

细杆端点 $z = u\cos\frac{\theta}{2}$，$|u| \leq \varepsilon$

$x = OP'\cos\theta = (a + u\sin\frac{\theta}{2})\cos\theta$，$\theta \in [0, 2\pi]$

$y = OP'\sin\theta = (a + u\sin\frac{\theta}{2})\sin\theta$

---

### §5.7 空间曲线的曲率与挠率

#### §5.7.1 Frenet 标架

设曲线 $\Gamma$ 的自然参数方程为 $r = r(s)$

一般参数方程为 $r = r(t)$

求导：$\frac{dr}{ds} = r'$，$\frac{d^2r}{ds^2} = r''$

$\frac{dr}{dt} = \dot{r}$，$\frac{d^2r}{dt^2} = \ddot{r}$

##### 1. 切线与法平面

与曲线 $\Gamma: r = r(s)$ 正向一致的单位切向量 $T(s) = r'(s)$

$dr = (dx, dy, dz)$，$ds^2 = dx^2 + dy^2 + dz^2 = \|dr\|^2$

$\therefore \|dr/ds\| = 1$，$\therefore r'(s)$ 是单位切向量

于是 $\Gamma$ 在点 $r(s_0)$ 的切线方程为 $\rho - r(s_0) = \lambda r'(s_0)$

法平面方程 $[\rho - r(s_0)] \cdot r'(s_0) = 0$

##### 2. 主法线和次法线

因为 $\|r'(s)\| = 1 = \langle r'(s), r'(s) \rangle$

对 $s$ 求导 $0 = r''(s) \cdot r'(s) + r'(s) \cdot r''(s) = 2r'(s) \cdot r''(s)$

$\Rightarrow r'(s) \perp r''(s)$，$r'(s) \times r''(s) \neq 0$

称 $N(s_0) = \frac{r''(s_0)}{\|r''(s_0)\|}$ 为曲线 $\Gamma$ 在 $r(s_0)$ 的主法向量

对应法线为主法线

记 $B(s_0) = T(s_0) \times N(s_0)$

为曲线 $\Gamma$ 在 $r(s_0)$ 的次法向量

$B(s)$ 与 $N(s)$ 张成法平面

##### 3. 密切平面

过曲线 $\Gamma$ 上的点 $r(s_0)$ 的平面有无穷多个，欲知 which 与 $\Gamma$ 最贴近

当 $\Gamma$ 为空间曲线时，考虑 $r(s_0)$ 邻近点 $r(s_0 + \Delta s)$

到上述平面的距离，设 $\Sigma$ 是过 $r(s_0)$ 的平面，其单位法向量 $n$，则 $r(s_0 + \Delta s)$ 到平面 $\Sigma$ 的距离

$d = \lambda = n \cdot (r(s_0 + \Delta s) - r(s_0))$

由 Taylor 公式

$r(s_0 + \Delta s) - r(s_0) = r'(s_0)\Delta s + \frac{1}{2}r''(s_0)(\Delta s)^2 + \vec{o}((\Delta s)^2)$

于是 $d = n \cdot r'(s_0)\Delta s + n \cdot r''(s_0)\frac{(\Delta s)^2}{2} + \vec{o}((\Delta s)^2)$

① 若 $\Sigma$ 不含切线，则 $\vec{n} \cdot \vec{r}'(s_0) \neq 0$

---

## 第六章 多元函数积分学及其应用

### §6.1 多元积分的概念与性质

#### 6.1.1 物体质量

设物体 $\Omega$ 的密度函数 $f$ 连续，求质量 $M$

**分割：** 将 $\Omega$ 分成 $n$ 个小部分 $\Omega_i$（$1 \leq i \leq n$），得分割 $T = \{\Omega_i\}$

**近似：** 任取 $P_i \in \Omega_i$，因 $f$ 在 $\Omega$ 连续，故当 $\Omega_i$ 的度量 $\Delta\Omega_i$ 充分小时，$f(P) \approx f(P_i)$，$P \in \Omega_i$

$\Omega_i$ 的质量 $M_i \approx f(P_i)\Delta\Omega_i$

**求和：** $M = \sum_{i=1}^n M_i \approx \sum_{i=1}^n f(P_i)\Delta\Omega_i$

**取极限：** 当 $\|T\| = \max_{1 \leq i \leq n} \Omega_i$ 的直径 $\to 0$ 时

$M = \lim_{\|T\| \to 0} \sum_{i=1}^n f(P_i)\Delta\Omega_i$

#### 6.1.2 多元积分的概念

若有界的几何体 $\Omega$ 可求长、面积、体积...则称它可度量

**定理A.** 有界几何体 $\Omega$ 可度量 $\iff \Omega$ 的边界的度量为 $0$

| 几何体 | 边界 |
|--------|------|
| 线段 $[a,b]$ | $\{a, b\}$ |
| 平面图形 | 曲线 |
| 立体 | 曲面 |

**定义1.1.** 设 $\Omega$ 是有界几何体，它是可度量的，再设 $f$ 是 $\Omega$ 上的有界函数，任取 $\Omega$ 的分割 $T = \{\Omega_i\}$ 以及介点集 $\{P_i\}$，当分割细度 $\|T\| = \max_{1 \leq i \leq n} \Omega_i$ 的直径 $\to 0$

若极限 $\lim_{\|T\| \to 0} \sum_{i=1}^n f(P_i)\Delta\Omega_i = J$ 存在，则称 $f$ 在 $\Omega$ 上可积，称 $J$ 为 $f$ 在 $\Omega$ 上的积分，记为 $J = \int_\Omega f(P)\,d\Omega$

$= \lim_{\|T\| \to 0} \sum_{i=1}^n f(P_i)\Delta\Omega_i$

(1) 若 $\Omega = [a,b]$ 且 $f$ 是 $\Omega$ 上的一元函数，则 $\int_\Omega f(P)\,d\Omega = \int_a^b f(x)\,dx$

(2) 若 $\Omega$ 是平面有界闭域 $D$ 且 $f$ 是 $D$ 上的二元函数，则 $\int_\Omega f(P)\,d\Omega = \iint_D f(x,y)\,d\sigma$ —— 二重积分

(3) 若 $\Omega$ 是 $\mathbb{R}^3$ 中有界闭域（立体）且 $f$ 是 $\Omega$ 上的三元函数

则 $\int_\Omega f(P)\,d\Omega = \iiint_\Omega f(x,y,z)\,dV$ —— 三重积分

(4) 若 $\Omega$ 是有界可求长曲线 $\Gamma$ 且 $f$ 是 $\Gamma$ 上的二元函数 or 三元函数

$\int_\Omega f(P)\,d\Omega = \int_\Gamma f(x,y)\,ds$ 或 $\int_\Gamma f(x,y,z)\,ds$ —— 曲线积分

(5) 若 $\Omega$ 是曲面 $S$ 且 $f$ 是 $S$ 上的三元函数，则

$\int_\Omega f(P)\,d\Omega = \iint_S f(x,y,z)\,dS$ —— 曲面积分

#### 6.1.3 积分的存在条件和性质

**1. 存在条件：** 设 $\Omega$ 是有界闭域且可度量，则下列函数在 $\Omega$ 上可积：
(1) 连续函数
(2) 不连续点集度量为 $0$

**2. 线性性质：** $\int_\Omega [k_1 f(P) + k_2 g(P)]\,d\Omega = k_1\int_\Omega f(P)\,d\Omega + k_2\int_\Omega g(P)\,d\Omega$

**3. 区域可加性：** 设 $\Omega = \Omega_1 \cup \Omega_2$，且 $\Omega_1, \Omega_2$ 内部不相交，则

$\int_\Omega f(P)\,d\Omega = \int_{\Omega_1} f(P)\,d\Omega + \int_{\Omega_2} f(P)\,d\Omega$

**4. 积分不等式：**
(1) $f(P) \leq g(P)$，$P \in \Omega$ $\Rightarrow$ $\int_\Omega f(P)\,d\Omega \leq \int_\Omega g(P)\,d\Omega$

(2) $\left|\int_\Omega f(P)\,d\Omega\right| \leq \int_\Omega |f(P)|\,d\Omega$

(3) 若 $m \leq f(P) \leq M$，$\forall P \in \Omega$ 则 $m\Delta\Omega \leq \int_\Omega f(P)\,d\Omega \leq M\Delta\Omega$

其中 $\Delta\Omega$ 为 $\Omega$ 的度量

**5. 中值定理：** 设 $\Omega$ 为可度量的有界闭域，$f$ 在 $\Omega$ 连续，则存在 $\xi \in \Omega$ 使 $\int_\Omega f(P)\,d\Omega = f(\xi)\Delta\Omega$

**$\Omega$ 可度量：** 以平面情形为例，平面图形的面积

设 $P$ 是平面有界图形，存在矩形 $R \supset P$，用平行于坐标轴的直线网将 $P$ 分割，此时 $T$ 的网眼——小闭矩形可分为三类：

① $\Delta_i$ 中全为 $P$ 内点
② $\Delta_i$ 中全为 $P$ 外点
③ $\Delta_i$ 中含 $P$ 的边界点

**定义：** 内和 $s_P(T)$ = ①类小矩形面积之和，若不存在则定义 $s_P(T) = 0$

外和 $S_P(T)$ = ①②类小矩形面积之和

则内和 $s_P(T) \leq$ 矩形 $R$ 的面积

外和 $S_P(T) \geq s_P(T)$

**定义：** 内面积 $\underline{I}_P = \sup_T s_P(T)$

外面积 $\bar{I}_P = \inf_T S_P(T)$

若 $\underline{I}_P = \bar{I}_P = I_P$，则称 $P$ 可求面积，称 $I_P$ 为 $P$ 的面积

**定理 $a_1$：** 平面有界图形 $P$ 可求面积 $\iff$ 对任何 $\varepsilon &gt; 0$，存在直线网 $T$ 使 $S_P(T) - s_P(T) &lt; \varepsilon$

**推论1：** 平面有界图形 $P$ 的面积为 $0$ $\iff$ $\bar{I}_P = 0$，即 $\forall \varepsilon &gt; 0$，存在直线网 $T$，使 $S_P(T) &lt; \varepsilon$，或存在有限个小矩形覆盖 $P$，面积之和 $&lt; \varepsilon$

**定理 $a_2$：** 平面有界图形 $P$ 可求面积 $\iff$ $P$ 的边界面积为 $0$

**定理 $a_3$：** 若曲线 $K$ 是 $[a,b]$ 上的连续函数 $f(x)$ 图像，则 $K$ 的面积为 $0$

**推论2：** 参数方程 $x = \varphi(t)$，$y = \psi(t)$，$\alpha \leq t \leq \beta$，所表示的光滑曲线（$\varphi'(t), \psi'(t)$ 连续且 $\varphi'^2(t) + \psi'^2(t) \neq 0$）

$K$ 的面积为 $0$

**推论3：** 平面上分段光滑曲线围成的有界闭区域可求面积

**注：** 存在不可求面积的平面有界图形，例如

$P = \{(x,y) | x, y \in [0,1] \cap \mathbb{Q}\}$

事实上，对任何直线网 $T$，因 $P$ 中点都为边界点，故含 $P$ 中点的小矩形均为③类，从而 $s_P(T) = 0$，$\underline{I}_P = 0$

下面证明③类小矩形全体覆盖正方形 $[0,1] \times [0,1]$

于是外面积是 $1$

记③类小矩形的并集为 $E$，因为小矩形是闭集，所以 $E$ 是闭集，对任何 $Q(x_0, y_0) \in D$，由实数可用有理数列逼近，存在 $[0,1]$ 中的有理数列 $\{x_n\}$，$\{y_n\}$ 使

$x_n \to x_0$，$y_n \to y_0$，从而 $(x_n, y_n) \in P$ 且 $(x_n, y_n) \to (x_0, y_0)$

说明 $Q$ 是 $P$ 的聚点，又因 $P \subset E$，所以 $Q$ 是 $E$ 的聚点，又 $E$ 是闭集，$\therefore Q \in E$，$D \subset E$ $\square$

---

### §6.2 二重积分的计算

#### 6.2.1 二重积分的几何意义

任取 $D$ 的分割 $T = \{\sigma_i\}$ 以及 $\{\xi_i, \eta_i\} \in \sigma_i$

则 $\sigma_i$ 对应的曲顶柱体体积 $\Delta V_i \approx f(\xi_i, \eta_i)\Delta\sigma_i$

于是 $\iint_D f(x,y)\,d\sigma = \lim_{\|T\| \to 0} \sum_{i=1}^n f(\xi_i, \eta_i)\Delta\sigma_i$

#### 6.2.2 直角坐标系下二重积分的计算

##### 一、矩形区域 $D = [a,b] \times [c,d]$

曲顶柱体与平面 $x = x_0$ 的截面是曲边梯形，面积 $A(x_0) = \int_c^d f(x_0, y)\,dy$

曲顶柱体体积 $V = \int_a^b A(x)\,dx = \int_a^b dx \int_c^d f(x,y)\,dy$

**定理A：** 设 $f(x,y)$ 在 $D = [a,b] \times [c,d]$ 上可积，且对每个 $x \in [a,b]$ 积分 $\int_c^d f(x,y)\,dy$ 存在，则累次积分 $\int_a^b dx \int_c^d f(x,y)\,dy$ 存在且 $\iint_D f(x,y)\,d\sigma = It$

**分析：** 令 $F(x) = \int_c^d f(x,y)\,dy$，去证 $\iint_D f(x,y)\,d\sigma = \int_a^b F(x)\,dx$

右边 $= \lim_{\Delta x \to 0} \sum_{i=1}^n F(\xi_i)\Delta x_i$ （$\Delta x = \max_{1 \leq i \leq n} \Delta x_i$）

$= \lim_{\Delta x \to 0} \sum_{i=1}^n \int_c^d f(\xi_i, y)\,dy \cdot \Delta x_i$

$= \lim_{\Delta x \to 0} \sum_{i=1}^n \sum_{k=1}^m \int_{y_{k-1}}^{y_k} f(\xi_i, y)\,dy \cdot \Delta x_i$

其中 $m_{ik} \leq f(\xi_i, y) \leq M_{ik}$

$m_{ik} = \inf_{\Delta_{ik}} f$，$M_{ik} = \sup_{\Delta_{ik}} f$

$\Delta_{ik} = [x_{i-1}, x_i] \times [y_{k-1}, y_k]$

$T = \{\Delta_{ik}\}$，$\|T\| = \max_{i,k} \Delta_{ik}$

$\sum_{i=1}^n \sum_{k=1}^m m_{ik}\Delta x_i \Delta y_k \leq I \leq \sum_{i=1}^n \sum_{k=1}^m M_{ik}\Delta x_i \Delta y_k$

当 $\|T\| \to 0$ 时，左、右 $\to \iint_D f(x,y)\,d\sigma$

**定理B：** 若 $f(x,y)$ 在 $D = [a,b] \times [c,d]$ 上连续，则

$$\iint_D f(x,y)\,d\sigma = \int_a^b dx \int_c^d f(x,y)\,dy = \int_c^d dy \int_a^b f(x,y)\,dx$$

##### 二、一般区域

通常可分为

**X型区域：** 对任何 $x_0 \in (a,b)$，直线 $x = x_0$ 与区域边界的交点最多有 $2$ 个，则此时区域可表示为

$D: a \leq x \leq b$，$y_1(x) \leq y \leq y_2(x)$

**Y型区域：** 对任何 $y_0 \in (c,d)$，直线 $y = y_0$ 与区域边界的交点至多 $2$ 个，此时 $D: c \leq y \leq d$，$x_1(y) \leq x \leq x_2(y)$

**定理C：** 若 $f(x,y)$ 在X型区域 $D$ 上连续，其中 $D: a \leq x \leq b$，$y_1(x) \leq y \leq y_2(x)$，$y_1(x), y_2(x)$ 在 $[a,b]$ 连续，则

$$\iint_D f(x,y)\,d\sigma = \int_a^b dx \int_{y_1(x)}^{y_2(x)} f(x,y)\,dy$$

**证明：** 存在矩形 $R = [a,b] \times [c,d] \supset D$

令 $F(x,y) = \begin{cases} f(x,y) & (x,y) \in D \\ 0 & (x,y) \in R \setminus D \end{cases}$

因 $D$ 可求面积，故 $\partial D$ 面积为 $0$

因 $f$ 在 $D$ 上连续，故 $F$ 的不连续点集 $K \subset \partial D$

从而 $K$ 的面积是 $0$，因此 $F$ 在 $R$ 上可积，于是

$\iint_D f(x,y)\,d\sigma = \iint_R F(x,y)\,d\sigma = \int_a^b dx \int_c^d F(x,y)\,dy = \int_a^b dx \int_{y_1(x)}^{y_2(x)} f(x,y)\,dy$

##### 三、例题

**例2.1.** $\iint_D (1 - \frac{x}{3} - \frac{y}{4})\,d\sigma$，其中 $D = [-1,1] \times [-2,2]$

**解：** $I = \int_{-1}^1 dx \int_{-2}^2 (1 - \frac{x}{3} - \frac{y}{4})\,dy = \int_{-1}^1 4(1 - \frac{x}{3})\,dx = 8$

**例2.2.** $\iint_D (x^4 + y^3)\,d\sigma$，$D$ 由 $y = 0$，$x = 1$ 及 $y = x^2$ 围成

**解：** $I = \int_0^1 dx \int_0^{x^2} (x^4 + y^3)\,dy = \int_0^1 (x^6 + \frac{1}{4}x^8)\,dx = \frac{1}{7} + \frac{1}{36} = \frac{26}{105}$

**例2.3.** $\iint_D xy\,d\sigma$，其中 $D$ 由 $y^2 = x$ 与 $y = x - 2$ 围成

看作Y形区域，不分区

$I = \int_{-1}^2 dy \int_{y^2}^{y+2} xy\,dx = \int_{-1}^2 y \cdot \frac{1}{2}[(2+y)^2 - y^4]\,dy = \frac{45}{8}$

**例2.4.** $\iint_D \frac{\sin x}{x}\,d\sigma$，其中 $D$ 由 $y = x$ 和 $y = x^2$ 围成

$I = \int_0^1 dx \int_{x^2}^x \frac{\sin x}{x}\,dy = \int_0^1 \frac{\sin x}{x}(x - x^2)\,dx = \int_0^1 (1-x)\sin x\,dx$

---

#### 分部积分的列表算法

$\int uv^{(n+1)}\,dx = \int u\,dv^{(n)} = uv^{(n)} - \int u'v^{(n)}\,dx$ ①

$= uv^{(n)} - \int u'\,dv^{(n-1)}$

$= uv^{(n)} - u'v^{(n-1)} + \int u''v^{(n-1)}\,dx$ ②

$= uv^{(n)} - u'v^{(n-1)} + u''v^{(n-2)} - \int u'''v^{(n-2)}\,dx$ ③

$= \cdots$

$= uv^{(n)} - u'v^{(n-1)} + \cdots + (-1)^n u^{(n)}v + (-1)^{n+1}\int u^{(n+1)}v\,dx$

| 求导 | $u$ | $u'$ | $u''$ | $u'''$ | $\cdots$ | $u^{(n)}$ | $u^{(n+1)}$ |
|------|-----|------|-------|--------|----------|-----------|-------------|
| 积分 | $v^{(n+1)}$ | $v^{(n)}$ | $v^{(n-1)}$ | $v^{(n-2)}$ | $\cdots$ | $v'$ | $v$ |

e.g. $\int x^4 e^x\,dx$：

| $x^4$ | $4x^3$ | $12x^2$ | $24x$ | $24$ | $0$ |
|-------|--------|---------|-------|------|-----|
| $e^x$ | $e^x$ | $e^x$ | $e^x$ | $e^x$ | $e^x$ |

原式 $= e^x(x^4 - 4x^3 + 12x^2 - 24x + 24) + C$

e.g. $\int x^3 \sin x\,dx$：

| $x^3$ | $3x^2$ | $6x$ | $6$ | $0$ |
|-------|--------|------|-----|-----|
| $\sin x$ | $-\cos x$ | $-\sin x$ | $\cos x$ | $\sin x$ |

$= \cos x(-x^3 + 6x) + \sin x(3x^2 - 6) + C$

e.g. $\int_0^1 (1-x)\sin x\,dx = [(1-x)(-\cos x) - \sin x]_0^1 = 1 - \sin 1$

| $1-x$ | $-1$ | $0$ |
|-------|------|-----|
| $\sin x$ | $-\cos x$ | $-\sin x$ |

**例：** $\iint_D (xy^3 + xy^3 e^{x^2+y^2})\,d\sigma$，其中 $D$ 由 $y = x$，$y = -1$，$x = 1$ 围成

**解：** 令 $D_1 = \triangle OAB$，$D_2 = \triangle OBC$

$xy^3$ 在 $D_1$ 上关于 $x$ 是奇函数

$D_2$ 上关于 $y$ 是奇函数

$\iint_D xy^3 e^{x^2+y^2}\,d\sigma = \iint_{D_1} + \iint_{D_2} = 0$

而由对称性 $\iint_D xy^3\,d\sigma = 2\iint_{D_2} xy^3\,d\sigma = 4\iint_{OECA} xy^3\,d\sigma = 2\iint_{OECA} xy^3\,d\sigma$

$= 2\int_0^1 x\,dx \times \int_0^1 y^3\,dy = \frac{2}{9}$

**例2.** 交换累次积分的顺序

$I = \int_{-2}^0 dy \int_0^{\frac{2+y}{2}} f(x,y)\,dx + \int_0^2 dy \int_0^{\frac{2-y}{2}} f(x,y)\,dx$

**解：** $I = \iint_D f(x,y)\,d\sigma = \int_0^1 dx \int_{2x-2}^{2-2x} f(x,y)\,dy$

---

#### 6.2.3 极坐标系下二重积分的计算

$$\iint_D f(x,y)\,d\sigma = \lim_{\|T\| \to 0} \sum_{i=1}^n f(\xi_i, \eta_i)\Delta\sigma_i$$

使用 $r = \text{const}$，$\theta = \text{const}$ 分割区域 $D$

$\Delta\sigma = \frac{1}{2}[(r+\Delta r)^2\Delta\theta - r^2\Delta\theta]$

$= r\Delta r\Delta\theta + \frac{1}{2}(\Delta r)^2\Delta\theta$

当 $|\Delta r|, |\Delta\theta|$ 充分小，$\Delta\sigma \approx r\Delta r\Delta\theta$

于是 $\lim_{\|T\| \to 0} \sum_{i=1}^n f(\xi_i, \eta_i)\Delta\sigma_i = \lim_{\|T\| \to 0} \sum_{i=1}^n f(\xi_i, \eta_i)r_i\Delta r_i\Delta\theta_i$

$$\iint_D f(x,y)\,d\sigma = \iint_\Delta f(r\cos\theta, r\sin\theta)\,r\,dr\,d\theta$$

其中 $\Delta$ 是 $D$ 在 $r$-$\theta$ 平面内的对应区域

##### 1. 原点在 $D$ 外

**$\theta$ 形区域：** $\theta = \theta_0$ 与 $D$ 的边界交点至多 $2$ 个，

此时 $\Delta: \alpha \leq \theta \leq \beta$，$r_1(\theta) \leq r \leq r_2(\theta)$

$$\iint_D f(x,y)\,d\sigma = \int_\alpha^\beta d\theta \int_{r_1(\theta)}^{r_2(\theta)} f(r\sin\theta, r\cos\theta)\,r\,dr$$

**$r$ 形区域：** $r = r_0$ 与 $D$ 的边界交点至多 $2$ 个，

此时 $\Delta: r_1 \leq r \leq r_2$，$\theta_1(r) \leq \theta \leq \theta_2(r)$

$$\iint_D f(x,y)\,d\sigma = \int_{r_1}^{r_2} dr \int_{\theta_1(r)}^{\theta_2(r)} f(r\cos\theta, r\sin\theta)\,r\,d\theta$$

##### 2. 原点在 $D$ 内

此时 $\Delta: 0 \leq \theta \leq 2\pi$，$0 \leq r \leq r(\theta)$

其中 $r = r(\theta)$ 是 $D$ 的边界曲线的极坐标方程

$$\iint_D f(x,y)\,d\sigma = \int_0^{2\pi} d\theta \int_0^{r(\theta)} f(r\cos\theta, r\sin\theta)\,r\,dr$$

##### 3. 原点在 $D$ 边界上

此时 $\Delta: \alpha \leq \theta \leq \beta$，$0 \leq r \leq r(\theta)$

$$\iint_D f(x,y)\,d\sigma = \int_\alpha^\beta d\theta \int_0^{r(\theta)} f(r\cos\theta, r\sin\theta)\,r\,dr$$

**例2.7.** $\iint_D (x^2 + y^2)\,d\sigma$，$D: a^2 \leq x^2 + y^2 \leq b^2$

**解：** $I = \iint_\Delta r^2 \cdot r\,dr\,d\theta = \int_0^{2\pi} d\theta \int_a^b r^3\,dr = 2\pi \cdot \frac{b^4 - a^4}{4}$

**例2.8.** 求由 $\begin{cases} x^2 + y^2 + z^2 \leq 4a^2 \\ x^2 + y^2 \leq 2ay \end{cases}$ 确定的立体的体积

**解：** $V = 4V_1 = 4\int_0^{\frac{\pi}{2}} d\theta \int_0^{2a\sin\theta} \sqrt{4a^2 - r^2}\,r\,dr$

$= 2\int_0^{\frac{\pi}{2}} d\theta \int_0^{2a\sin\theta} \sqrt{4a^2 - r^2}\,d(r^2)$

$= \frac{4}{3}\int_0^{\frac{\pi}{2}} (4a^2 - r^2)^{\frac{3}{2}}\Big|_0^{2a\sin\theta}\,d\theta$

$= \frac{4}{3}\int_0^{\frac{\pi}{2}} (2a)^3(1 - \cos^3\theta)\,d\theta$

$= \frac{32a^3}{3}(\frac{\pi}{2} - \frac{2}{3})$

**例2.9.** 将累次积分 $I = \int_0^1 dx \int_{1-x}^{\sqrt{1-x^2}} f(x^2+y^2)\,dy$ 化为极坐标系的累次积分

**解：** $D: 0 \leq x \leq 1$，$1-x \leq y \leq \sqrt{1-x^2}$

$AB: x + y = 1$，$\xrightarrow{x = r\cos\theta, y = r\sin\theta} r = \frac{1}{\sin\theta + \cos\theta}$

$\widehat{AB}: y = \sqrt{1-x^2} \Rightarrow r = 1$，$0 \leq \theta \leq \frac{\pi}{2}$

$I = \int_0^{\frac{\pi}{2}} d\theta \int_{\frac{1}{\sin\theta+\cos\theta}}^1 f(r^2)\,r\,dr$

**例2.10.** $\iint_D e^{-(x^2+y^2)}\,d\sigma$，$D: x^2 + y^2 \leq R^2$

**解：** $I = \int_0^{2\pi} d\theta \int_0^R e^{-r^2}r\,dr = \pi \int_0^R e^{-r^2}\,d(r^2)$

$= \pi(-e^{-r^2})\Big|_0^R = \pi(1 - e^{-R^2})$

**应用于 Gauss 积分：** $\int_0^{+\infty} e^{-x^2}\,dx = \frac{\sqrt{\pi}}{2} = I$

$I^2 = (\lim_{R \to +\infty} \int_0^R e^{-x^2}\,dx)^2$

$= \lim_{R \to +\infty} \int_0^R e^{-x^2}\,dx \int_0^R e^{-y^2}\,dy$

$= \lim_{R \to +\infty} \iint_D e^{-(x^2+y^2)}\,dx\,dy$，$D = [0,R] \times [0,R]$

利用圆进行放缩：

令 $B_r = \{(x,y) | x^2 + y^2 \leq r^2, x, y \geq 0\}$

则 $B_R \subset D \subset B_{\sqrt{2}R}$

$\frac{\pi}{4}(1-e^{-R^2}) = \iint_{B_R} e^{-(x^2+y^2)}\,dx\,dy \leq I \leq \iint_{B_{\sqrt{2}R}} e^{-(x^2+y^2)}\,dx\,dy = \frac{\pi}{4}(1-e^{-2R^2}) \to \frac{\pi}{4}$

$\therefore \lim_{R \to +\infty} I = \frac{\pi}{4}$，$\therefore I = \frac{\sqrt{\pi}}{2}$

**例2.11.** $\iint_D x^2\,d\sigma$，$D: x^2 + y^2 = R^2$ 与 $y = -x$ 围成的右上半区域

**解：** $I = \iint_D x^2\,d\sigma = \iint_D y^2\,d\sigma$

$I = \frac{1}{2}\iint_D (x^2+y^2)\,d\sigma = \frac{1}{2}\int_{-\frac{\pi}{4}}^{\frac{3\pi}{4}} d\theta \int_0^R r^3\,dr = \frac{\pi R^4}{8}$

**EX (1)** $\iint_D (\sin^2 x + \cos^2 y)\,d\sigma$，其中 $D = [0,1] \times [0,1]$

$I = \iint_D (\sin^2 y + \cos^2 x)\,d\sigma = \frac{1}{2}\iint_D 2\,d\sigma = 1$

**(2)** $\iint_D (\frac{x^2}{a^2} + \frac{y^2}{b^2})\,d\sigma$，$a \neq b$，$D: x^2 + y^2 \leq 1$

依然关于 $x, y$ 对称

$I = \iint_D (\frac{x^2}{a^2} + \frac{y^2}{b^2})\,d\sigma = \iint_D (\frac{y^2}{a^2} + \frac{x^2}{b^2})\,d\sigma$

$= \frac{1}{2}(\frac{1}{a^2} + \frac{1}{b^2})\iint_D (x^2+y^2)\,d\sigma$

$= \frac{1}{2}(\frac{1}{a^2} + \frac{1}{b^2})\iint_D r^3\,dr\,d\theta$

$= \frac{1}{2}(\frac{1}{a^2} + \frac{1}{b^2})\int_0^{2\pi} d\theta \int_0^1 r^3\,dr = \frac{\pi}{4}(\frac{1}{a^2} + \frac{1}{b^2})$

---

#### 6.2.4 曲线坐标下二重积分的计算

（二重积分的变量代换）

**例：** $\iint_D e^{\frac{x-y}{x+y}}\,dx\,dy$，令 $u = x - y$，$v = x + y$

变换 $T: u = u(x,y)$，$v = v(x,y)$，其中 $(x,y) \in D$，$(u,v) \in \Delta$

若下列条件满足：
(1) $u(x,y), v(x,y) \in C^1(\Delta)$（一阶偏导连续）
(2) Jacobi 行列式 $\frac{\partial(u,v)}{\partial(x,y)} = \begin{vmatrix} u_x & u_y \\ v_x & v_y \end{vmatrix} \neq 0$，$\forall (x,y) \in D$
(3) 变换 $T: D \to \Delta$ 是一对一的

$xOy$ 平面 $\to$ $uOv$ 平面

则称变换 $T$ 为**正则变换**

① 正则变换 $T: D \to \Delta$ 有唯一逆变换 $T^{-1}: \Delta \to D$ —— (*)

由隐函数组定理，对每个点 $P \in \Delta$，方程组

$\begin{cases} F(x,y,u,v) = u(x,y) - u = 0 \\ G(x,y,u,v) = v(x,y) - v = 0 \end{cases}$ —— (*2)

唯一确定定义在邻域 $U(P)$ 上的函数组

$\begin{cases} x = x(u,v) \\ y = y(u,v) \end{cases}$ —— (*3)

从而唯一确定局部逆变换 $T^{-1}(u,v) = (x,y)$ —— (*4)

由唯一性，对任何 $p, q \in \Delta$，在 $U(P) \cap U(Q)$ 上，(*4) 相同

$\therefore$ 它们可拼成完整逆变换 (*)

② 逆变换 $T^{-1}$ 也是正则变换

设 $T: u = f(x,y)$，$v = g(x,y)$

$T^{-1}: x = \varphi(u,v)$，$y = \psi(u,v)$

则 $u = f(\varphi(u,v), \psi(u,v))$

$v = g(\varphi(u,v), \psi(u,v))$

或写成 $T \circ T^{-1}(u,v) = (u,v)$，$T \circ T^{-1} = id_\Delta$

求导得 $\begin{pmatrix} u_x & u_y \\ v_x & v_y \end{pmatrix} \begin{pmatrix} x_u & x_v \\ y_u & y_v \end{pmatrix} = \begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix}$

于是 $\frac{\partial(u,v)}{\partial(x,y)} \cdot \frac{\partial(x,y)}{\partial(u,v)} = 1$

正则变换：$\partial \to \partial$，$int \to int$

**例2.** 证明：
(1) 若 $f(x,y)$ 连续，则原像 $f^{-1}[a,b] = \{(x,y) | a &lt; f(x,y) &lt; b\}$ 是开集
(2) 设映射 $T: \Delta \to D$，$(u,v) \to (x,y)$ 连续

若 $V \subset D$ 是开集，则原像 $T^{-1}(V) = \{P | T(P) \in V\}$ 是开集

(3) 设(2)中的函数组 $x = x(u,v)$，$y = y(u,v) \in C^1(\Delta)$ 且 $\frac{\partial(x,y)}{\partial(u,v)} \neq 0$，则 $T: \Delta \to D$ 将边界变成边界，内部变成内部

**证明：** (1)(2) 依连续性，开集的定义

函数 $f$ 连续 $\iff$ $\{x | f(x) &gt; c\}$ 与 $\{x | f(x) &lt; c\}$ 是开集

(3) 当 $\frac{\partial(x,y)}{\partial(u,v)} \neq 0$ 时，由隐函数组定理，映射 $T: \Delta \to D$

存在逆映射 $T^{-1}: D \to \Delta$，它们都连续且一对一

若 $P \in \partial\Delta$ 为边界点，$q = T(P) \in D$ 是内点，则存在邻域 $U(q) \subset D$，于是 $U(P) = T^{-1}(U(q))$ 是开集，$P$ 为 $\Delta$ 的内点，矛盾

若 $P \in \Delta$ 是内点，但 $q = T(P) \in \partial D$ 是边界点，则 $P = T^{-1}(q)$

且存在邻域 $U(P) \subset \Delta$，于是 $U(q) = T(U(P)) = (T^{-1})^{-1}(U(P))$ 是开集，矛盾

---

**逆变换 $T^{-1}: \Delta \to D$**

$uOv$ 平面内，直线 $v = v_0$，$v = v_0 + \Delta v$，$u = u_0$，$u = u_0 + \Delta u$

$\downarrow$ $\downarrow$ $\downarrow$ $\downarrow$

$xOy$ 平面内，曲线 $r = r(u_0, v)$，$r = r(u_0, v_0 + \Delta v)$，$r = r(u, v_0)$，$r = r(u_0 + \Delta u, v)$

于是 $uOv$ 平面内矩形 $P_0P_1P_2P_3$ $\xrightarrow{T^{-1}}$

$xOy$ 平面内曲边四边形 $Q_0Q_1Q_2Q_3$

当 $|\Delta u|, |\Delta v|$ 充分小时：

$\Delta\sigma = S_{Q_0Q_1Q_2Q_3} \approx \|\vec{Q_0Q_1} \times \vec{Q_0Q_2}\|$

$\vec{Q_0Q_1} = r(u_0 + \Delta u, v_0) - r(u_0, v_0)$

$= r_u(u_0, v_0)\Delta u + o(\Delta u)$

$\vec{Q_0Q_2} = r(u_0, v_0 + \Delta v) - r(u_0, v_0)$

$= r_v(u_0, v_0)\Delta v + o(\Delta v)$

$\Delta\sigma \approx \|r_u(u_0, v_0)\Delta u \times r_v(u_0, v_0)\Delta v + o(\cdots)\|$

$\approx \|r_u(u_0, v_0) \times r_v(u_0, v_0)\| \cdot |\Delta u \cdot \Delta v|$

$d\sigma = \|r_u \times r_v\|\,du\,dv$

$r_u \times r_v = (0, 0, \frac{\partial(x,y)}{\partial(u,v)})$

$d\sigma = \left|\frac{\partial(x,y)}{\partial(u,v)}\right|du\,dv$

$$\iint_D f(x,y)\,d\sigma = \iint_\Delta f(x(u,v), y(u,v))\left|\frac{\partial(x,y)}{\partial(u,v)}\right|du\,dv$$

**例2.12.** $\iint_D (y-x)\,d\sigma$，$D: y = x+1$，$y = x-3$，$y = -\frac{1}{3}x + 5$，$y = -\frac{1}{3}x + \frac{9}{7}$ 围成

令 $u = y - x$，$v = y + \frac{1}{3}x$，则 $u \in [-3, 1]$，$v \in [\frac{7}{3}, 5]$

$\frac{\partial(u,v)}{\partial(x,y)} = \begin{vmatrix} -1 & 1 \\ \frac{1}{3} & 1 \end{vmatrix} = -\frac{4}{3}$

$\frac{\partial(x,y)}{\partial(u,v)} = \frac{1}{\frac{\partial(u,v)}{\partial(x,y)}} = -\frac{3}{4}$

$I = \iint_\Delta u \cdot \frac{3}{4}\,du\,dv = \int_{-3}^1 \frac{3}{4}u\,du \int_{\frac{7}{3}}^5 dv = -\frac{88}{3}$

**例3.** $\iint_D e^{\frac{x-y}{x+y}}\,d\sigma$，$D: x \geq 0$，$y \geq 0$ 及 $x + y = 1$ 围成

**解：** $u = x - y$，$v = x + y$，$x = \frac{u+v}{2}$，$y = \frac{v-u}{2}$

$\begin{cases} x = 0 \Rightarrow u + v = 0 \\ y = 0 \Rightarrow v = u \\ x + y = 1 \Rightarrow v = 1 \end{cases}$

$\frac{\partial(u,v)}{\partial(x,y)} = \begin{vmatrix} 1 & -1 \\ 1 & 1 \end{vmatrix} = 1 + 1 = 2$

$\therefore \frac{\partial(x,y)}{\partial(u,v)} = \frac{1}{2}$

$I = \frac{1}{2}\iint_\Delta e^{\frac{u}{v}}\,du\,dv = \frac{1}{2}\int_0^1 dv \int_{-v}^v e^{\frac{u}{v}}\,du = \frac{e - e^{-1}}{4}$

**例13.** $\iint_D \sqrt{xy}\,d\sigma$，$D: xy = 1$，$xy = 2$，$y = x$，$y = 4x$（$x, y &gt; 0$）

**解：** 令 $u = xy$，$v = \frac{y}{x}$

$u \in [1, 2]$，$v \in [1, 4]$

$\frac{\partial(u,v)}{\partial(x,y)} = \begin{vmatrix} y & x \\ -\frac{y}{x^2} & \frac{1}{x} \end{vmatrix} = \frac{y}{x} + \frac{y}{x} = 2v$，$\frac{\partial(x,y)}{\partial(u,v)} = \frac{1}{2v}$

$I = \iint_\Delta \frac{\sqrt{u}}{2v}\,du\,dv = \int_1^4 \frac{dv}{2v} \int_1^2 \sqrt{u}\,du = \frac{(2\sqrt{2}-1)\ln 2}{3}$

---

**注：** 极坐标变换 $T: \square \to \mathbb{R}^2$

$x = r\cos\theta$，$y = r\sin\theta$，$0 \leq r &lt; +\infty$，$0 \leq \theta \leq 2\pi$

从带状区域到全平面的满射

$\frac{\partial(x,y)}{\partial(r,\theta)} = r$，变换 $T$ 在原点和正实轴上非一对一

$$\iint_D f(x,y)\,d\sigma = \iint_\Delta f(r\cos\theta, r\sin\theta)\,r\,dr\,d\theta$$

① $xOy$ 平面上圆域 $D: x^2 + y^2 \leq R^2$

对应 $r\theta$ 平面是矩形区域 $\Delta = [0,R] \times [0, 2\pi]$

在 $D$ 中剔除 (a) 小圆盘 $x^2 + y^2 &lt; \varepsilon^2$（原点邻域）

(b) 圆心角为 $\varepsilon$ 的扇环 $ABB'A'$

余下部分为 $D_\varepsilon$

此时 $T: \Delta_\varepsilon \to D_\varepsilon$ 是一对一、正则，

且在 $\Delta_\varepsilon$ 上 $\frac{\partial(x,y)}{\partial(r,\theta)} = r &gt; 0$

$$\iint_{D_\varepsilon} f(x,y)\,d\sigma = \iint_{\Delta_\varepsilon} f(r\cos\theta, r\sin\theta)\,r\,dr\,d\theta$$

由可加性 左 $= \iint_D - \iint_{D \setminus D_\varepsilon} \xrightarrow{\varepsilon \to 0^+} 0$

右 $= \iint_\Delta - \iint_{\Delta \setminus \Delta_\varepsilon} \xrightarrow{\varepsilon \to 0^+} 0$

② $D$ 为一般有界闭域，则存在充分大的 $R &gt; 0$ s.t.

$D \subset B_R = \{(x,y) | x^2 + y^2 \leq R^2\}$

$F(x,y) = \begin{cases} f(x,y) & (x,y) \in D \\ 0 & (x,y) \in B_R \setminus D \end{cases}$

它在 $B_R \setminus D$ 上的不连续点集 $E \subset \partial D$，面积为 $0$

从而 $F$ 在 $B_R$ 可积

$$\iint_{B_R} F(x,y)\,dx\,dy = \iint_\Delta F(r\cos\theta, r\sin\theta)\,r\,dr\,d\theta = \iint_D f(x,y)\,dx\,dy = \iint_\Delta f(r\cos\theta, r\sin\theta)\,r\,dr\,d\theta$$

**广义极坐标变换：** $\begin{cases} x = ar\cos\theta \\ y = br\sin\theta \end{cases}$，$0 \leq r &lt; +\infty$，$0 \leq \theta \leq 2\pi$

$\frac{\partial(x,y)}{\partial(r,\theta)} = \begin{vmatrix} a\cos\theta & -ar\sin\theta \\ b\sin\theta & br\cos\theta \end{vmatrix} = abr$

$$\iint_D f(x,y)\,dx\,dy = \iint_\Delta f(ar\cos\theta, br\sin\theta)\,ab\,r\,dr\,d\theta$$

**例2.14.** $\iint_D x^2\,d\sigma$，$D: \frac{x^2}{4} + \frac{y^2}{9} \leq 1$

**解：** $x = 2r\cos\theta$，$y = 3r\sin\theta$，$r \geq 0$，$0 \leq \theta \leq 2\pi$

$I = \iint_\Delta (2r\cos\theta)^2 \cdot 6r\,dr\,d\theta = \int_0^{2\pi} \cos^2\theta\,d\theta \int_0^1 24r^3\,dr = 6\pi$

**例4.** 求椭球 $\frac{x^2}{a^2} + \frac{y^2}{b^2} + \frac{z^2}{c^2} \leq 1$ 的体积

**解：** 令 $x = ar\cos\theta$，$y = br\sin\theta$，$r^2 + \frac{z^2}{c^2} = 1$，$z^2 = c^2(1-r^2)$

第一卦限 $V_1 = c\iint_D \sqrt{1-r^2}\,d\sigma = c\int_0^{\frac{\pi}{2}} d\theta \int_0^1 \sqrt{1-r^2}\,ab\,r\,dr = \frac{\pi}{6}abc$

$V = 8V_1 = \frac{4}{3}\pi abc$

---

### §6.3 三重积分

#### 6.3.1 三重积分化为累次积分

##### 1. 截面法

已知平行截面质量求立体质量

$\sum_{i=1}^n f(P_i)\Delta V_i = \sum_{j=1}^n \left(\sum_{k=1}^n f(\xi_k, \eta_k, \zeta_j)\Delta\sigma_k\right)\Delta z_j$

对每个 $z \in [p,q]$ 有截面 $D(z)$，则

$$\iiint_V f(x,y,z)\,dV = \int_p^q dz \iint_{D(z)} f(x,y,z)\,dx\,dy$$ —— 先二后一

特别地，当 $f(x,y,z) = z$ 时，

则 $\iiint_V z\,dV = \int_p^q z \cdot A(z)\,dz$

其中 $A(z)$ 是截面 $D(z)$ 的面积

##### 2. 投影法

将 $V$ 投影至 $xOy$ 平面得区域 $D$

特征：$(x,y) \in D$，$z_1(x,y) \leq z \leq z_2(x,y)$

因此 $\sum_{i=1}^n f(P_i)\Delta V_i = \sum_{j=1}^n \left(\sum_{k=1}^n f(\xi_k, \eta_k, \zeta_j)\Delta\sigma_k\right)$

$$\iiint_V f(x,y,z)\,dV = \iint_D dx\,dy \int_{z_1(x,y)}^{z_2(x,y)} f(x,y,z)\,dz$$ —— 先一后二

**例3.1.** $\iiint_V xyz\,dV$，其中 $V$ 由平面 $x = 0$，$y = 0$，$z = 0$，$x + y + z = 1$ 围成

**解：** 截面法，对 $z \in [0,1)$ 截面 $D(z): x + y \leq 1 - z$，$x, y \geq 0$

于是 $I = \int_0^1 z\,dz \iint_{D(z)} xy\,dx\,dy$

$= \int_0^1 z\,dz \int_0^{1-z} y\,dy \int_0^{1-z-y} x\,dx$

$= \int_0^1 x\,dx \int_0^{1-x} y\,dy \int_0^{1-x-y} z\,dz$

$= \int_0^1 x\,dx \int_0^{1-x} y \cdot \frac{1}{2}(1-x-y)^2\,dy$ （$1-x-y = u$）

$= \frac{1}{2}\int_0^1 x\,dx \int_0^{1-x} [(1-x)u^2 - u^3]\,du$

$= \frac{1}{2}\int_0^1 \frac{1}{12}x(1-x)^4\,dx \xrightarrow{1-x=v} \frac{1}{24}\int_0^1 (1-v)v^4\,dv = \frac{1}{720}$

**例3.2.** $\iiint_V z\,dV$，其中 $V: x^2 + y^2 + z^2 \leq R^2$，$z \geq 0$

**解：** 将 $V$ 投影到 $xOy$ 平面得区域 $D: x^2 + y^2 \leq R^2$

$I = \iint_D dx\,dy \int_0^{\sqrt{R^2-x^2-y^2}} z\,dz$

$= \iint_D \frac{1}{2}(R^2 - x^2 - y^2)\,dx\,dy$

$= \frac{1}{2}\int_0^{2\pi} d\theta \int_0^R (R^2 - r^2)r\,dr = \frac{1}{4}\pi R^4$

**例3.3.** $\iiint_V z^2\,dV$，其中 $V: \frac{x^2}{a^2} + \frac{y^2}{b^2} + \frac{z^2}{c^2} \leq 1$

**解：** 截面法，对每个 $z \in [-c, c]$ 截面 $D(z): \frac{x^2}{a^2} + \frac{y^2}{b^2} \leq 1 - \frac{z^2}{c^2}$

$A(z) = \pi ab(1 - \frac{z^2}{c^2})$

$I = \int_{-c}^c z^2 \cdot A(z)\,dz = \int_{-c}^c z^2 \pi ab(1 - \frac{z^2}{c^2})\,dz = \frac{4}{15}\pi abc^3$

**例3.4.** $\iiint_V (x + y + z)\,dV$，$x^2 + y^2 \leq z \leq \sqrt{2 - x^2 - y^2}$

**解：** 两个曲面交线在 $xOy$ 平面投影为圆周 $x^2 + y^2 = 1$

因为 $V$ 关于 $yOz$ 平面对称，$f = x$ 是奇函数

所以 $\iiint_V x\,dV = 0$，类似地 $\iiint_V y\,dV = 0$

于是 $I = \iiint_V z\,dV = \iint_D dx\,dy \int_{x^2+y^2}^{\sqrt{2-x^2-y^2}} z\,dz$

$= \iint_D \frac{1}{2}[2 - x^2 - y^2 - (x^2+y^2)^2]\,dx\,dy$

$= \frac{1}{2}\int_0^{2\pi} d\theta \int_0^1 [2 - r^2 - r^4]r\,dr = \frac{7\pi}{12}$

---

#### 6.3.2 柱坐标与球坐标下三重积分的计算

##### 1. 三重积分的变量代换

取正则变换 $u = u(x,y,z)$，$v = v(x,y,z)$，$w = w(x,y,z)$

$(x,y,z) \in V$，$(u,v,w) \in V'$

在此变换下，点 $P_0(x_0, y_0, z_0)$ 可用三个曲面的交点确定：

$u(x,y,z) = u(x_0, y_0, z_0) = u_0$

$v(x,y,z) = v(x_0, y_0, z_0) = v_0$

$w(x,y,z) = w(x_0, y_0, z_0) = w_0$

我来继续整理剩余的笔记内容。

---

 y_0, z_0) = u_0$

$v(x,y,z) = v(x_0, y_0, z_0) = v_0$

$w(x,y,z) = w(x_0, y_0, z_0) = w_0$

在 $Ouvw$ 空间中，用曲面族 $u = c_1$，$v = c_2$，$w = c_3$ 分割 $V'$

体积微元 $dV' = du\,dv\,dw$

$T = (x(u,v,w), y(u,v,w), z(u,v,w))$

$T_u = (x_u, y_u, z_u)$，$T_v = (x_v, y_v, z_v)$，$T_w = (x_w, y_w, z_w)$

$\frac{\partial(x,y,z)}{\partial(u,v,w)} = \begin{vmatrix} x_u & x_v & x_w \\ \cdots & \cdots & \cdots \end{vmatrix}$

$$\iiint_V f(x,y,z)\,dV = \iiint_{V'} f(x(u,v,w), \cdots)\left|\frac{\partial(x,y,z)}{\partial(u,v,w)}\right|du\,dv\,dw$$

##### 2. 柱坐标

$V \xrightarrow{\text{投影}} D \xrightarrow{\text{极坐标}} \Delta$

取变换 $x = r\cos\theta$，$y = r\sin\theta$，$z = z$

$0 \leq r &lt; +\infty$，$0 \leq \theta \leq 2\pi$，$-\infty &lt; z &lt; +\infty$

$\frac{\partial(x,y,z)}{\partial(r,\theta,z)} = \begin{vmatrix} \cos\theta & -r\sin\theta & 0 \\ \sin\theta & r\cos\theta & 0 \\ 0 & 0 & 1 \end{vmatrix} = r$

当 $r &gt; 0$，$0 \leq \theta &lt; 2\pi$ 时，为正则变换

因此 $\iiint_V f(x,y,z)\,dV = \iiint_\Delta f(r\cos\theta, r\sin\theta, z)\,r\,dr\,d\theta\,dz$

**例3.5.** $\iiint_V z\,dV$，其中 $V$ 是由曲面 $z = x^2 + y^2$，圆柱面 $x^2 + y^2 = 2y$ 与平面 $z = 0$ 所围成区域（抛物面之外，$xOy$ 之上）

**解：** $I = \iint_D dx\,dy \int_0^{x^2+y^2} z\,dz$

$= \iint_D \frac{1}{2}(x^2+y^2)^2\,dx\,dy$

$= \int_0^\pi d\theta \int_0^{2\sin\theta} \frac{1}{2}r^4 \cdot r\,dr$

$= \frac{1}{2}\int_0^\pi d\theta \cdot (2\sin\