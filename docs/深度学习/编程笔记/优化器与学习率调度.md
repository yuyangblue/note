# PyTorch优化器与学习率调度实战

> 本篇侧重PyTorch代码实践，优化器的数学原理详见 [06_模型训练.md](./06_模型训练.md)

---

## torch.optim模块

PyTorch提供了统一的优化器工具包 `torch.optim`，封装了各种优化算法的实现细节。我们不需要手写梯度更新的数学公式，只需要调用API。

```python
import torch.optim as optim

# 常见优化器
optimizer = optim.Adam(model.parameters(), lr=0.001)
optimizer = optim.SGD(model.parameters(), lr=0.01, momentum=0.9)
optimizer = optim.RMSprop(model.parameters(), lr=0.01)
```

---

## model.parameters()是什么

训练模型时，我们常说"优化模型参数"，具体优化的是哪些参数呢？

### CNN模型的可学习参数

```
卷积层:   卷积核权重 (weight)     shape: [out_channels, in_channels, kH, kW]
          偏置 (bias)             shape: [out_channels]

BatchNorm: γ (gamma/缩放因子)      shape: [out_channels]
          β (beta/平移因子)       shape: [out_channels]

全连接层: 权重矩阵                 shape: [out_features, in_features]
          偏置                     shape: [out_features]
```

`model.parameters()` 会自动遍历模型所有可学习参数，生成一个迭代器供优化器使用：

```python
# 查看模型有多少参数
total_params = sum(p.numel() for p in model.parameters())
trainable_params = sum(p.numel() for p in model.parameters() if p.requires_grad)

print(f"总参数: {total_params:,}")
print(f"可训练参数: {trainable_params:,}")
```

### 手动指定要优化的参数

有时我们只想优化部分参数：

```python
# 只优化分类器，不优化特征提取器
optimizer = optim.Adam([
    {'params': model.features.parameters(), 'lr': 1e-4},  # 特征层低学习率
    {'params': model.classifier.parameters(), 'lr': 1e-3}  # 分类层正常学习率
])
```

---

## 优化器的"三板斧"

Adam优化器是一个**对象**，它有状态、有方法。日常训练中我们主要用三个方法：

### optimizer.zero_grad() — 清空梯度

```python
optimizer.zero_grad()
```

**为什么需要清空？**

PyTorch默认会**累积**梯度。如果不清空，每次backward会把新梯度加到旧梯度上：

```
第一次: grad = 1.0
第二次: grad = 1.0 + 2.0 = 3.0  # 错误累积！

正确做法:
第一次: zero_grad() → grad = 1.0
第二次: zero_grad() → grad = 2.0
```

### optimizer.step() — 更新参数

```python
optimizer.step()
```

这一步根据梯度调整参数。以Adam为例：

$$\theta_{t+1} = \theta_t - \frac{\alpha}{\sqrt{\hat{v}_t} + \epsilon} \cdot \hat{m}_t$$

（数学原理见06号笔记）

### optimizer.state_dict() — 保存/恢复状态

```python
# 保存
checkpoint = {
    'model': model.state_dict(),
    'optimizer': optimizer.state_dict(),
    'epoch': epoch
}
torch.save(checkpoint, 'checkpoint.pth')

# 恢复
model.load_state_dict(checkpoint['model'])
optimizer.load_state_dict(checkpoint['optimizer'])
```

**Adam的状态里有什么？**

```python
print(optimizer.state_dict())
# {'state': {0: {'exp_avg': tensor, 'exp_avg_sq': tensor, ...}, ...},
#  'param_groups': [{'lr': 0.001, 'params': [...], ...}]}
```

- `exp_avg`: 梯度的一阶动量（类似速度）
- `exp_avg_sq`: 梯度的二阶动量（历史梯度平方）

这些状态帮助Adam实现自适应学习率！

---

## 反向传播流程

参数更新的完整流程：

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  前向传播   │ →   │  损失函数   │ →   │  backward   │
│ forward()  │     │  loss = ... │     │ 计算梯度    │
└─────────────┘     └─────────────┘     └─────────────┘
                                               │
                                               ↓
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ 参数已更新  │ ←   │ step()      │ ←   │   梯度就绪  │
│ 模型更聪明  │     │ 更新参数    │     │ grad ≠ 0    │
└─────────────┘     └─────────────┘     └─────────────┘
```

代码层面：

```python
# 1. 前向传播
outputs = model(images)
# 2. 计算损失
loss = criterion(outputs, labels)
# 3. 清空旧梯度（重要！）
optimizer.zero_grad()
# 4. 反向传播，计算梯度
loss.backward()
# 5. 更新参数
optimizer.step()
```

---

## 学习率调度器

### 为什么需要调整学习率

训练过程不同阶段需要不同的学习率：

| 阶段 | 学习率 | 原因 |
|-----|-------|------|
| 初期 | 较大 | 快速接近最优区域 |
| 中期 | 适中 | 精确定位 |
| 后期 | 较小 | 精细调整，避免震荡 |

### StepLR — 固定步长衰减

```python
scheduler = optim.lr_scheduler.StepLR(
    optimizer,
    step_size=3,    # 每3个epoch
    gamma=0.7       # 学习率乘0.7
)

# 训练循环
for epoch in range(num_epochs):
    train()
    scheduler.step()  # epoch结束后调用
```

学习率变化：

```
Epoch 1-3:   lr = 0.01
Epoch 4-6:   lr = 0.007
Epoch 7-9:   lr = 0.0049
Epoch 10-12: lr = 0.00343
```

### ReduceLROnPlateau — 自适应衰减

当指标停止改善时自动调整学习率：

```python
scheduler = optim.lr_scheduler.ReduceLROnPlateau(
    optimizer,
    mode='min',        # 'min': 损失变小; 'max': 准确率变大
    factor=0.5,        # 学习率乘0.5（减半）
    patience=5,        # 连续5次没改善才调整
    verbose=True       # 打印信息
)

# 训练循环
for epoch in range(num_epochs):
    train_loss = train()
    scheduler.step(train_loss)  # 传入监控的指标
```

**典型配置场景：**

| 场景 | factor | patience |
|-----|--------|----------|
| 快速收敛 | 0.5 | 2-3 |
| 稳定训练 | 0.1 | 5-10 |
| 微调预训练模型 | 0.5 | 3 |

### CosineAnnealingLR — 余弦退火

学习率按余弦曲线周期变化：

```python
scheduler = optim.lr_scheduler.CosineAnnealingLR(
    optimizer,
    T_max=10,      # 周期长度
    eta_min=1e-6   # 最小学习率
)
```

```
lr
0.01 ┤        ╭╮
     │       ╱  ╲
     │      ╱    ╲
     │     ╱      ╲
     │    ╱        ╲
1e-6 ┼───╱──────────╲──── t
     0   T_max/2   T_max
```

---

## 完整训练代码示例

基于CNN验证码识别项目：

```python
import torch
import torch.nn as nn
import torch.optim as optim

# ========== 1. 初始化 ==========
# 损失函数：交叉熵（多分类任务标配）
criterion = nn.CrossEntropyLoss()

# 优化器：Adam（对学习率不敏感，适合新手入门）
optimizer = optim.Adam(
    model.parameters(),
    lr=learning_rate,      # 推荐从0.001开始
    betas=(0.9, 0.999),    # 默认值，可不写
    eps=1e-8
)

# 学习率调度器：每3个epoch学习率乘0.7
scheduler = optim.lr_scheduler.StepLR(
    optimizer,
    step_size=3,
    gamma=0.7
)

# ========== 2. 训练循环 ==========
for epoch in range(num_epochs):
    model.train()  # 切换到训练模式（启用Dropout/BatchNorm训练行为）
    
    running_loss = 0.0
    correct = 0
    total = 0
    
    for batch_idx, (images, labels) in enumerate(train_loader):
        # 前向传播
        outputs = model(images)
        loss = criterion(outputs, labels)
        
        # 反向传播（关键三步）
        optimizer.zero_grad()   # 清空梯度
        loss.backward()          # 计算梯度
        optimizer.step()        # 更新参数
        
        # 统计
        running_loss += loss.item()
        _, predicted = outputs.max(1)
        total += labels.size(0)
        correct += predicted.eq(labels).sum().item()
    
    # Epoch结束，更新学习率
    scheduler.step()
    
    # 打印日志
    train_loss = running_loss / len(train_loader)
    train_acc = 100. * correct / total
    current_lr = scheduler.get_last_lr()[0]
    
    print(f'Epoch [{epoch+1}/{num_epochs}] '
          f'Loss: {train_loss:.4f} '
          f'Acc: {train_acc:.2f}% '
          f'LR: {current_lr:.6f}')

# ========== 3. 保存模型 ==========
torch.save({
    'epoch': num_epochs,
    'model_state_dict': model.state_dict(),
    'optimizer_state_dict': optimizer.state_dict(),
    'loss': train_loss,
}, 'captcha_model.pth')
```

### 验证集评估

```python
def evaluate(model, val_loader, criterion):
    model.eval()  # 切换到评估模式（固定Dropout/BatchNorm）
    
    val_loss = 0.0
    val_correct = 0
    val_total = 0
    
    with torch.no_grad():  # 不计算梯度，节省显存
        for images, labels in val_loader:
            outputs = model(images)
            loss = criterion(outputs, labels)
            
            val_loss += loss.item()
            _, predicted = torch.max(outputs.data, 1)
            val_total += labels.size(0)
            val_correct += (predicted == labels).sum().item()
    
    accuracy = 100. * val_correct / val_total
    avg_loss = val_loss / len(val_loader)
    
    return avg_loss, accuracy
```

---

## 优化器选择指南

> 更多理论对比见 [06_模型训练.md](./06_模型训练.md)

| 场景 | 推荐算法 | 理由 |
|-----|---------|------|
| 新手入门 | **Adam** | 对学习率不敏感，默认参数效果一般不差 |
| Kaggle竞赛 | **AdamW + CosineAnnealing** | 现代炼丹标配 |
| 生产部署 | **SGD + Momentum** | 泛化性能好，有时精度更高 |
| 图像分类 | **SGD + Momentum** | 经典配置，配合学习率衰减效果最好 |
| NLP/语言模型 | **Adam** | Transformer时代标配 |
| 稀疏数据 | **AdaGrad/Adam** | 自适应学习率照顾稀疏特征 |

### 我的推荐：验证码识别项目用这个

```python
# 入门阶段：简单可靠
optimizer = optim.Adam(model.parameters(), lr=0.001)
scheduler = optim.lr_scheduler.StepLR(optimizer, step_size=5, gamma=0.5)

# 调优阶段：效果更好
optimizer = optim.AdamW(model.parameters(), lr=0.001, weight_decay=0.01)
scheduler = optim.lr_scheduler.CosineAnnealingWarmRestarts(
    optimizer, T_0=10, T_mult=2
)
```

---

## 常见问题排查

### 损失不下降

| 可能原因 | 解决方案 |
|---------|---------|
| 学习率太大 | 降低到1e-4或1e-5 |
| 学习率太小 | 提高到1e-3 |
| 梯度消失 | 检查BatchNorm、激活函数 |
| 学习率衰减太快 | 增大gamma(如0.9)或step_size |

### 损失NaN

```python
# 检查方法
if torch.isnan(loss):
    print(f"NaN detected! Input max: {images.max()}")
```

| 可能原因 | 解决方案 |
|---------|---------|
| 学习率过大 | 降低学习率 |
| 除零错误 | 检查损失计算 |
| 输入数据异常 | 归一化检查 |
| softmax输入数值过大 | 损失函数前加logits |

### 显存不足

```python
# 减小batch_size
batch_size = 16  # 而不是32或64

# 梯度累积
accumulation_steps = 4
effective_batch = batch_size * accumulation_steps

# 清理显存
torch.cuda.empty_cache()
```

---

## 小结

```
torch.optim          → 优化器工具箱
model.parameters()  → 要优化的参数列表
zero_grad()          → 清空累积的梯度
step()               → 根据梯度更新参数
state_dict()         → 保存/恢复优化器状态

scheduler.step()     → 自动调整学习率
```

记住这个训练模板：

```python
for images, labels in loader:
    optimizer.zero_grad()     # 1. 清梯度
    outputs = model(images)   # 2. 前向传播
    loss = criterion(outputs, labels)  # 3. 计算损失
    loss.backward()           # 4. 反向传播
    optimizer.step()          # 5. 更新参数
```

搞定！🎉

---

## 多标签分类的训练实战：验证码识别

### 和普通分类的区别

普通分类：1张图 → 1个输出 → 1个loss

验证码识别：1张图 → **多个输出**（每个字符位置独立分类）→ **多个loss求和**

比如5位验证码，每位有36种可能（0-9 + A-Z），模型输出5个独立的分类结果：

```
outputs[0] → 第1个字符的分类概率（36类）
outputs[1] → 第2个字符的分类概率（36类）
outputs[2] → 第3个字符的分类概率（36类）
outputs[3] → 第4个字符的分类概率（36类）
outputs[4] → 第5个字符的分类概率（36类）
```

### 损失函数怎么算

5个位置分别算CrossEntropyLoss，然后**加起来**作为一个batch的总loss，一次性反向传播：

```python
criterion = nn.CrossEntropyLoss()
batch_loss = 0
for i in range(5):  # 5个字符位置
    batch_loss += criterion(outputs[i], labels[:, i])
batch_loss.backward()
optimizer.step()
```

`labels[:, i]` 取的是每个样本第i个位置的真实标签。

**为什么加起来而不是分开训练？** 因为5个位置共享同一个CNN特征提取器，一起训练更高效，特征提取能力更强。

### 准确率：序列准确率

验证码任务里，**5个字符全部猜对才算对**，错1个就算错：

```python
predicted_chars = []
for i in range(5):
    _, predicted = torch.max(outputs[i], 1)  # 每个位置取概率最大的类
    predicted_chars.append(predicted)

predicted_sequence = torch.stack(predicted_chars, dim=1)  # 拼成 [batch_size, 5]
correct_sequence = (predicted_sequence == labels).all(dim=1)  # 5个全对才算对
train_correct += correct_sequence.sum().item()
```

`(predicted_sequence == labels).all(dim=1)` 这个操作：
- 先逐元素比较 → 得到 [batch, 5] 的bool矩阵
- `.all(dim=1)` → 每行全True才返回True → [batch] 的bool向量
- 只有5个位置全部正确才算1个正确样本

### 保存最佳模型

```python
if epoch == 0 or avg_val_loss < min(val_losses[:-1]):
    torch.save(model.state_dict(), 'best_captcha_model.pth')
```

只存验证损失**最低**的那个epoch，不存最新的。这样拿到的永远是效果最好的模型，避免过拟合后的烂模型覆盖了好模型。

### 完整训练流程图

```
初始化 → 循环每个epoch:
  ├── 训练阶段（model.train()）
  │     循环每个batch:
  │       zero_grad → 前向传播 → 5个位置分别算loss加总 → backward → step
  │     记录训练loss和准确率
  │
  ├── 验证阶段（model.eval() + torch.no_grad()）
  │     不算梯度，跑一遍验证集
  │     记录验证loss和准确率
  │
  ├── 调整学习率（scheduler.step(avg_val_loss)）
  └── 保存最佳模型（验证loss最低时）
```

### 训练日志示例

```
Epoch [1/50]  Train Loss: 12.3456 | Train Acc: 5.23% | Val Loss: 10.8912 | Val Acc: 8.12% | Time: 12.34s
Epoch [2/50]  Train Loss: 8.7654  | Train Acc: 15.67%| Val Loss: 7.2345  | Val Acc: 18.45%| Time: 11.89s
...
保存最佳模型 (验证损失: 7.2345)
```

---

## 多标签分类实战：验证码识别项目完整实现

### 项目整体架构

```
验证码识别项目
├── 数据集构建 (CaptchaDataset)
│     ├── 字符集定义：0-9 + a-z = 36类
│     ├── 图像预处理：Resize → ToTensor → Normalize
│     └── 数据划分：训练集/验证集/测试集 = 8:1:1
│
├── 模型定义 (CaptchaCNN)
│     ├── 特征提取：4层卷积块 (32→64→128→256)
│     └── 分类头：5个独立分类器 (每个位置一个)
│
├── 训练流程
│     ├── 优化器：Adam
│     ├── 学习率调度：ReduceLROnPlateau
│     └── 早停策略：保存验证损失最低的模型
│
└── 评估体系
      ├── 序列准确率：5个位置全对才算对
      ├── 逐字符准确率：分析每个位置的识别效果
      └── 错误分析：可视化预测错误的样本
```

### 数据集构建：CaptchaDataset

```python
import torch
from torch.utils.data import Dataset
from torchvision import transforms
from PIL import Image
import json
import string

class CaptchaDataset(Dataset):
    """验证码数据集"""
    
    # 字符集：数字 + 小写字母 = 36类
    CHARSET = string.digits + string.ascii_lowercase  # '0123456789abcdefghijklmnopqrstuvwxyz'
    NUM_CLASSES = len(CHARSET)  # 36
    CAPTCHA_LENGTH = 5  # 验证码长度
    
    def __init__(self, annotations_file, img_dir, transform=None):
        """
        Args:
            annotations_file: JSON标注文件路径
            img_dir: 图像文件夹路径
            transform: 图像变换
        """
        self.img_dir = img_dir
        self.transform = transform
        
        # 加载标注数据
        with open(annotations_file, 'r') as f:
            self.annotations = json.load(f)
        
        # 构建字符到索引的双向映射
        self.char_to_idx = {char: idx for idx, char in enumerate(self.CHARSET)}
        self.idx_to_char = {idx: char for idx, char in enumerate(self.CHARSET)}
        
        # 过滤：只保留长度为5且字符合法的样本
        self.samples = [
            (img_path, label) 
            for img_path, label in self.annotations.items()
            if len(label) == self.CAPTCHA_LENGTH and all(c in self.CHARSET for c in label)
        ]
        
        print(f"数据集加载完成: {len(self.samples)} 张有效样本")
    
    def __len__(self):
        return len(self.samples)
    
    def __getitem__(self, idx):
        img_path, label = self.samples[idx]
        
        # 加载图像
        image = Image.open(img_path).convert('L')  # 转为灰度图
        
        # 应用变换
        if self.transform:
            image = self.transform(image)
        
        # 标签转为索引张量 [5]
        label_indices = torch.tensor([
            self.char_to_idx[c] for c in label
        ], dtype=torch.long)
        
        return image, label_indices

# 图像预处理
train_transform = transforms.Compose([
    transforms.Resize((40, 100)),      # 统一尺寸
    transforms.ToTensor(),              # [0,1] → [-1,1]
    transforms.Normalize(0.5, 0.5)      # 标准化
])

# 创建数据集
train_dataset = CaptchaDataset(
    annotations_file='train_annotations.json',
    img_dir='./data/train',
    transform=train_transform
)
```

### CNN模型架构：CaptchaCNN

```python
import torch.nn as nn
import torch.nn.functional as F

class CaptchaCNN(nn.Module):
    """验证码识别CNN模型"""
    
    def __init__(self, num_classes=36, num_chars=5):
        super(CaptchaCNN, self).__init__()
        self.num_chars = num_chars
        
        # ========== 特征提取器：4层卷积块 ==========
        # 输入: [batch, 1, 40, 100]
        
        # Block 1: 1 → 32
        self.conv1 = nn.Sequential(
            nn.Conv2d(1, 32, kernel_size=3, padding=1),
            nn.BatchNorm2d(32),
            nn.ReLU(),
            nn.Conv2d(32, 32, kernel_size=3, padding=1),
            nn.BatchNorm2d(32),
            nn.ReLU(),
            nn.MaxPool2d(2, 2)  # → [batch, 32, 20, 50]
        )
        
        # Block 2: 32 → 64
        self.conv2 = nn.Sequential(
            nn.Conv2d(32, 64, kernel_size=3, padding=1),
            nn.BatchNorm2d(64),
            nn.ReLU(),
            nn.Conv2d(64, 64, kernel_size=3, padding=1),
            nn.BatchNorm2d(64),
            nn.ReLU(),
            nn.MaxPool2d(2, 2)  # → [batch, 64, 10, 25]
        )
        
        # Block 3: 64 → 128
        self.conv3 = nn.Sequential(
            nn.Conv2d(64, 128, kernel_size=3, padding=1),
            nn.BatchNorm2d(128),
            nn.ReLU(),
            nn.Conv2d(128, 128, kernel_size=3, padding=1),
            nn.BatchNorm2d(128),
            nn.ReLU(),
            nn.MaxPool2d(2, 2)  # → [batch, 128, 5, 12]
        )
        
        # Block 4: 128 → 256
        self.conv4 = nn.Sequential(
            nn.Conv2d(128, 256, kernel_size=3, padding=1),
            nn.BatchNorm2d(256),
            nn.ReLU(),
            nn.Conv2d(256, 256, kernel_size=3, padding=1),
            nn.BatchNorm2d(256),
            nn.ReLU(),
            nn.MaxPool2d(2, 2)  # → [batch, 256, 5, 6]
        )
        
        # ========== 展平 + Dropout ==========
        self.dropout = nn.Dropout(0.5)
        
        # ========== 5个独立分类器 ==========
        # 每个字符位置有独立的分类器
        self.classifiers = nn.ModuleList([
            nn.Sequential(
                nn.Linear(256 * 5 * 6, 512),  # 7680 → 512
                nn.ReLU(),
                nn.Dropout(0.5),
                nn.Linear(512, num_classes)    # 512 → 36
            )
            for _ in range(num_chars)
        ])
    
    def forward(self, x):
        # 特征提取
        x = self.conv1(x)
        x = self.conv2(x)
        x = self.conv3(x)
        x = self.conv4(x)
        
        # 展平
        x = x.view(x.size(0), -1)  # [batch, 7680]
        x = self.dropout(x)
        
        # 5个位置分别分类
        outputs = [classifier(x) for classifier in self.classifiers]
        
        # 返回每个位置的logits
        return outputs

# 模型信息
model = CaptchaCNN(num_classes=36, num_chars=5)
print(f"模型参数量: {sum(p.numel() for p in model.parameters()):,}")
# 输出: 模型参数量: 4,215,844
```

**特征图尺寸变化：**

```
输入图像:     [1,   40, 100]   → 40×100
Block 1:      [32,  20,  50]   → 20×50   (2×缩小)
Block 2:      [64,  10,  25]   → 10×25   (2×缩小)
Block 3:      [128, 5,   12]   → 5×12    (2×缩小)
Block 4:      [256, 5,    6]   → 5×6     (2×缩小)

展平后:       256 × 5 × 6 = 7680
```

### 数据加载器划分

```python
from torch.utils.data import random_split, DataLoader

def create_data_loaders(dataset, batch_size=64, val_ratio=0.1):
    """划分数据集为训练/验证/测试集"""
    
    # 计算划分大小
    total_size = len(dataset)
    val_size = int(total_size * val_ratio)
    test_size = int(total_size * val_ratio)
    train_size = total_size - val_size - test_size
    
    # 随机划分
    train_dataset, val_dataset, test_dataset = random_split(
        dataset, 
        [train_size, val_size, test_size],
        generator=torch.Generator().manual_seed(42)
    )
    
    # 创建DataLoader
    train_loader = DataLoader(
        train_dataset, 
        batch_size=batch_size, 
        shuffle=True,
        num_workers=2
    )
    val_loader = DataLoader(
        val_dataset, 
        batch_size=batch_size, 
        shuffle=False,
        num_workers=2
    )
    test_loader = DataLoader(
        test_dataset, 
        batch_size=batch_size, 
        shuffle=False,
        num_workers=2
    )
    
    print(f"训练集: {len(train_dataset)} | 验证集: {len(val_dataset)} | 测试集: {len(test_dataset)}")
    
    return train_loader, val_loader, test_loader

# 使用
train_loader, val_loader, test_loader = create_data_loaders(
    train_dataset, 
    batch_size=64
)
```

### 训练函数实现

```python
import time

def train_model(model, train_loader, val_loader, num_epochs=30, lr=0.001):
    """训练验证码识别模型"""
    
    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
    model = model.to(device)
    
    # 损失函数
    criterion = nn.CrossEntropyLoss()
    
    # 优化器
    optimizer = optim.Adam(model.parameters(), lr=lr)
    
    # 学习率调度器：验证损失不再下降时减半
    scheduler = optim.lr_scheduler.ReduceLROnPlateau(
        optimizer, 
        mode='min', 
        factor=0.5,      # 学习率减半
        patience=5,       # 5个epoch没改善才调整
        verbose=True
    )
    
    # 记录训练历史
    history = {
        'train_loss': [], 'train_acc': [],
        'val_loss': [], 'val_acc': []
    }
    
    best_val_loss = float('inf')
    
    for epoch in range(num_epochs):
        epoch_start = time.time()
        
        # ========== 训练阶段 ==========
        model.train()
        train_loss = 0.0
        train_correct = 0
        train_total = 0
        
        for images, labels in train_loader:
            images = images.to(device)
            labels = labels.to(device)
            
            # 前向传播
            optimizer.zero_grad()
            outputs = model(images)  # outputs: 5个[batch, 36]的张量列表
            
            # 计算损失：5个位置分别计算后求和
            loss = sum(criterion(outputs[i], labels[:, i]) for i in range(5))
            
            # 反向传播
            loss.backward()
            optimizer.step()
            
            # 统计
            train_loss += loss.item()
            
            # 序列准确率：5个位置全对才算对
            predicted_indices = torch.stack([
                torch.max(outputs[i], 1)[1] for i in range(5)
            ], dim=1)
            correct = (predicted_indices == labels).all(dim=1)
            train_correct += correct.sum().item()
            train_total += labels.size(0)
        
        train_loss /= len(train_loader)
        train_acc = 100. * train_correct / train_total
        
        # ========== 验证阶段 ==========
        model.eval()
        val_loss = 0.0
        val_correct = 0
        val_total = 0
        
        with torch.no_grad():
            for images, labels in val_loader:
                images = images.to(device)
                labels = labels.to(device)
                
                outputs = model(images)
                loss = sum(criterion(outputs[i], labels[:, i]) for i in range(5))
                
                val_loss += loss.item()
                
                predicted_indices = torch.stack([
                    torch.max(outputs[i], 1)[1] for i in range(5)
                ], dim=1)
                correct = (predicted_indices == labels).all(dim=1)
                val_correct += correct.sum().item()
                val_total += labels.size(0)
        
        val_loss /= len(val_loader)
        val_acc = 100. * val_correct / val_total
        
        # 记录历史
        history['train_loss'].append(train_loss)
        history['train_acc'].append(train_acc)
        history['val_loss'].append(val_loss)
        history['val_acc'].append(val_acc)
        
        # 学习率调整
        scheduler.step(val_loss)
        current_lr = optimizer.param_groups[0]['lr']
        
        # 保存最佳模型
        if val_loss < best_val_loss:
            best_val_loss = val_loss
            torch.save(model.state_dict(), 'best_captcha_model.pth')
            save_marker = " ★"
        else:
            save_marker = ""
        
        # 打印日志
        epoch_time = time.time() - epoch_start
        print(f"Epoch [{epoch+1:2d}/{num_epochs}] "
              f"Loss: {train_loss:.4f}/{val_loss:.4f} | "
              f"Acc: {train_acc:.2f}%/{val_acc:.2f}% | "
              f"LR: {current_lr:.6f} | {epoch_time:.1f}s{save_marker}")
    
    return model, history

# 启动训练
model, history = train_model(
    CaptchaCNN(), 
    train_loader, 
    val_loader,
    num_epochs=30,
    lr=0.001
)
```

### 评估函数设计

```python
def evaluate_model(model, test_loader):
    """全面评估模型性能"""
    
    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
    model = model.to(device)
    model.eval()
    
    criterion = nn.CrossEntropyLoss()
    
    # 总体统计
    total_loss = 0.0
    total_correct = 0
    total_samples = 0
    
    # 每个位置的单独准确率
    position_correct = [0] * 5
    position_total = [0] * 5
    
    # 错误样本收集
    wrong_samples = []
    
    with torch.no_grad():
        for images, labels in test_loader:
            images = images.to(device)
            labels = labels.to(device)
            
            outputs = model(images)
            loss = sum(criterion(outputs[i], labels[:, i]) for i in range(5))
            
            total_loss += loss.item()
            
            # 预测结果
            predicted_indices = torch.stack([
                torch.max(outputs[i], 1)[1] for i in range(5)
            ], dim=1)
            
            # 序列准确率
            correct_sequences = (predicted_indices == labels).all(dim=1)
            total_correct += correct_sequences.sum().item()
            total_samples += labels.size(0)
            
            # 位置准确率
            for pos in range(5):
                position_correct[pos] += (predicted_indices[:, pos] == labels[:, pos]).sum().item()
                position_total[pos] += labels.size(0)
            
            # 收集错误样本（最多10个）
            wrong_mask = ~correct_sequences
            if wrong_mask.sum() > 0 and len(wrong_samples) < 10:
                for i in range(images.size(0)):
                    if wrong_mask[i]:
                        pred_seq = ''.join([
                            dataset.idx_to_char[p.item()] 
                            for p in predicted_indices[i]
                        ])
                        true_seq = ''.join([
                            dataset.idx_to_char[t.item()] 
                            for t in labels[i]
                        ])
                        wrong_samples.append({
                            'image': images[i].cpu(),
                            'predicted': pred_seq,
                            'ground_truth': true_seq
                        })
    
    # 打印结果
    print("=" * 60)
    print("测试集评估结果")
    print("=" * 60)
    print(f"总体准确率: {100. * total_correct / total_samples:.2f}%")
    print(f"平均损失: {total_loss / len(test_loader):.4f}")
    print()
    print("各位置准确率:")
    for pos in range(5):
        acc = 100. * position_correct[pos] / position_total[pos]
        print(f"  位置 {pos+1}: {acc:.2f}%")
    print()
    
    # 错误样本分析
    print("错误样本示例 (最多显示10个):")
    for i, sample in enumerate(wrong_samples[:5]):
        print(f"  样本{i+1}: 预测={sample['predicted']} | 真实={sample['ground_truth']}")
    
    return {
        'accuracy': total_correct / total_samples,
        'loss': total_loss / len(test_loader),
        'position_accuracy': [position_correct[i] / position_total[i] for i in range(5)],
        'wrong_samples': wrong_samples
    }

# 评估
results = evaluate_model(model, test_loader)
```

### 训练曲线绘制

```python
import matplotlib.pyplot as plt

def plot_training_curves(history):
    """绘制训练曲线"""
    
    epochs = range(1, len(history['train_loss']) + 1)
    
    fig, axes = plt.subplots(1, 3, figsize=(15, 4))
    
    # 1. 损失曲线
    axes[0].plot(epochs, history['train_loss'], 'b-', label='训练损失', linewidth=2)
    axes[0].plot(epochs, history['val_loss'], 'r-', label='验证损失', linewidth=2)
    axes[0].set_xlabel('Epoch')
    axes[0].set_ylabel('Loss')
    axes[0].set_title('损失曲线')
    axes[0].legend()
    axes[0].grid(True, alpha=0.3)
    
    # 2. 准确率曲线
    axes[1].plot(epochs, history['train_acc'], 'b-', label='训练准确率', linewidth=2)
    axes[1].plot(epochs, history['val_acc'], 'r-', label='验证准确率', linewidth=2)
    axes[1].set_xlabel('Epoch')
    axes[1].set_ylabel('Accuracy (%)')
    axes[1].set_title('准确率曲线')
    axes[1].legend()
    axes[1].grid(True, alpha=0.3)
    
    # 3. 综合对比
    axes[2].plot(epochs, history['val_acc'], 'g-', linewidth=2)
    axes[2].set_xlabel('Epoch')
    axes[2].set_ylabel('Val Accuracy (%)')
    axes[2].set_title('验证准确率变化')
    axes[2].grid(True, alpha=0.3)
    
    plt.tight_layout()
    plt.savefig('training_curves.png', dpi=150)
    plt.show()
    
    print("训练曲线已保存至 training_curves.png")

# 绘制
plot_training_curves(history)
```

### 主程序完整代码

```python
def main():
    # 配置
    batch_size = 64
    num_epochs = 30
    learning_rate = 0.001
    
    # 设备选择
    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
    print(f"使用设备: {device}")
    
    # 数据准备
    train_transform = transforms.Compose([
        transforms.Resize((40, 100)),
        transforms.ToTensor(),
        transforms.Normalize(0.5, 0.5)
    ])
    
    train_dataset = CaptchaDataset(
        annotations_file='train_annotations.json',
        img_dir='./data/train',
        transform=train_transform
    )
    
    train_loader, val_loader, test_loader = create_data_loaders(
        train_dataset, batch_size=batch_size
    )
    
    # 模型
    model = CaptchaCNN(num_classes=36, num_chars=5)
    print(f"模型参数量: {sum(p.numel() for p in model.parameters()):,}")
    
    # 训练
    model, history = train_model(
        model, train_loader, val_loader,
        num_epochs=num_epochs,
        lr=learning_rate
    )
    
    # 绘制训练曲线
    plot_training_curves(history)
    
    # 测试集评估
    model.load_state_dict(torch.load('best_captcha_model.pth'))
    results = evaluate_model(model, test_loader)
    
    return model, history, results

if __name__ == '__main__':
    model, history, results = main()
```

### 完整项目结构总结

```
captcha_recognition/
├── data/
│   ├── train_annotations.json    # 训练集标注
│   └── train/                   # 训练图像
├── models/
│   └── captcha_cnn.py           # 模型定义
├── utils/
│   ├── dataset.py               # 数据集类
│   └── evaluation.py            # 评估函数
├── train.py                     # 训练脚本
└── requirements.txt

# 训练输出示例
Epoch [ 1/30] Loss: 12.3456/10.2345 | Acc: 5.23%/8.12% | LR: 0.001000 | 12.3s ★
Epoch [ 2/30] Loss: 9.8765/8.1234 | Acc: 12.45%/15.67% | LR: 0.001000 | 11.8s
...
Epoch [30/30] Loss: 0.1234/0.2345 | Acc: 98.76%/95.43% | LR: 0.000031 | 10.5s

# 测试集准确率: 95.43%
```
