1 堆排序 其实是对选择排序的一种改进 我们先看一个比较傻的算法 就是一个非常直接了当的DeleteMin 这个 Heap_Sort在一开始的时候调用了一下BuildHeap 这个函数 我们大家知道 有一种线性复杂度的算法 可以把一个数组直接调整成一个最小堆 那调整好了以后 每次我就调用一个DeleteMin 把那个根节点给他弹出来 弹出来以后我得有一个地方存 我需要另外开一个临时的数组 去存这些元素 总之我就一个一个的 把它那个最小元弹出来 然后依次存到这个临时数组里面去 然后就完了吗 没完 我们最后 必须把这个临时数组里的元素 导回到A这个数组里面去 因为我这个 Heap_sort 默认的接口是什么 你的用户把一个无序的数组给了你 你在执行完了这个Sort之后 返回给他的必须是一个有序的数组 而这个有序的数组还必须要存在 原来的这个数组里面 所以光把这个TmpA生成了是不行的 你还得把TmpA 里面所有的元素导回到A里面去 那这个其实就是挺浪费时间的一步了 那我们来看一下整体的时间复杂度是什么呢 BuildHeap这一步 我们之前分析过 他是一个线性时间复杂度的 然后在这个循环里面 每一步一个DeleteMin 是我们知道是一个logN复杂度的 所以乘上这个外循环应该是NlogN 这一步应该是NlogN复杂度的 这一步呢 单纯的导一下 整体应该是O(N)复杂度的 所以全部加在一起 他的时间复杂度应该是 NlogN的 那么这个算法呢 看上去非常简单 但是他有一个非常大的问题就是 他需要额外 开一个O(N)这个数量级的空间 你说我空间有富裕啊 你如果是整个A的规模比较小 那其实是没有关系的 但是如果你想象一下你的内存一共有2GB 那本来 你是可以对2GB的数据进行排序的 但是因为你要开这个额外的空间 导致你一次只能排1GB的东西 那这个时候就有问题了 你要排2GB的元素 你就开不出这个空间来 你这个算法就不能用了 另外 就是复制这个元素 也是需要时间的 问题是 其实我们本来是没有必要做这一步的 怎么能把这一步跳过去呢 我们有更聪明的第二个算法 要理解算法2 我们还是先来看一个例子 比如说我们有一个数组 有四个元素 算法2的一个想法是 我不是要把它调整成一个最小堆 而是把它调整成一个最大堆 那怎么调整呢 你应该会做的吧 从这个子树开始 然后再调整这棵子树 最后我们会得到 这样的一个结果 d 是 abcd 里面最大的那个字母 然后他是处在根节点的位置 那因为我们知道 d 是最大的元素 而在一个正常排好序的数组里面 最大元素应该放在哪呢 他应该是放在最后一个位置的 所以我们要做的就是把根节点 跟最后一个节点 做一个交换 把b放上去 把d放下来 放下来以后呢 我们就把这个整个堆的规模-1 我把这个d就排除在外了 因为后面再做什么这个d都不用动了 他已经放在他最终的正确的位置上了 然后我们再来看剩下的这个堆 剩下这个堆还有三个元素 然后我要继续把这个堆 调整成一个最大堆 那么这个是调整的结果 c 应该在上面 然后再重复前面的步骤 也就是说 把 c 换到现在最后一个位置上去 把 c 和 a 换一下位置 然后我们再把这个 c 也给砍掉 现在的堆就只剩下 a 和 b 两个元素 我们再把它调整成一个最大堆 然后我们知道 b 和 a 做最后一次交换 就完成了我们最后的排序 那在这 我要先稍微暂停一下 你们要思考一个问题 在何老师讲到堆这个概念的时候 我们说堆的元素 是从第一个下标为1的元素开始计数的 A[0]是不放任何真的元素的 A[0]里面放的是一个哨兵 对不对 但是在我们排序的算法里头 你的用户 可不知道在前面应该给你留个哨兵 你的用户是从第0个元素就开始存的 所以在堆排序里面的这个堆 他的元素是从0开始记的 那这也就导致了任何一个节点 他跟他的孩子节点的那个下标的关系就不一样了 到底怎么不一样了呢 我们停一下 做一个小测验再回来 好搞清楚了 这个细节以后 我们就来看一下这个真正的堆排序 他的伪代码 那前面这一块 实际上就是把那个BuildHeap这个函数写的更具体了一点 他的核心就是调用一个 向下过滤的这么一个子函数 i对应的是根节点所在的位置 N的对应的是当前这个堆里一共有多少个元素 从这个i=N/2开始 反复的调用 就把一个最大堆 建立起来了 然后就进入了我们的 这个堆排序的循环 在这个循环里我们要做的是 在我最大堆建立完了以后 我就知道 A[0]这个根节点 里面一定存的是最大的元素 那么我把它跟A[i]  A[i]是什么东西 那么 i 呢其实就记录的是 当前的最后一个元素他的下标 我也就是把这根节点 换到当前这个堆的最后一个元素的位置上去 就相当于是一个DeleteMax 这么一个过程 然后把剩下的元素继续调整成 一个最大堆 调整的时候是以0为根节点 然后 i 是当前这个最大堆的元素的个数 那么这个Heap_Sort他的时间复杂度是什么样的呢 我们有这样的一个定理 说 堆排序 处理N个不同元素的随机排列的平均比较次数 是 由这个公式给出的 所以无论如何 他是O(NlogN) 他比Nlog还要再小一点 他减掉了另外一个东西的 所以他的平均比较次数 我仍然可以写成O(NlogN) 但是我们要知道 他比NlogN稍微要好一点 虽然堆排序给出了一个最佳平均时间复杂度 但是实际应用的效果 可能不如这个 希尔排序 用一个Sedgewick增量序列的那个效果要更好 到底是不是这样呢 那我们课后布置一道题 大家可以把 各种不同的算法都用到那道题上去 然后比较一下 他们到底谁比谁要好 然后到底要好多少