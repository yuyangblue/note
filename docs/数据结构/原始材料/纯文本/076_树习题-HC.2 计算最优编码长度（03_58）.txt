1 根据我们前一节的分析呢 我们的核心算法基本上就分两步 第一呢要验证他的长度 是不是等于最右编码的长度 第二呢就来检查 他是不是一个没有歧义的前缀码 那么首先呢 我们就得先计算出这个标准答案 最优编码的长度到底是多少 这个计算呢应该是比较简单的 前面呢 我们何老师已经给过大家一个 哈夫曼的代码 就是如何去建立一棵哈夫曼树 在这呢 我们就声明一棵哈夫曼树 然后用这个哈夫曼算法去生成它 那要注意到 在调用哈夫曼算法的时候 就必须要先有一个最小堆 所以呢在前面我先得声明一个最小堆 然后我调用那个现成的CreateHeap 去创建一个空的容量为N的最小堆 然后接下来呢 我们要做的事情就是把 这个读进来的频率 读到这个堆的数组里面去 为后面我们建立一个最小堆来做好准备 那么建立最小堆 这件事情是在哈夫曼算法里面完成的 大家回忆一下 哈夫曼算法里面先建了一个最小堆 然后每次从里面弹出两个最小的结点 然后生成一颗新的树再把它压回去 诸如此类 那么调用完了这个哈夫曼之后 我们就生成了一颗哈夫曼树 但是并不等于说 我们就得到了这个最优的编码长度 这棵树是有了编码长度呢 还是要我们自己另外去算一下的 需要我们另外去写一个函数啊 比如说我把它取名字叫 WPL 他返回的 CodeLen 就应该是整个这棵哈夫曼树 所对应的最优编码的总长度 那么这个WPL要怎么个写法呢 明显的 我传进去的是传进去了这个树的树根 然后这边这个0是怎么回事呢 我们先来看 随便一棵树的例子 整个这棵树 我要计算最优编码的总长度的话 也就是说 我需要去访问他一个一个的叶子结点 当我访问到他的某一个叶子结点的时候 我应该知道这个叶子结点 所处的深度是多少 就是从根结点到这个叶子结点 他路径的长度得到深度以后呢 我就可以用深度乘以他的频率 我就得到了这一条编码 他的这个 WPL 那么我要做的准备的事情呢 就是把所有叶子结点的WPL 整个求和就对了 如果你习惯 用这个递归的思想去考虑问题的话 很简单的 一棵树的总的WPL 就是他左子树的WPL 加上他右子树的WPL 对不对 我们需要的呢 就是在递归调用左边 和递归调用右边的时候 把一个正确的深度给传进去 所谓深度呢 就是我传进去的每一棵子树的根结点 他当前的深度是多少 那我们是从整棵树的根结点开始的 那么当前这个根的深度是0 所以我从零开始 接下来的事情就很简单 实际上是一个递归的先序遍历的过程 那我们传进来一个哈夫曼tree 他的根结点 以及这个根结点T当前的深度 如果 T 没有左孩子 并且没有右孩子就意味着 如果当前这个 T 结点 是一个叶子结点的话 那我们就开始计算了 就是用他当前的深度乘以他的权重 然后返回这个结果 如果他不是根结点的话 如果我们在这生成的这个哈夫曼tree 是正确的话 那么他一定有两个孩子 于是呢 我们就去递归地解决他左边的问题 然后加上递归的解决他右边的问题 然后返回这个两边的和就对了 那么在递归解决左右问题的时候 注意到那个结点的深度 是要加1的