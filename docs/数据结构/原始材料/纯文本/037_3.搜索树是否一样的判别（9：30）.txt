1 接下来到我们最关键的一步 现在我们的判别方法是什么呢 前面提到过了 我是已经构造了一个树T 接下来我有一个序列 我要你判别这个序列 是不是跟我们的这个T是一致的 所谓一致呢 就是我按这个序列构造的搜索树 跟你的搜索树是一样的 对这个例子来讲 用3 1 4 2 就是第一行的序列来构造我们的T 那么就形成了左边这样的一棵树 然后我们要比较的序列是3 2 4 1 他实际上对应的就是我们右边这棵树 所以我们的目标是怎么来判别 3 2 4 1这个序列 跟我们的T是一致的 其实我们可以用种方法很容易做 就把这个序列每个整数3 2 4 1到 这个树T里面去查找一下 比方说3 到T里去找找3在哪个位置 然后2到T里去找找在哪个位置 如果在这个查找过程当中 发现查找某个整数 他所经过的路径有某个节点 以前没碰到过的 就是在这前面没出现过的 那么这个序列跟这个树一定不一致 因为如果没碰到过 那么当前的这个整数 就不应该插在后面 应该在 没有碰到过的节点上就应该插入了 而不会再往下插的 所以我们把一个序列 跟一个树是不是一致的这个判别 去转换成为 查找这个序列中的每一个整数 在这个树中的位置 如果在查找过程当中发现某个位置 以前没碰到过 那就说明这个是不一致的 如果所有的整数 查找过程当中全都是碰到过的 那就说明这两个是一致的 对这个例子来讲 我们的树是左边这个树T 然后我们查找的序列是3 2 4 1 我们先找3 3是根节点 这是第一个数 所以马上就是标记为 flag 就为1 就是我找过了 接下来找第二个数2 那么为了找2 他要从3开始查找 接下来要找到1 接下来再找到2 从3 1 2这个过程当中 3前面碰到过了 而1没碰到过 那么这个时候我们就可以断定 这个序列 3 2 4 1跟我们的T是不一致的 可以这就是我们的判别方法 所以我们的判别方法就很简单 就是在T里面 去搜索我们序列中的每一个整数 就是实际上是个查找过程 在查找的时候我们来观察 有没有哪些节点以前没碰到过 那么怎么判别有没有碰到过呢 我们就需要一个标记 flag 就一开始都是为0 找过了把设为1 所以在这个查找过程当中 一旦发现某个节点 他的 flag 是0 那好了 那就可以说明这个是不一致的 所以我们就用一个函数叫check 在T这个树里面去查找V这个整数 那么首先判别一下这根节点 flag 是0还是1 如果 flag是等于1 就是我们if后面的这段话 这个时候是查找过了 这个时候我们到左边去找 或者到右边去找 那就要看V跟根节点值v之间的关系 如果比他小就到左边去找 所以就调用check left 如果比他大就到右边去找 所以就调用check right 如果相等 就意味的在这个序列里面 有两个整数是 出现了两次以上 就重复出现了 那么这个时候我们认为是不一致的 所以这个时候return 0 反过来 如果 flag 标记为0 就意味着没被访问过 就是else这部分的语句 那就要看了 当前的节点的V 是不是我要查找的值 如果是相等 那么这个时候 就把这个节点的flag设为1 那么就return回来 就是原来没被找过了 现在是正好我要找这个节点 我就把他的flag设为1 然后就说明还是对的 我就return回去了 否则的话 当前这节点flag为0 又不是我要找的 那就说明我碰到了一个 以前没见过的节点 那就说明是不一致的 就return 0 所以我们这个函数check 他的程序结构实际上是一个 查找的过程 他的目的 是在当前T里面去找我们的整数V 在查找过程当中来发现是不是一致的 有了check这个函数之后 我们整个Judge的判别 那就变成是判别这N元素的序列 每一个整数是不是一致的 所以我们去 执行一系列的check的这个 这样的一个操作 所以我们就先读入第一个整数 放到V里面 然后判别当前的这个V 跟我们的树根 T的树根是不是一样的 如果不一样 那就说明我们的这个序列第一个元素 它是作为所对应二叉搜索树的树根 跟我们当前T的树根是不一样的 那么这两个搜索树肯定不一样 那么就return 0 了 否则的话就说明数根是一样的 那么就把当前的 flag 设为1 就说明我要找的这个节点 正好是当前这个节点 就说明这个节点算是被我找过了 那flag就设为1 接下来是对后面N-1个数 逐个的进行check 所以我们的循环从1开始到N-1 每次读进一个整数 然后调用我们前面的那个函数 check(T,V）去检查 V是不是跟T的结构是一致的 如果不一致 就check是不成功返回0的话 那么这个时候就return 0 如果都一致 那么这个循环一遍一遍的做N个数 检查下来 全部都是一致 check都是等于1 这个时候 循环退出来说明是完全一致的 就每一个整数全都一致了 那就return 1 但再仔细分析一下 我们这样的一个程序处理方法 是有问题的 因为我们要连续 L个序列跟T去做比较 如果在处理某个序列的时候 某个数发现是不一致的 那么这个时候 按照我们现在这个程序 这个Judge要ruturn出来的 比方说我们刚才的例子 3是一致的 树根是一致的 2去找 要经过1 说明是不一致了 读完2了之后 发现不一致就return 0 出来了 一return出来了之后 意味着要进入下一轮序列的检查 就会把这个序列的后面几个数 就4跟1当成是下个序列 那这个程序就是有问题了 所以当我们发现某个数不一致的时候 不能马上退出 必须把这一行的其他的整数全部读完 然后才能够退出来 这样保证 下一个序列 是我们真正要求的这个序列 所以这个程序是有BUG的 因为你不能一发现有问题就退出来 必须要把后面数都读完 那么怎么把后面数都读完呢 程序可以改写一下变成这样 我们也可以设个标记 不是节点上的标记 而是整个程序的一个标记 那么这个标记也叫flag 我们一开始把设为0 那 flag 代表什么意思呢 flag 等于0代表到目前为止 处理序列的过程当中 还认为是一致的 没有发生矛盾的情况 当 flag为1的时候 就说明我们已经发现矛盾了 但是这个时候我们不能退出来 程序还要继续运行 要把后面的几个整数都把它读完 程序先读进这个序列 第一个整数把它放到V里面去 然后比较这个V 和树根一样不一样 如果跟树根不一样 我们前面说return 现在不能return 先要把 flag设为1 就代表我已经有矛盾了 如果V跟树根是一样的 那么就这个节点的flag设为1 大家要看到 T flag跟 flag 是两个不一样的变量 一个是节点上的标记 一个整个程序运行的标记 接下来的话一样的是 顺序读入后面N个整数 所以通过scanf读进来 然后去做 check 但是在check之前我们看一下 是不是已经有矛盾了 如果已经有矛盾你check都不要check 就读下一个整数就行了 所以这个if语句实际上就保证了 当我发现有矛盾的时候 我会程序循环会继续做 但是不做check了 没必要check了已经有矛盾了 只要把数读进来就行了 所以当flag等于1的时候 我们!flag !flag就是0 那么他是0的时候 后面check就不做了 当 flag 为0 那么我们去做check了 检查了后结果如果return 0 的话 意味着有矛盾了 我们 flag 设为 1 也就是说 flag 为0 check返回值也为0的时候 那就说明刚刚发现了矛盾 那么这个时候把 flag设为1 所以这个循环就一直做 所以加flag的目的 是当我发现有矛盾的时候 让程序继续做 但这个程序做的工作就是把树读完 不做check 当for循环 把N-1个数都读完了之后 就退出来了 退出来之后我们判别一下 flag 是等于0 还是1 等于1就说明前面发现过矛盾了 就return 0 否则的话就return 1 最后我们讲两个小程序 一个Reset 把一个序列 这个读完了之后我们要处理下个序列 在处理下个序列之前 我们要把 树上每个节点的 flag 标记 全部清为0 所以叫Reset 他的目的 就是把这个树上的 每个节点的 flag 都清为0 那么这个很容易做 左子树清零 右子树清零 总然后把当前节点清零 FreeTree 我们一组数据处理完了之后 要进入下一组数据之前 要把当前这个树 把它释放掉 那么怎么释放空间 一样的道理也是递归 把左子树的空间释放 把右子树的空间释放 然后把当前根节点的空间释放