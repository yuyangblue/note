1 那之所以我们先介绍一个递归的算法 是因为我们觉得 递归算法可能比较容易理解 当然了数据结构学到现在 我相信你已经知道 递归算法在真正实现的时候 其实并不是那么美妙的 因为他要占用系统的堆栈 而且他有很多额外的操作 都让他比较慢 那么规并排序 有没有可能用不递归的算法来 做出来呢 当然可以 而且其实也没有那么复杂 非递归算法的一个基本思路是这样的 我假设在一开始的时候 我一共有n个有序的子序列 每一个子序列里都只含有一个元素 然后下一步我要做的就是 把相邻的两个有序的子序列 做一次规并 于是我就形成了若干个有序的子序列 每个子序列的长度就变成了2 就每个子序列里面包含两个元素 这两个元素之间是有序的 然后我继续规并两个相邻的子序列 又产生了一系列有序的子序列 这些子序列的长度就变成了4 以此类推 一直到最后 我得到一个完整的有序的序列 整个这个算法过程 其实是非常容易理解的 有一点麻烦的就是 我们看看到底要多少额外的空间 如果是照着这个图来理解的话 这个额外的空间复杂度 好像有点恐怖的 是不是 他一共叠戴了多少层 就是每次乘以2 每次乘以2 每次乘以2 一直到最后得到n为止 那么他这个深度有多少呢 这一共是logN对不对 如果在每一层 我要开一个新的临时数组 去存这个中间结果的话 那么我的额外空间复杂度就变成 一个多么可怕的景象了 但是换一个角度讲 真的有必要这么做吗 所以给你时间 仔细的去想一下 我们要用这个非递归的算法 去实现规并排序的话 你能够做到最小的额外空间复杂度是多少呢 想一下再回来 是O(N) 对不对 实际上我们需要每次都开一个临时数组吗 当然没有必要 聪明一点的话 实际上我们只要开 一个临时数组就够了 原来那个A也不是不可以用的 那第一次 我们把这个A 给他规并到临时数组里面去 第二次 我其实就可以把临时数组里面的东西 规并回到A里面去 对吧 然后再把A导到临时数组里头 然后再把临时数组导回到A 那最后一步呢 那要看我们的运气了 如果运气好的话最后一步这个就是A 那我们就完事大吉就直接退出了 如果运气不好呢 那最后这一步可能是那个临时数组他不是A 但是也没关系的 我们最多就是 再把这个临时数组再加一步 导回到A里面去就可以了 所以在整个的执行过程中 他的核心步骤 就是一趟规并 那这一趟规并的伪代码要怎么写呢 我们来看一下 我们把这一趟规并的函数取个名字 叫做Merge_pass 传进来的东西 仍然是原始的数组加上那个临时数组 这个N是整个待排序列的长度 然后多了一个length 这个length指的是 当前有序子列的长度 也就是说 这个length在一开始的时候是等于1的 我们假设每一个数字自己 就是一个有序的子列 然后这个length在执行的过程中 每一次是要加倍的 在我们之前已经讨论过了那个Merge函数以后 这件事情就变得非常简单 那我这一步执行的其实就是从左到右 一对一对的 去调用那个Merge 每次给的是这个 i 是他最左边的位置 然后 i+length 是跳过这么长一段 然后下一段的初始位置 下一段的终止位置 就是 i+2*length-1 每一次循环的时候 i+2*length就是跳过两段 然后去找下一对 为什么这个Merge 我在这写成了Merge1呢 他跟原始的Merge稍微有点不一样 原始的那个规并函数 在规并完了以后 那个有序的结果一定是放在A里面的 虽然我把结果先导到了TmpA里 最后还是要把TmpA导回到A里面去的 但是 Merge1里面我们不做最后的这一步 我调用Merge1的时候 就意味着我是把A中的元素规并到TmpA里面去的 最后有序的内容是放在TmpA里面 那我们来注意一下这个 循环结束的条件 这个循环 不是执行到N的时候结束的 而是执行到N-2*length的时候结束的 为什么会这样呢 因为我们去想一下 前面我们是一对一对做规并的 正好规并完成的一个前提条件 是说你要规并的子序列正好是偶数个 他正好是成对的 如果他是奇数个 那么他最后就会多出一个来 对吧 那么规并到最后的时候 最后两段有序序列的 长度都可能不一样 所以那个尾巴是一件很麻烦的事情 那尾巴另外处理 我先把前面保证都是成对的那一部分 先给他处理完 所以我只处理到倒数第二对 把这个处理完了以后我再看尾巴 在当前的这个i 如果加上了一段以后还是小于N的 那么就说明我最后是不止一个子列 我最后是有两个子列的 那么我继续去调用Merge1 把最后这两个子列 注意到最后一个子列的结尾 无论如何都是N-1 把最后两个子列归并一下 如果这个条件不成立意味着什么呢 意味着在当前 i 这个位置 加上了一个length之后 他就跳到N外面去了 也就意味着最后我只剩下一个子列了 我就直接把剩下的A直接导到这个TmpA里面去 就完了 那这样就完成了我们一趟的规并 然后接下来我们再看我们的那个 原始的统一的接口应该怎么写 我们真正拿出来见人的这个接口 Merge_sort 传进来的是一个原始的A和他的个数N 然后在这里面 我们声明了一个 跟原始数组等长的一个临时数组Tmp 如果我们有足够的时间的话 我们就继续 否则的话 我们就要宣布空间不足 然后在我做完了中间这堆事情以后 我要记得把这个Tmp也给他 free 掉 那这些都是细节问题 最关键的是说 如果我们拿到了这个空间 我们就要开始往里面做 这个Merge pass 在最开始的时候我们假设的 有序子列的长度是等于1 对不对 那我们先初始化一下子序列的长度 然后我们把这个length带到那个 Merge_pass里面去 调用了一次Merge_pass以后 这个A就被导到了TmpA里面 length是当前的这个1 执行完了一次Merge_pass以后 那么当前 有序的子序列他的长度就变成了2 然后我再调用一次Merge_pass的时候 传进去的这个长度就是2 然后接下来我要做的 就是把 前一步的结果是放在TmpA里面的 再导回到A里面去 所以TmpA和A的位置要换一下 前面这个是初始状态 后面这个是规并以后的状态 执行完了这个Merge_pass以后 这个length再次double变成了4 那我在这里头 每一次循环我都做两步的Merge_pass 这样能够保证 当我这个while循环里面跳出来的时候 无论如何 最终的结果都是存在A里面的 哪怕他最后一步 比如说我执行到这一步的时候 他已经完全有序了 那这个时候length就已经等于N了 那也没关系 我多执行一步这Merge_pass 会发生什么事情呢 这个时候在Merge_pass里面执行的 实际上就是 TmpA原封不动的导到了A里面 什么都没有做 然后我们就会自然的跳出 那么这个while循环的条件是什么呢 就是当这个length 还没有达到N的时候 我们就一直在循环 一直到这个length大于等于N的时候 我们就可以跳出来了 那Merge_sort有一个非常好的性质 他是稳定的 我们前面说的这个Merge_sort归并排序 有各种各样的好处 包括他的平均复杂度是NlogN 最坏时间复杂度也是NlogN 而且他还是稳定的 他这么好那么好 千好万好有一点不好 就是他需要一个额外的空间 并且 他需要 在那个数组和数组之间 来回来去的去复制 导这个元素 这是他最大的缺点 所以在实际应用中间 我们这个Merge_sort 基本上不被用于做内排序 就如果我们所有的元素 都可以在内存里面完成 这个时候 没人用规并排序去做这件事 规并排序在什么情况下特别有用 就是在外排序的时候 他是一个非常有用的工具