那我们前面知道了邻接矩阵呢 在表示稀疏图的时候 是很不方便的 他比较浪费 不仅浪费空间而且浪费时间 为了解决这个问题呢 我们就有了另外一种表示 法叫做邻接表 邻接表是一个链表的集合 我们首先定义一个指针数组 对应每一个节点1 我们定义一个指针 然后对应矩阵的是我们把每一行 非零元素存成一个链表 这样如果有大量的0的话 我们那些0全部都不会出现在链表里 想通过这个来省一下这个空间 于是我们再来看一个例子 还是我们刚才的这个小城镇 你要怎么用邻接表去存他呢 在这暂停一下自己回去写一写 然后我们来对答案 看我们答案呢 是这样的 首先我要为每一个节点开一个指针 这是一个指针数组 我一共有10个节点 所以这边就是对应了 一个长度为10的数组 数组的元素是一个头指针 0这个顶点跟1相连跟3相连 所以呢他就串起来这么一个链表 是由1和3组成的 那我们再看一一呢跟0跟3 跟跟二跟五全都是相连的 所以在他对应的这个链表上就会有 四个元素 以此类推啊 我们就形成了这么样的一个邻接表 来表示这个图 那一个要注意的呢 就是邻接表的表示法是不唯一的 因因为在这个一串上面 这几个节点以什么顺序出现 其实都是无所谓的 我们不讲究这个顺序 但是你要注意一点 如果你仔仔细细的去数一下 这个图一共占了多少空间的话 你说他真的很省吗 在我们那个邻接矩阵的表示法里头 每一条边是占了一个整数的位置 在这里头每一条边是怎么存的呢 如果5到9之间有一条边的话 那么5这个链表上会有9 9这个链表上也会有5 所以一条边必定是被存了两遍 而且在存一条边的时候 我们不仅存了一个整数 我们这个整数是那个顶点的编号 不仅要存这个顶点的编号 还要有指针指向下一个 所以等于一条边占差不多两个整数 而且对于网络来说 结构中还要增加一个权重的域 我们想想这件事情在 邻接矩阵里面是怎么解决的 其实我们在邻接矩阵里面 是只要把那个一变成那个权重就好了 对吧所以呢 如果用邻接表的话 这个是一定要够稀疏才合算 我们来看看 邻接表的表示法都有什么特点 首先呢 他是非常方便来找任意顶点的 所有邻接点的 我要找 vi 的所有邻接点 我只要 顺着那个 gi 的链表一直走下去 就把他所有的邻接点都访问到了 没有任何不必要的零点 需要访问 当然我们做的一个原因就是为了节 约这个稀疏图的空间 但是我们要仔细的去看一下 整个这个邻接表用到了多少空间呢 我们需要 n 个头指针 然后我们每条边存两遍 所以也就需要2E个节点 每个节点呢 如果不考虑权重的话 至少是需要两个域的 所以我们的问题就是 到底这个图要多稀疏 才是合算的 想一下再回来 好我们下一个功能呢是说 我们需要计算一个顶点的度 那么邻接表是不是方便计算 任一个顶点的度呢 这个问题是 yes and no 这取决于我们在说一个什么图 如果对于无向图来说 是的 我们只要访问那个顶点所代表的 那个链表 然后数一下这个链表上一共 串了多少条边 我们就知道这个度是多少了 但是对于有向图来说呢 这就是一个比较麻烦的事情了 因为串在 vi 对应的那条链表上面 只是从 vi 往外指出去的那些边 所以呢我们只能计算出度 如果还要计算入度的话 那我们应该怎么办呢 要构造一个逆邻接表 正邻接表存的是邻接矩阵的每一行 存成了一个链表 逆邻接表呢 就是把邻接矩阵的每一列 再存成一个链表 那相应的这一列存的就是 指向他自己的边 那这样才能够比较方便的计算入度 最后一个问题呢 是如果我们想检查任意对顶点之间 是不是存在边呢 这个对于邻接表来说很不幸 是一个非常麻烦的问题 在这还有一件事情 要跟大家讲明白的是 虽然我们给大家介绍了两种 表示图的方法 大多数的教科书上也都介绍的是 这么两种表示的方法 但是这并不意味着说 表示一个图 只能用邻接矩阵或者邻接表 不是这样的 实际上对于图的表示 有很多种五花八门的方法 他取决于你要解决的那个具体问题