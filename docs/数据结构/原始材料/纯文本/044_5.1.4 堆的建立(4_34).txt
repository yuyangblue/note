1 那么接下来我们再看 一个蛮重要的一个问题 最大堆的建立这个问题 最大堆很有用 我们后面会讲到了 在排序里面我们可以用堆排序 就是在堆上面做 而排序里用堆来做的 首先要形成一个堆 所以必须要考虑给你 n 个元素 怎么样子把它建成最大堆 这是所有堆应用你的前提 怎么建堆呢 有一种很简单的方法就是不断的插入 一开始堆是空的 插进去一个 再插进去一个 再插进去一个 做 n 轮循环 每次插入 它的时间复杂性是Log2N 我总共循环 N 遍 所以整个时间复杂性呢是Nlog2N 其实这个方法效率不够 我们可以找出更加好的方法 这个方法呢是线性时间复杂性 就在O(n) 时间之内 可以建一个堆 他的策略是怎么做呢 分两步第一步把所有的元素 先按照顺序放在一起 也就是先把我们N个元素 形成一个完全二叉树 接下来继续调整 以满足我们讲的有序的 这样的一个要求 下面我们来看一下具体怎么做 这总共有12个元素啊 这12个元素呢在数组里面顺序放 那么从下标为1的地方开始放 N个数顺序放 就形成了一个完全的二叉树 接下来我们要不断的去调元素 把它调成一个堆 怎么调 调的这个方法呢 跟我们前面删除的操作是很像的 删除是这样的 这是堆顶的元素 然后呢 他的左子树是个堆 右子树呢也是个堆 要把这个元素删掉 元素删掉我们怎么做 是把最后一个元素 替换过来替换到这个位置 好了大家注意 当把根顶的元素拿掉 我们用最后一个元素替换过来的时候 我们发现了什么 是不是 这一个是个对 这个是个堆 然后我来了一个新的元素 怎么把它调成一个堆 所以在堆删除里面 最核心的一种操作 实际上是已知左边是个堆 右边是个堆 我来了一个元素怎么把它调成一个堆 调的方法呢 就是跟下面左右儿子里面去比较 然后调一个上来 这就是他的一个基本的 这样的一种思路 我们能不能把这种思路用在建堆 这里面 如果对根结点来讲 79来讲 目前的情况左边不是堆右边不是堆 对66来讲 左边可能也不是堆 右边也不是堆 那怎么办这个过程怎么做呢 我们有办法 我们从底下开始做 底下谁开始了 这个位置 倒数第一个 有儿子的结点开始 对这个结点来讲我们是不是 它左边是个堆 因为第一个有儿子的节点 左边一定是只有一个儿子 如果有右儿子的话也只有一个 也就左右最多只有一个儿子 一个儿子 那么这儿子本身当然是个堆所以的话 这个是个堆 我们用刚才 delete 的过程的策略 就可以把它调整成这个堆了 再看他 又把它调成堆 再看它 又把它调成堆 然后到了这一步的时候 我们看它已经是堆了 它也是堆了 所以用同样的策略又调整堆 对它来讲也一样的 这个是堆 这个是堆 我们又把它调成堆 到79的时候 左右两边已经是堆了 用刚才这个策略了 又可以调整堆 所以这就是我们建堆的基本过程 从这开始 建成堆 接下来考虑它建成堆 再往前考虑 就每一步就往前考虑一个结点 这些接点是连续放的啊 87的前一个是72 72的前一个是83 再调 又调整堆 然后 再前一个就到了 事实上这个位置了再调 好再前一个就66这个位置了再调 现在到根结点了 左右里面挑一个大的 91放上去 然后79到这个位置的时候呢 在左右里面挑大的是83 再跟79一比较又比它大 那么83再上来 到79到这个位置 然后呢左右两边再挑大的66 66比不过79 那么就好了 所以这个呢 整个呢就建成了一个堆 线性的时间 到O(n)的时间 就可以完成建堆的过程