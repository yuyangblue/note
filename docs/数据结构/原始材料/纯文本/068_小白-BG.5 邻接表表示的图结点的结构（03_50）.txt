1 那么接下来我们再看怎么处理 用邻接表表示的图 那所谓邻接表就是一个指针的数组 这个数组里面的 每一个指针对应着矩阵 每行一个链表 只存非0的元素 那这个是一个邻接表表示的图的例子 那么这一串里头比如说这个G[2] 后面串的这个链表就表示 就有一条边是从2到1 从2到5 从2到4 如果我们再说无向图的话 那么一条边会被存两次 比如说如果从1到5有一条边的话 那么从5到1也应该有一条边 当我们在定义这个图的节点的时候 其实大部分内容都跟前面是一样的 就是他也得有顶点数 他也得有边数 然后他这边有一个 Ptr 然后把这个邻接表方式存储的图类型定义为 指向这个节点的指针 最关键的就是这块地方是不一样的 在前面 这块地方的核心是那个邻接矩阵 那在这个地方 核心就应该是这个邻接表 那我仍然把这个邻接表的名字取为G 这个AdjList 是这个英文邻接表的缩写 那这个类型的是要我们自己去定义的 他实际上 是一个数组 这个数组的大小 就是我们一共有多少个顶点 这个数组就有多大 所以他是MaxVertexNum 那么他的每一个元素 都是一个节点 这个节点我们取个名字叫Vnode 这个头节点里头应该包含什么东西 首先他要包含一个指针 指向这么一个边的节点 那我们把这个边节点的指针的类型 定义为这个 PtrTo 指向一个AdjVNode 这个是边节点的类型 等一下我们再定义 把这个指针我们命名为FirstEdge 也就是第一条边 当然你可以根据自己的喜好 给他取另外的名字 那在这 我们把它叫做FirstEdge的原因是说 他总是指向 他这个链表里面的第一条边 如果顶点里头没有要存其他的元素 那么这样就可以了 如果在有的情况下每一个顶点 还要存一堆乱七八糟的东西的话 那么这个乱七八糟的东西 就把它取个名字叫 Data 存为这个 DataType 那跟前面一样 这个 DataType 有可能是整型 可能是double类型 也可能复杂一点是结构体 接下来 我们再来看这个 AdjVNote 就是这个链表里头代表每一条边的这个节点 它的结构它应该包含什么东西 虽然说他代表的是一条边 但是他需要存两个顶点吗 想一下 好像是不需要的 对吧 因为他必定是 放在某一个顶点的链表里 那么 这条边的出发点就已经决定了他在谁家的链表里 谁就是他的出发点 所以在这条边的节点里面 我们只需要存他的终点 也就是这个顶点他的邻接点 所以我们把它取个名字叫邻接点AdjV 他的类型是邻接点的下标 Vertex 这个 Vertex 还像前面一样 我们把它定义为整型 除此之外 如果我们是在说一个有有权图的话 还得有一个变量来存他这个边的权重 然后 这是一个链表里面的一个节点 所以他还有一个指向 下一个节点的指针 到此为止 我们就完成了用邻接表表示的 这个图的 所有结构的定义 那接下来就是来建立这个图