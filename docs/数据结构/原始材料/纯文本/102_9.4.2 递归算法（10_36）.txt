1 归并算法在谈到具体实现的时候 他其实有两种不一样的实现的策略 那我们先从比较容易理解的这个递归算法 开始介绍 归并排序的递归算法 就是一种非常典型的分而治之 这个策略的应用 什么叫分而治之呢 我们其实之前也讨论过 就是假设我们待排的序列 是放在这么样的一个数组里头 那所谓分 就是我先把它一分为二 然后我就递归的去考虑问题 我递归的去把左边排好序 我在递归的去把右边排好序 这样我就得到了两个有序的子序列 而且他们肩并肩的放在一起 最后一步呢 就调用我们前面那个核心的 规并的算法 然后把它规并到一个完整的数组里面去 这就是分而治之这个策略的一个 典型应用 这个算法的伪代码实现 也是非常简单 要注意到 我们这头又传进来了一堆参数 这是我们原始的待排的 这个元素的数组 然后另外 我们仍然是传进来了一个临时的数组 这个L指的是什么呢 L指的是 这个位置 就是当前待排的这个序列 最左边的这个位置 RightEnd的指的是当前待排序列 最右边的这个位置 然后我需要一个变量 叫做Center来记录他中间的位置 因为我要把它从中间一分为二 那么当某种条件满足的时候 一会我们再说什么条件满足的时候 我们计算了一个终点的位置 然后我就递归的去把从L到Center的左半边 做了一个递归的排序 然后接着 把Center+1一直到RightEnd 这个右半边又递归的做了一个排序 两边都排好序了以后 我调用我刚才的Merge 调用Merge的时候传进去的是什么 一个是原始数组A 一个是临时数组TmpA 还有一个是左边的起始点 这个是右边的起始点 这个是右边的终点 我们就完成了整个的排序 那这个排序出来的结果 会存在这个原来的数组A里面 回过头来看 这个当什么条件满足的时候 我们才做这件事呢 或者说当什么条件不满足的时候 我们就不做这件事了呢 其实很简单 就是当我们待排的这个序列里面有元素的时候 也就是说 当L小于RightEnd时候 我们才做这件事情 递归到最底层 就是当L等于RightEnd的时候 就意味着 你要排的这个 数组这一部分就只剩一个元素了 就什么都不用排我直接返回去就好了 那我们对于那些 用分而治之的策略来执行的算法 做时间复杂度分析 也不是第一次了 对吧 那这是一个老套路就是说 假设我们解决整个问题用的时间 是T(N)的话 那么我们递归的去解决 左半边用的时间 就一定是T(N/2) 因为数据的规模减了一半 那么递归的去解决右半边用的时间 也是T(N/2) 然后我们还需要执行一步 就是一个Merge就是一步的规并 那这一步规并的时间复杂度 之前我们也分析过了 这是一个O(N)数量级的算法 于是我们就得到了一个递推的公式 也就是说T(N)是等于 左边的 T(N/2) 加上右边的T(N/2) 加上一个O(N) 然后我怎么往下推呢 这个我就不重复了 之前我们推过一次了 对吧 从这个递推式我们就可以 得到最后的结果 知道T(N)=O(NlogN) 那注意到这个NlogN是非常强的NlogN 他没有最好时间复杂度 也没有最坏时间复杂度 也没有平均时间复杂度 任何情况下他都是O(NlogN) 另外 规并排序 还有一个非常好的性质就是 他是一个稳定的算法 那这个算法到这 是不是我们就说完了呢 等一下 还没有完 我们在最开始的时候有一个约定 我们在讨论任何排序算法的时候 他都有一个统一的接口 要叫什么什么sort 然后他传进来的参数 不能是这么一堆乱七八糟的东西 他传进来的参数只能是原始的数组A 加上这个数组里面元素的个数N 所以这个MSort的函数是不满足这个条件的 于是呢 当用户在调用一个排序算法的时候 他会发现你这个很不友好 你必须要给他一个非常友好的 统一的函数接口 那这个接口要怎么做呢 首先我们得把它写成一个Merge_Sort 就是规并排序 然后传下来的参数就是原始的数组A 以及这个元素的个数N 那我们要记得在之前我们不管是Merge这个函数 还是MSort这个函数 我们都始终带了一个TmpA 那这个临时数组是在哪里声明的呢 就是在这个接口函数里面声明的 在这我根据这个N的大小 声明了一个 跟他同等大小的一个临时的数组 如果这个空间能申请的到的话 那么我接着往下做 否则的话呢我要跳出来说 空间不够了 那如果我可以顺利申请到空间的话 我们就调用这个核心的 MSort这个函数 那MSort这函数 初始的时候我要给 这两个参数应该是什么值呢 这两个参数是什么 一个是待排序列最左边的位置 一个是待排序列最右边的位置 所以最开始的时候我应该传进去的是 从N到N-1 那最后呢 作为一个专业的习惯良好的程序员 我们不要忘了 要把这个临时数组的空间给释放掉 在这呢 我不知道你有没有对一个问题 感到很疑惑 就是 为什么我这个MSort要一直带着这个TmpA呢 在整个程序的执行过程中 我在什么地方才真正 用到了这个TmpA呢 其实我在MSort里面 他只是一个递归的调用 真正用到TmpA是在那个Merge 我们核心的那个规并函数里面 才用到这个TmpA的 为什么这个TmpA要在 最外面的这个函数声明 然后每一次都把它作为一个参数往里传 看上去很啰嗦的样子 为什么我们不直接在Merge函数的内部 去声明这个TmpA呢 那我们要仔细的看一下这个TmpA 我们到底是怎么用的 一开始的时候我们有这样的一个数组 然后我们在Merge_sort里面 刚一进去的时候就声明了 另外一个等长的数组 然后我进入了这个MSort 把这个问题一分为二 就开始递归的去解决这个问题 然后继续递归 然后继续递归 那么在递归的过程中间 我实际上是用到了 比如说我在解决这个子问题的时候 我用到的是这个数组的哪一段呢 我实际上是把中间的那个结果 通过那个Merge函数 把它规并到了这个临时数组的这一段 然后再把这一段的内容导回到 这个A里面去 然后我继续的递归的去解决这一半 那也是用到临时数组的这一段 然后再把这一段 导回去 然后再把这两段东西规并的放到 临时数组的这一块 然后再把这一块的内容导上去 我们从始至终都是在 同一个数组的某一段上面 反反复复的 做这件事情 从头到尾我这个malloc只调用了一次 然后整个程序执行完了以后 这个 free 也只执行了一次 如果我不这么做会怎么样呢 如果我只在Merge中间声明 一个临时数组 Merge函数的这个接口 就可以写的更简单一点 MSort这个接口也写的更简单一点 那个临时数组就不出现 在这个传递的参数里头了 我只有在做Merge的时候才会去声明 一个 如果我这么做的话 在执行程序的时候 你的机器里面到底在发生什么事情呢 这是我的初始状态 然后 我把问题一分为二 然后继续一分为二 继续一分为二 然后再继续一分为二 那假设说这已经是我分到的 最小的单元了 我在MSort里面执行了一次Merge 我在执行Merge的时候 临时开了这么一个数组 开完了以后倒腾了一下 我把它给free掉了 然后我去递归的解决这一半 解决这一半的时候我又声明了 这么样的一个数组 执行完了以后又把它free掉 然后再退回到这层递归的时候 我又在Merge里面 临时声明了这么长的一个数组 我需要把这两段给他归并进来 然后做完了以后又把这个free掉 然后我去递归的解决这一边 那又是重复的 开了一个关了一个 开了一个关了一个 然后在这两段分别执行完了以后 我在这个递归程序里面 又要去开这样的一个临时数组 对吧 于是同样的我要去解决这边的问题 解决完了这两个问题以后呢 我又要去开一个这样的连续数组 同样的方法要去解决这一半 然后最后还是要开一个大的数组 去把这两边的元素 规并到这个数组里面 然后再把这个数组里面的东西 再导回去 所以我们看到我们额外的空间复杂度 如果我每次都记着去释放空间的话 那么额外空间复杂度 倒是O(N)这个数量级的 但是你要看看我们 malloc free  malloc free malloc free多少次 这么做实际上是不合算的 最核算的做法就是在那个 一开始的时候就声明一个数组 然后每一次 我只把这个数组的指针传进去 然后只在这个数组的某一段上面做操作 就不需要重复的 malloc 和 free