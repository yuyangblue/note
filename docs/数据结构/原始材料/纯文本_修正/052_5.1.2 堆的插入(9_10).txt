1 下面我们来讲一下 最大堆的操作是如何实现的 首先我们来看一下 堆 这个数据结构我们怎么定义 堆我们可以把它定义成一个结构 因为我们是用数组来存储 一个完全二叉树 所以它里面一定 包含有一个数组的指针啊叫 Elements 有一个分量来指示这个数组大小 另外呢当前堆的大小呢 我们也需要有个变量啊 这就是我们的这个Size 我们想建堆的时候呢 就是要建立这样的一个结构 所以我们先通过 malloc申请一块空间 这样结构的一个空间然后覆盖 H 那接下来呢 对这个结构里的三个分量 分别进行赋值 Elements本身呢他 将来是要指向一个数组 所以我们再申请一块数组的空间 空间的大小是多少呢 就是我们希望的这个堆的容量 加上1 MaxSize+1 为什么要加1呢 因为我们堆 存放是从下标为1的地方 开始存放的 下标为0的我们是不存放堆的 正式的元素的 所以是 MaxSize+1 然后一开始建堆的时候呢Size=0 整个堆的容量呢 就是我们的MaxSize上 因为我们堆 是从下标为1的地方开始放 0就空在那边没用 没用呢我们这里就赋一个最大的值 对最大堆来讲我们赋个最大值 我们将来用做岗哨或者叫哨兵 这个后面我们再来解释 那么有了这样的几个 我们来看一下相应的操作怎么实现 最主要两个操作插入跟删除 我们先看插入怎么做 因为堆我们把它 看成是一个完全二叉树 所以它在数组里面是从1开始进行放 现在对于5个元素 也说数组下标从1到5 是我们对应的5个元素 下面我们想插入一个元素了 想插入一个元素最自然的一种想法 他的实现方法 就是把这个元素放在 数组的最后一个位置 现在我们是前五个元素 所以新插入元素呢 很自然就想要放到第六个位置 放进去之后 按照我们完全二叉树的这样的一种 一个规则 所以它的父亲呢就相当于是 3号位置就31这个位置 如果我们插入一个20 把20放在下边为6的这个位置 有没有破坏有序性 没有的 每个结点 仍然是它所在子树的这个最大值 我们插入是35 这个时候呢它是一个完全二叉树 但是它的这个有序性被破坏了 我们要求有序性是什么呢 是要求所有结点 它都是它所在子树的一个最大值 显然这个不是 31它所在的子树是这个 但最大值是35 所以这个时候怎么办 换位置 两个位置一换就可以了 好这就满足了我们所需要的要求 假令说我们插入一个元素58 就是58我们仍然是放在 树这个位置啊 这个位置是31 那么58比31来的大 所以两个人呢就换位置 换完之后呢58 到了这个位置 接下来还是不行 这个根结点应该是所有树里最大的 那58比它大 所以再换位置就变成这样的一个结构 然后从这里面我们再仔细观察 58 20 50递降的 58 44 31递降的 可以发现 原来这条线从根结点开始是递降的 你现在往这个末尾插入了某个元素 那么这个递降呢 这个性质呢被破坏了 所以就要调整 要个父结点去调整 一直调整到 你插进去之后仍然是有序的 从大到小这样的一个序列 那我们来看看这个算法的 这样的一个实现 我们有一个堆叫H 这是个结构 然后来了个Element 那我们就怎么做呢 判别堆是不是满了啊 堆是不是满了就这个Size 跟它的容量 是不是相等 相等的嘛就满了 如果这个堆不满 不满的话 我们就把这个元素呢放到堆的后面 后面哪个位置呢就Size+1的位置 因为Size原来最后一个位置 下面我们要想插入一个元素 所以的话就是放的位置就是Size+1 所以呢 我们就通过++Size的赋给 i 的形式 一方面既把Size加了个1同时的话 i 又是我准备要放进去的位置 我放进去了之后 满足了这个完全二叉树 这样的一个结构 但是有序性是得不到满足的 怎么保证它的有序性呢 所以我们要跟父结点去比较 发现父结点不对的时候呢 要跟它换个位置 然后再跟父结点去比较 发现不对呢再换再换一个位置 所以这个i 用来只是我当前准备要放进去的位置 那么它的父结点是谁呢 父结点是i/2 所以如果i/2就父结点 比我来的小 意味着我要放上去了 你父结点要挪下来了 就是i/2的位置 把它拷贝到i的位置 这句话就父结点把它挪下来 接下来继续比较 怎么比较呢 就是我到父结点的位置 就是这这句话呢就是 i 等于 i/2 就是 i 到父结点这个位置了 所以就通过这样的一个循环 每次跟父结点去做比较 这个for循环的条件呢 就是我当前这个结点和父结点去比较 如果我比你大 你就下来 下来怎么做呢 这 i/2 赋给 i 这个位置 然后完了之后呢 再下一轮循环 我自己跑到父结点这个位置 就是 i= i/2 啊 就这样子一直循环一直做 做到前面父结点比我要来的大了 那好了就是我就不再往上调了 那我就做进去了 这里有几个技巧 第一个技巧 我们说一直要跟父结点去比较 如果我这个元素 是所有里面最大的 意味着我一直放放放放 要放到头上去了 打个比方说我们现在的话 这个树根在这个位置 树根是20 然后他的右儿子是5左儿子是2 那么就形成这样的一个树的结构 这是有序的 从大到小 这样的顺序 然后呢如果我们在这边 比方说我们堆的最后一个元素 在这个位置 我们插入一个元素如果等于25 25要插在这个序列里面 按我们刚才的这个过程 就是把25跟6去比较 发现6比它小 6挪下来 然后跟8去比较发现 8比它小 8挪到这个位置 这个12去比较 12比它小 12挪到这个位置 在这个挪的过程当中 每次 i 除以2除以2除以2 所以一开始是这个位置 然后除以2到这个位置27 这个是代表它数组中的下标 27/2 取整数 13 再除以2 到6 再除以2 到3 再除以2 到1 所以整个循环里面呢 这个 i 呢 在不断的变化 原来指这个位置 i 不断地往前挪 然后每次比较的话 都是把25就是现在的item的值 跟 i/2 当 i 在这个位置的时候 i/2是这个位置 i 在这个位置的是 i/2 这个位置就是跟它的父结点去比较 如果这个条件满足了 就是前面比方说我们插入的是9 插入的9 那么跟它比较 那么6挪过来跟8比较 8挪过来 跟12比较 发现你12比我大 那么我就放在这个位置了 但是如果我们要插入的这个数 是所有这个数里最大的 意味着这个比较过程会一直往前走 那么到了这个位置 25跟20比较 20比它小 20挪过来 那么这个时候怎么办 按照我们的循环 还要继续往前比 而这是数组的第一个位置 再往前呢就是0换位置了 已经在堆在外面了 所以的话一般情况下面 你做这个循环的时候呢 还要再加一个条件 如果 i 是 大于1的时候 那么这个循环一直做一直做 等于1的时候就不要做了 已经是到顶上了直接放上去 所以一般来讲这个循环呢 就除了这个条件之外 除了我们这个条件之外 还要再加上个 and i > 1 那么我们这里为什么不弄 i >1呢 因为我们在数组下标为0的位置 放了一个哨兵 是 MaxData 是最大的值 是比这个所有值都要大的值 打个比方说是1,000 所以的话任何一个数25 一直比一直比比到后面 它要再跟父结点比的时候就是 i等于1 i/2等于0 跟它一比的时候 比不过它了 25自然放过来了就放到这个位置 所以这是个哨兵就挡住了 你不会去 到了这一步就不会 再往前比了 所以 这样的一种处理方法呢我们这个 and 的条件可以去掉了 用不着判别 i 到底是不是大于1 了 这使得我们程序效率能得到提高 这就是哨兵的一个作用 这时我们的插入它的时间复杂性呢 是O(log N) 就是树的高度