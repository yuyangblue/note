1 我们这道题呢是关于Huffman Codes 那 Huffman Codes最大的问题 就在于这个Huffman编码是不唯一的 我们来看一个非常简单的例子 比如说我们有4个字符 它的频率分别是1122 按照Huffman算法呢 我们把两个频率最小的先合并 然后呢再跟频率次小的去合并 最后再合并最后一个字符 那么就得到了这样一棵Huffman编码树 根据这棵树呢 那这两个频率为1的字符 他们的编码长度分别是3 然后这个字符呢编码长度是2 这个是1 最后我们根据Huffman的结论呢 这样得到的一个编码的总长度 一定是最优的 但是呢像这样的一棵树 他对应的编码并不是唯一的 一个最简单的不唯一的例子呢 就是我们把这棵树给他整个的 镜像的反一下 换言之呢 如果我们把向左的分叉都定义为0 向右的分叉都定义为1的话 那么这就相当于我把编码里面的 0和1整个对调了一下 得到的编码仍然是 另外一套哈夫曼编码 那么除此之外呢比如说这两个字符 虽然他们的频率相等都是1和1 但是这个字符的位置是可以交换的 比如说我原来这里是 a 这里是 b 那我可以这里放 b 这里放 a 我又得到一套不一样的编码 但是他们都是最优编码 那么除了这个编码里面 0和1会互换之外 还有一种 就是这个树可能长得也完全不一样 比如说我在合并了1和1之后 得到一个新的结点 他的权重是2 那这个时候呢我有三个2 那我可以把这个2和这个2去合并 也可以把这两个2去合并 那我如果先把这两个字符的2去 合并的话呢 我就得到了一个等长编码的结果 这个等长编码仍然是一套哈夫曼编码 他仍然是最优编码 这些呢都说明哈夫曼编码是不唯一的 那另外还有一个额外的问题是说 Huffman同学在他的博士毕业论文里面 证明了用他的算法 得到的编码一定是最优的 但是呢这个命题反过来是不成立的 最优编码不是 只能通过哈夫曼算法才能得到 最优编码 还可以通过其他的方式来得到 你能想一个反例出来吗 其实这个反例很现成啊 我们还是来看这四个字符 既然说等长编码也是最优编码的话 那么 我把这四个字符怎么放都是没关系的 我可以先合并1和2 再合并另一个1和2 然后我得到的这个等长编码 跟这个等长编码最后的效果是一样的 所以呢最优边码 还可以通过各种其他的途径得到 说到这呢 如果我们把这道题的意思理解为 仅仅要判断他是不是 Huffman Codes 可能不太准确啊事实上呢 我们要的是 只要他满足最优编码的条件 就都是正确的 那接下来呢我们就来看一下 在写程序的时候 我们读进来一段编码 要判断他的哪些特点 首先这个学生提交的编码 他一定要是最优编码 就意味着他的编码的总长度 叫 Weighted Path Length 一定是最小的 那么你要想了一个最简单的情况是 我把每一个字符都给他 或者是0或者是1就一个编码 行不行呢 这样的话我整个加起来那个总长度 不是肯定是最小的吗 但是呃那个不行的 因为你还要考虑到解码对不对 所以呢 我们这个编码一定得是无歧义的解码 那么何老师已经教过大家了 这样的码呢就是前缀码 那前缀码对应到一棵二叉树里面 就意味着 每一个字符 都一定要放在这棵树的叶子结点上 如果有任何一个字符放在了 中间的内部结点上 那就意味着一定存在另外一个字符 他的编码会经过这个字符的编码 也就是说 这个字符就会成为另外一个字符的 前缀编码 这个解码的时候就会有歧义 当然了何老师还告诉过大家 Huffman树他是没有度为1的结点 但其实呢 这一条在程序里面是不需要判断的 因为如果一套编码 他满足第一和第二的话 他一定没有度为1的结点 为什么是这样呢 想一下看 如果不是的话 那我们用反证法 如果在这棵树里面 存在一个度为1的结点 那么这个结点 假设他有这样一棵子树的话 他肯定不是叶子结点对不对 于是呢 在这个结点上一定不会放真正的字符 那么他的这个一棵子树下面 一定是有叶子结点的 那些叶子结点是对应着真正的字符的 那么 如果我们把这个结点给它去掉的话 会有什么影响呢 如果我们把这个结点去掉 并不影响 其他的编码 仍然保持为前缀码这个特点 因为所有的字符还都在叶子结点上 那么他的原来这棵子树 上面所有的叶子结点 他们所有的编码都会缩短一个单位 于是我们就得到了一套更短的编码 但是呢 如果我们还说他的条件一是满足的 他本身已经是一个最优编码了 那么这是不可能的 我们应该不可能得到更短的编码 所以这就是一个矛盾啊 于是我们用反证法证明了这一点 所以当我们写这个程序去实现的时候 只要我们能判断两个条件 第一他的编码长度是最优的 第二他的所有的真正的字符 都是落在这棵树的叶子结点上 能判断清楚 这两点我们就能保证说 ok 这个编码是对的 当然我们注意到 如果要这样写程序的话呢 首先 我得知道最优编码长度到底是多少 就是我先得有一个标准答案 我再把学生的答案往那个标准答案 上面去套 那么要求这个标准答案呢 我就必须要去建一棵哈夫曼树 那么有些有小聪明的同学 要想个办法把这件事情绕过去 他不想建那棵哈夫曼树 那怎么办呢 他就想通过判断2和3 来决定这个编码是不是对的 但是你要知道 同时满足2和3的树 可不一定是我们要的 那个有最优编码的那棵树 你能举一个反例出来吗 想一下再回来 不管你想不想得出反正我肯定想得出 我们看一个这个非常简单的例子 比如说我们就有三个字符 它的频率分别是1、2和2 那么我们构造了这么两棵二叉树 看上去呢 好像都是满足2和3的 首先他们的字符都是在叶子结点上 没错吧所有的结点要么是叶子结点 要么度都是为2的 这个3也没错吧 但是你看一下这两棵树 他都是最优编码树吗 这棵树的编码长度是多少 这是2乘以2 加上2乘以2 加上1乘以1 它的总编码长度是等于9 而这棵树呢 它的长度应该是 1乘以这个长度是2 然后2乘以2 加上2乘以1这个长度是8 那事实上呢 最优编码的总长度应该是8 而这棵树呢虽然满足条件2和3 但他不是一个最优编码的树