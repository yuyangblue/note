你好！我是浙江大学何钦铭 下面由我来做第二讲：线性结构 线性结构是数据结构里面最基础、也是最简单的一种数据结构类型 其中典型的一种叫“线性表”。那么什么是线性表呢？ 下面我们举一个简单的例子给大家看一下 我们这个例子是关于一元多项式的表示的问题 大家知道，一元多项式的基本形式，是a0加上a1x，一直加…… 其中每一项是叫 ai x^i 这样的一个多项式，它的基本运算就包括两个多项式 相加、相乘、相减等等这些运算 我们就碰到了一个问题：怎么用程序设计语言来表示这样的一个多项式 以及实现相应的操作 首先，我们来考虑一下，多项式如何进行表示 在程序设计语言里面，要表示一个问题 首先我们要分析一下，这个问题的关键数据、关键信息在哪里 对多项式来讲，它的关键信息主要有这么几个 一个是它的项数，包括它的最高指数 另外一个呢，我们很重要是要表现多项式的每一项 而多项式的每一项的关键信息，又是每一项的系数和指数 多项式的表示有很多方法 其中最简单的一种方法就是我们称之为“顺序存储的直接表示” 首先我们可以用个数组来表示多项式的有关信息 多项式的每一项我们可以用一个分量来表示 这个分量主要表示每一项的系数。那么指数怎么表示呢？ 指数我们可以对应于这个分量的下标 所以，比如说我们用a这个数组来表示一个多项式 a[i] 就可以表示相应的项的系数，而这个 i 就是对应的指数 所以用这样的一种表示方法，打个比方来说 我们有这样一个多项式，叫 4x^5-3x^2+1 我们可以很直截了当地用这样一个数组来表示 数组分量有 6 项，其中下标为 0 的这一项，它的值是 1 是代表指数为 0 的那一项的系数 然后下标是 1 的这一项，它的值是 0，代表 x^1 这一项，系数是 0 所以我们就可以很简单地用这样 6 个分量 来表示我们 3 个项所组成的这样的一个多项式 酱紫表示方法是非常简单的，而且它做的有关的一些操作也容易实现 比方说多项式相加，它就变成两个对应数组的对应分量的相加 但是这里面也有一些问题，比方说我们看这个例子 我们这个多项式是 x+3x^2000 我要用多大的数组来表示这样的一个多项式呢？ 显然我们至少要用 2001 个分量的这样的一个数组来表示 而这2001个分量，我们可以看得到，其实只有2项是非零的 其它全是 0。这样的一种表示方法显然会造成空间的巨大浪费 而且做加法运算的时候，你怎么说也要做个循环 从 0 开始一直做到 2000，实际上很多计算是在加无效的 0 所以这种表示方法虽然方便，但是也有很大的问题 所以我们再想，有没有可能用更好的方法 为什么要把所有零项、非零项，系数为零的、非零的，全都要表示进来呢？ 有没有可能我只表示非零项？所以这就引入了第二种方法 它的基本思路是这样的：我只表示非零项 那么非零项有什么东西？ 每个非零项实际上有两个信息：一个是系数，一个是指数 所以我们可以把多项式看成一个由系数跟指数二元组组成的一种集合 所以我们也可以用数组来表示。那么数组的每个分量是什么呢？ 跟前面就不一样了 —— 前面的每个分量是系数 现在我们这里的每个分量就不能简简单单是系数了，而且要包含指数 那用什么样的数组呢？可以用结构数组 所以我们可以用结构数组来表示这样的多项式 数组的分量是由系数和指数所构成的这样一种结构 它对应着我们一个非零项 这样的一种表示方法，举个例子来说 我们现在有两个多项式，一个P1，一个P2 一个有3项，一个有4项 我们可以很简单地用这样一种数组来进行表示 比方说这个，它就是一个结构，它是第1个数组的第0个分量 同样的，第二个多项式，第2个数组的第0个分量也是一个结构 用这样的一种表示方法，显然我们只要表示非零项就可以了 很多零的项，我们可以省下来 所以这样对前面我们看到的 x+3x^2000 我们只要用两个分量来表示就行了，空间显然大大节省了 但这样的表示方法，运算方便么？ 我们同样可以做到运算方便地进行 其中要点就是，我们的每一项按照指数大小有序进行存储 在我们这个例子里边，我把指数大的排在前面，指数小的排在后面 把它递降地去存储 那么这样的一种做法，它怎么做加法运算呢？ 我们来看一下，就前面这个例子 我们的 P1 实际上是 3 项，P2 有 4 项 我们按照指数递降的顺序进行排列 那么我们的加法过程，可以从头开始， 比较两个多项式当前最大指数的那一项 比如说我们现在要开始做加法运算了 我们比较第一项：P1 的第一项是这个，P2 的第一项是这个 这两个一比较呢，我们就要看指数谁大？显然是 19 大 这两个对象相加的结果，和的多项式第一项一定是 x^19 那一项 所以这样的话，和的第一个结果就是 x^19 P2 的第一项作为和的结果，第一项输出了 那么，接下来我们继续比较 这个时候我们比较 P1 的第一项，跟 P2 当前的这一项 也就是说比较 x^12 和 x^8，显然是 12 次方大 所以接下来呢，x^12 这个输出 P1 的下一项，也就是这一项，是 x^8，跟 P2 的 x^8 它两个系数不一样，一个 15 一个 -4 显然这两个指数一样，就系数相加，加后的结果是等于 11 所以我们就输出和的第三项 接下来我们再比较 P1、P2 的各自后面这一项 它们分别是 x^2 这一项，跟 x^6 这一项 显然 x^6 大，所以我们继续输出 这个时候，接下来我们比较 P1 这项 x^2，跟 x^0，输出是 x^2 这项 P1 已经结束了，所以下面只剩下把 P2 剩下的内容输出就可以了 所以这就是我们两个多项式相加的结果 它的基本方法，就是从头开始比较 看哪个指数高，那个指数高的那项出来；指数相等对应系数相加 所以酱我们得到的多项式是这样的一个多项式 显然，这样的一种表示方法就是说我们用指数、系数构成的结构 存储在数组里边，来表示非零项 酱的一种方法，是一种节省空间的方法，而且它的操作效率也不算差 同样的表示非零项的酱种方法，我也不一定说一定要用数组来做 我们可以用另外一种方法，是什么呢？链表 所以这就引入了我们的第三种方法，可以用链表结构来存储非零项 同样的道理，在这个链表里面，每个结点也是包含两个最主要的信息 一个是指数，一个是系数；然后用指针域，把不同的项串起来 同样，排列的时候，我们仍然可以按照指数递降的顺序进行排 或者递升也可以，按照一定顺序，有序地进行排列就可以了 对酱的一种表示方法，对应的结构定义可以写成酱紫 我们可以定义一个结构，叫 PolyNode 里面包含了系数跟指数两个关键信息，同时也包含了一个指针域 所以我们前面的两个多项式，如果用链表存储的形式 可以表示成这样的两个链表，是按照指数递降的顺序进行排列的 同样它的加法运算，整个逻辑过程跟前面两个数组的运算是一样的 也就是说，我们分别指向多项式的头，然后比较指数大小 大的输出；相等的话，系数相加 所以这就是我们给大家看到的，同样一个问题，一元多项式酱一个问题 可以有多种的表示方法。可以用数组实现，也可以用链表实现 用数组实现还有两种方法，一个直接表示，还有一个只表示非零项