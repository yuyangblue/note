1 接下来我们讲讲程序的框架 这个程序的框架比较简单 因为我们是要判别两个二叉树是不是同构 那么一开始输入的信息 是两个二叉树有关的信息 所以首先 就要把这两个二叉组把它建起来 接下来判别这两个二叉组是不是同构 在这里我们只需要设计两个函数 一个怎么从输入数据里面 建立对应的二叉树 另外一个 怎么判别这两个二叉树是不是同构的 具体的main函数是这样的 首先 我们通过BuildTree来建第一棵树 R1 然后再调用BuildTree来建第二棵树 R2 那么这里T1 T2 就是我们前面提到的结构数组 是个全局变量 他作为参数传进去 接下来我们调用Isomorphic 这个函数来判别R1 R2 这两棵二叉树是不是同构的 那我们先讨论一下怎么建这个二叉树 也就说BulidTree这个函数该怎么写 我们知道BulidTree它的输入是什么呢 就是我们的数据 首先 他要求输入的是这个树有多少个节点 然后是每个节点的基本信息 所以我们整个程序里面 首先是scanf 把这个节点数 就是我们这个例子里看到的8 把它读进来 读入N之后 判别一下N是不是不等于0 N不等于0的时候 接下来就把这个N个节点的信息 一个个的读进来 所以我们就设置了一个for循环 从0开始一直到N-1 每轮循环干什么呢 每轮循环读一行数据 这一行数据就包括了三个信息 一个是Element的信息 一个是 left 一个是 right 那么这个 我们为了处理方便我们把这三个读进来的时候 都处理成字符的方式读进来 left跟right 我们再把字符转化为整数 再放到left跟right里面去 所以就是用了这样的一个scanf 三个%c把这三个数据读进来 这样的话就把 二叉树这N个节点信息都读好了 那这里 我们这个函数有个返回值 这个函数返回值 就是树根 树根在哪里 所以接下来我们很关键的一个问题 就是树根在哪里 这个Root在哪里 Root在哪里呢 前面我们也提到了相应的想法 什么个思路呢 可以把这个结构数组 从头到尾扫描一遍 然后看看有没有哪个节点 不存在其他节点指向他 如果没人指向他 他就是根结点了 非根结点肯定有人指向他了 所以我们的策略就是把T[i]这个数组遍历一遍 看看有没有谁指向他 我们的做法可以是这样的 用一个数组叫 check 他对应的是我们的N个节点 就是结构数组的那N个节点 check的值一开始都等于0 就是每个节点所对应的 check 一开始都等于0 然后在信息读的过程当中就Element left跟right读进来之后 同时对他的left跟right进行处理 如果我一个节点 有一个left指向了某个位置 那么就把那个位置的check 把它设为1 如果读进来这个right 指向着另外一个节点 那么我就把right指向那个位置checek设为1 所以在读这个每行信息的同时 就把对应的 他指向的 那些节点的check都设为1 一开始大家都是0 如果有人指向他 他就被设为1 所以在这里面大家可以看到了 整个循环做完了之后 没有被别人check 被改为1的节点 也说仍然保留0的节点 那个节点就是根节点了 所以这个for循环 从0到N 退出来之后 再用一个for循环 从0到N-1 判别一下哪一个节点的check值是0的 在这里 唯一的只有一个节点 check值为0的 那么他就是根节点 所以就break出来 然后 这个 这个break的这个位置 这个i就是Root的位置 把这个i赋给Root 然后return Root 所以在处理过程当中 我们读进了一个left和right 在这里我们就赋给了 cl 跟 cr 我们判别 cl cr 到底是什么 cl cr 如果是 — 就说明他没有左儿子 或者没有右儿子 所对应的left把它设为Null 就左儿子是空的 所以他对应的值那就设为Null 那就是我们这里的else 如果我们读进来的东西不是— 那就说明是一个左边儿子或者右儿子的位置 那么这个时候 把那个位置的check 把它设为1 因为我们是以字符方式读进来 所以把它减掉字符0 就得到了对应的整数 字符2减去字符0就是整数2 那么就把这个是对应的整数 就是赋给了left 所以大家这里看到的这个程序是对左边的处理 右边的处理也一样的道理 这是一个建树过程 接下来我们来看一下 已经知道两棵树了 R1跟R2 怎么判别这两棵树是不是同构的 也就说Isomorphic这个函数怎么写 这个函数 一进来 首先要对一些基本情况做个判别 如果这两棵树都是空的 也就是说他的这个R1 R2值都是-1 这个时候 两个空的树我们认为是同构的 就是return 1 反过来 一个空一个不空 那就肯定不同构 就return 0 这是第二个if 你需要处理的 还有一个 这两棵树的Element 就树根的植 本身就不一样 一个是A 一个可能是B 这个两个树根的值本身不一样 那肯定就不同构 所以这里return 0 还有一种 是就这两棵树的左子树都是空的 都是空的 那这个树同构不同构呢 那就看右边同构不同构 所以的话就变成如果左子树都是空的话 就变成判别右子树是不是同构 两个右子树是不是同构 所以就递归的调用右子树的判别 除此之外 左边不是同时空的这样的一种情况 那么看看左边是不是同时不空 左边如果同时不空 那么我们接下来看看 左边的Element是不是一样的 左边Element是一样的 那么我们这个时候通过了判别 就变成是 看看左边相等不相等 右边相等不相等 所以接下来就变成是return 左边左边的判别 右边右边的判别 反过来 如果这个Element值是不一样的 就是我要判别这两棵树是不是同构 结果发现 两个左子树的Element不一样 那么这时候有可能是左边跟右边同构 右边跟左边同构 所以就变成了是调用 最后那个return的语句 左右的判别 当然还有一种情况 左边的这个指向的是空树 右边指向的不空树 也就是说这两棵树 一个左边是空的 一个左边 是不空的 那么这个时候同样的也变得是左边跟右边的判别 左边跟右边的判别 所以这个就是我们整个判别的这个两棵树是同构的程序 那么这里程序 最主要的还是逻辑上面的一种思考 逻辑上不要遗漏某种特别的情况 逻辑比较完整清楚了之后 这个程序还是很好写的