1 接下来我们要介绍的是希尔排序 英文叫做 Shell sort 是 Donald Shell 这个人在1959年的时候提出来的 那他的一个基本思路就是 利用了插入排序的简单 同时他要克服插入排序 每次只交换相邻两个元素的这个缺点 那要理解这个Shell sort 我们先来举个例子 假设这是我们给定的一个数组 Shell sort的第一步要做的是说 我先做一个5-间隔的排序 什么叫5-间隔的排序呢 就是我考虑这个序列里面的一个子序列 这个子序列的元素是每隔5个来选取的 也就是说我先选的第1个元素 然后相隔1234 第5个 然后再1234 第5个 我用插入排序 去对这个子序列就是这3个数字 做一个插入排序 排序以后的结果呢 是放在他们相应的位置 然后继续考虑的是 下面的一个5-间隔的子序列就是 94 17和75 接着继续考虑11 95和15 然后最后是96和28 然后12和58 这就是5-间隔排序以后的效果 在5-间隔排序完成了以后 他把这个数字稍微变小一点 变成3比如说 我要做一个3-间隔的排序 在5-间隔排序的这个结果的基础上 我来考虑这是每隔3个取1个 那这是一个3-间隔排序 这是下一趟 再下一趟 这是一个3-间隔排序的结果 我们会看到这个序列的情况 有了非常明显的改善 当然最后我要保证这个序列有序的话 最后我还是得做这个1-间隔的 也就是彻底的一个插入排序 但是当我们在做 最后这个1-间隔的原始插入排序之前 我们发现这个序列已经基本有序了 也就是他大部分的逆序对 已经在前面的两趟排序里面被消掉了 所以他的主要思想 就是我先要定义一个增量序列 也就是我现在是随便定义了一个 5 3 1 你也可以定义你自己的 从某个数字开始 几间隔 几间隔 几间隔 但是无论如何这个序列他是递减的 递减到最小的那个 最后一步必须是1间隔的 定义了这个增量序列以后 我们对每一个增量 进行这么一个增量间隔的排序 那么这个 k 是从这个 M 开始 一直减到1为止 那在这里头呢 有一个很重要的性质 我们必须要观察到 就当我对这个序列 先进行了5-间隔的排序 然后又进行了3-间隔的排序 问题是3-间隔排序以后 这个序列还是5-间隔有序的吗 我们来看一下 在3-间隔排序以后我们看到28 隔5个  41 再隔5个  81 那么28 41和81他们仍然是有序的 然后再看12 58和96也是有序的 那你仔细检查一遍你会发现 3-间隔有序的序列 还保持了前面5-间隔有序的这个性质 也就是说 更小间隔的排序 就没有把上一步的结果变坏 这是一个非常重要的性质 否则的话 这个希尔排序就不好用了 我们这用文艺一点的说法 就是说一个Dk-间隔有序的序列 在执行下一步 Dk-1-间隔的排序以后 仍然还保持 原来那个 Dk-间隔是有序的 那看上去呢 希尔排序是一个非常简单的算法 原始的希尔排序也的确是非常简单的 那么他的那个增量序列怎么选的呢 他就一开始就是取一个N/2 然后每次把这个增量/2 每次减半 每次减半 最后减到1为止 所以 这个希尔排序的原始的算法 写起来是非常简单的 就是外面套一个大循环 是关于增量的 这是一个希尔的增量序列 他从N/2开始一直到1为止 每次除以2 在这个大for循环的内部 他执行的就是一个非常直接的插入排序 你还记得原始的插入排序吗 原始的插入排序 假设第0张牌已经在我们手里 我们每次是从第1张牌开始摸 在这呢 因为间隔了D个距离 所以我们假设第0张牌在我手里 我下一张牌是从第D张牌 开始摸 也就是说把原来的那个插入排序里面 注意到吗 就把所有的1全部换成D就可以了 所以这就是一个非常简单的 希尔排序的这个伪代码 但是 一个很坏的消息告诉我们最坏情况下 希尔排序的时间复杂度是θ(N2） θ是什么意思大家还记得吗 我们说O是一个上界 他有可能达不到那个上界 Ω是一个下界 而θ既是上界又是下界 也就是说他的增长速度 真真的是跟N2一样快的 那这是一个非常坏的消息 那到底什么地方出了问题呢 我们再来看一个坏的例子 假设这是我们的初始序列 如果用希尔的这个增量序列 我们会一开始怎么做呢 这一共有16个数字 那我们一开始就先做8-间格的排序 8-间格的排序我们就从1开始 然后往后数12345678 下一个是5 然后再往后数1234567 没有了 所以呢就是排1和5 我发现他本来就是有序的 所以什么都不用动对吧 然后下一个呢就是9和13 也是有序的 2和6还是有序的 10和14还是有序的 你继续往后看 就会发现我做了一个8-间格的排序 结果哪个元素都没有动 大家保持原样的走下来了 下一步我要做什么呢 我要做4-间格的 4-间隔的 1然后1234 1 3 5 7 还是有序的 你继续去检查 其他的4-间隔的这个子序列 全部都是有序的 于是这一趟又白跑了 2-间隔的排序你已经能够猜到结果了对不对 还是啥都没干 最后呢 要达到有序还得靠我们1-间隔的排序 所以呢 这其实是一个让人非常囧的情况 就是我前面白做了三趟排序 然后最后呢 还是跟原始的插入排序一样 那结果呢 还不如一开始 我就直接做原始的插入排序呢 那到底什么地方出的问题呢 我们通过仔细的分析会发现 因为他的增量元素不互质 84的倍数 42的倍数 21的倍数 那么 小的增量就有可能在后面的排序里头 根本不起作用 那为了克服这个问题呢 有更多的学者提出了更多的增量序列 比如说Hibbard增量序列 他是这么定义的 他把每一步的增量定义成2的k次方-1 这个增量序列的好处呢 是保证了相邻的元素是互质的 什么是互质 知道也就是说相邻的两个元素 他们之间没有公因子 希尔排序用Hibbard增量训练 他的情况会稍微变好一点 最坏情况下他的时间复杂度是N的3/2次方 你要奇怪 这3/2次方是怎么得到的 事实上这个证明还是比较困难的 我们就不在这讲了 另外还有一个猜想就是 到现在为止都没有人能够证明的 Hibbard增量带来的平均时间复杂度 可能是O(N的5/4次方) 另外还有一个非常知名的学者Sedgewick提出了他的增量序列 他的增量序列长成 这个很奇怪的样子啊 那中间的每一个元素呢 或者是用这个公式算出来 或者是用这个公式算出来 那关于他的表现呢 到现在都没有人能够真正的证明出来 只是猜想 猜想他的平均时间复杂度可能是O(N的7/6次方) 而他的最坏时间复杂度 可能是O(N的4/3次方) 希尔排序从实际应用的角度来讲 如果你要排序的元素 它的数量是几万这个数量级的 那么用希尔排序加上Sedgewick增量序列的话 这个效果是比较好的 希尔排序的就给我们大家一个很好的例子 你就看到一个算法他会是如此的简单 但是呢 关于他的复杂度分析是非常非常的难