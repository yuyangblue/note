我们前边说，NlogN 已经是很快的算法了 但是就解决这个问题来说，它仍然不是最快的 我们还有一个更快的算法，叫做“在线处理”算法 这个算法的伪码描述现在出现在屏幕上 你不需要去理解它为什么是正确的 你只要用很快的速度告诉我，它的时间复杂度是多少？ 很显然，这个算法描述里面只有一个 for 循环 for循环里面所有的if-else这些东西都是常数数量级的复杂度 所以很显然，这个算法的复杂度就是 O(N)，是线性的 这个必须是我们可能得到的最快的一个算法了 因为无论如何我们总得把每一个元素看一遍吧 就我读输入也需要 O(N) 的时间吧 所以，解决整个的问题居然可以用 O(N) 的速度 那已经是我们可以想象的、最快最好的算法了 当然，一个算法的效率这么高，一般来说是有副作用的 副作用就是什么呢？它的正确性不是特别地明显 也就是说，别人要理解这个程序到底是怎么工作的 略微有点困难 为了更好地理解它，我们还是来先看一个例子 我们来看一个具体的例子，这仍然是有 8 个数字 然后我们一个数字一个数字地开始处理，走一遍这个程序 最开始我们仍然是有 ThisSum，就是当前的子列和 MaxSum 是最大子列和。都初始化为 0 然后我们进入了这个很重要的 for 循环 第一件事做了什么呢？就是当前子列和把当前的这个数加进来 于是我们看到的第一个数字是 -1， -1 加进来 那 -1 并不能使当前的最大和变大，所以这块被跳过去了 重要的是这一条 如果当前的子列和是负的，那么我们就直接把它抛弃 把当前子列和重新归零，把这个 -1 直接扔掉 为什么可以这么做呢？因为我们是要求连续的子列和 所以如果当前子列和到这里，下一步我们是要顺着往后去求和的 如果现在的和是负数的话，往后面不管加什么数 它都只能让后面的数越变越小，不可能越变越大 所以呢，我还不如从头加起 因为它不可能使后面的部分和增大，所以就抛弃之 然后进入 for 循环的第二轮，我们开始读下一个数字 读进来的是 3 然后发现当前的这个 3 比 MaxSum 要大 于是当前的最大子列和记为 3 然后因为 ThisSum 是正的，所以我们进入下一个循环 再加一个，是 -2。于是当前和是 +1 +1 比 MaxSum 要小，所以这个最大和是不动的 而 ThisSum —— 当前的和也不会被抛弃 因为是 +1，总有可能使后面的和变得更大 于是我们继续进入下一轮，读下一个数字 4 果然，它是把下一个数变大了，现在当前和是 5 5 比现在的最大和要大，所以最大和要更新一下，变成了 5 为什么这个算法叫“在线处理”呢？ 我们看到整个的处理过程是什么样的呢？ 就是一个数字一个数字地读进来，而我的输入 如果说到现在为止，我突然停住，后面的输入不读了 那么现在这个程序就应该给我返回 5 作为当前的最大子列和 而这个结果是对的——对于前 4 个数字而言，这个结果是对的 这个就叫做“在线处理” 我们继续往下走。现在是 5，往下加一个 -6 于是当前的子列和变成了 -1，是负的了，我们就把它抛弃掉 重新归零，开始考虑下一个 下一个是 1，什么都没变化 再下一个是 6，加起来等于 7，它比原来的最大和要大 所以最大和更新一下 然后再加一项，进来是 -1，什么都没改变 输入结束了。我们跳出来返回的结果是 7 所以为什么它会快呢？就充分利用了这一点 就是说一旦发现当前的子列和是负的话，它其实就没有用了 直接就把它抛弃掉，我们扫描后面的就可以 “在线”的意思，就是指每输入一个数据就进行“即时”的处理 在任何一个地方终止输入，算法都能给出正确的、当前的解 接下来这张表是在某一台机器上跑出来的结果 有兴趣的话你可以真的把这四个算法全部都实现一下 然后跑一下，看看它们的时间都怎么样 这一列是 N 从 10 变到 10万 N 立方的这个算法跑到 N 等于1万的时候就已经跑不动了 NA 的意思是 Not Available，就是不算了 N 平方算到10万的时候，它也算不下去了 而我们看 NlogN，最后虽然也多花了几秒钟 但仍然是可以在几秒钟内给出结果的 而表现最好的当然是线性的算法 N 等于10万的时候，仍然是在小于 1 秒的时间内给出结果 当然我们这个结果里头，没有算输入输出的时间