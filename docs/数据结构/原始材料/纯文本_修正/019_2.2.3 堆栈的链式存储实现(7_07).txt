1 我们前面讲了怎么用数组来实现一个堆栈 同样来讲的话 也可以用链表来实现堆栈 堆栈不会是一个有序的序列 在一头做操作 所以可以想象 可以用一个单向链表来实现我们的堆栈 当然由于堆栈的特点 我们的插入 删除操作 只能在这条链的一头进行 但问题是 对一个单向链表来讲 它是有两头的 比方说这是一个单向链表 这是单向链表的头 这是单向链表的尾 我们究竟是用哪一头来作为我们的Top 我们来看一下 如果我们用左边 就是单向链表的头作为我们的Top 那么在这一头要做插入操作跟删除操作 方便不方便 显然是方便的 我们要做删除 由于头结点它知道下一个元素的位置在哪里 所以你就可以把Top指向下一个元素 我们要插入 那么你只要把插入的结点指向这个头结点 然后把Top指向刚刚插入的结点就可以了 反过来 我们如果把链的尾巴作为我们的Top 行不行 我们来看一下 插入操作没问题 我们可以用当前的这个链表的尾巴指向一个新的结点 但删除就有问题了 删了之后 它前一个结点在哪里 因为这是单向链表 找不到前面一个结点 所以 链尾是不能作为Top的 所以当用链表来表示堆栈的时候 一定要记住 它的Top一定是在链表的头上 这是我们的结构 那么我们定义了这样的结构 这个结构里面是有两个域 一个叫Data域 一个叫Next域 为了方便操作 我们可以给链式存储这种堆栈 取一个栈头结点 这样的话 在链表里面 插入 删除都处理起来比较方便 设计这个函数 CreateStack 这个函数 就相当于是建一个空的堆栈 它实际上是生成一个堆栈的头结点 这个头结点呢 是不代表任何一个元素 只是通过这个头结点 可以方便地找到堆栈里面的一些具体的元素 所以CreateStack 这个操作 就是首先以通过malloc函数申请这样的一个结点 S指向它 然后S->Next就把它设为NULL 所以我们建一个空的堆栈实际上是形成这样一个结构 将来有结点要插入的时候就是插在这个后面 如果采用这样的一个结构 我们要判别一个堆栈 是不是空的 很简单 你只要判别S->Next是不是等于NULL 等于NULL那就是后面没结点 那么它就是空的了 我们来看一下 Push操作 根据刚才讲的这样的一个结构 那么我们要往堆栈里面插入一个结点 实际上 就是往堆栈的头上插入我们的结点 可以这样子来理解 S是一个指针 它指向的是堆栈的头结点 然后这里有一个指针 指向后面的堆栈的元素 就是从这开始的结点后面才是真正的元素 这是堆栈头结点 做Push操作呢 那么是要把item把它插进去 所以我们的做法肯定是先申请一个结点malloc 申请这样的一个结点 然后这个结点 就是由Tmpcell指示到这个地方来 接下来我们做什么事情呢 就是把item这个值填到这里去 那么再下来什么事情呢 因为我们是要把它插到堆栈里面去 那就插到链表的头上 那么链表的头在哪里呢 是这个位置 所以要把这个结点插在它的前面 那么这个时候 我们要有一个指针指向它 那么同样的 S是堆栈的头结点就要指向新的结点 那么就相当于这块就断掉 所以完成这样的一个操作 那么这个操作呢 就是用后面的这两个语句 这个语句跟这个语句来进行实现 那么对应来讲 Pop操作也是差不多同样的道理 首先要判别一下 堆栈空不空 那么这个时候 有同学会问为什么我做Push操作的时候 不去判别堆栈满不满 由于我们这个是由链表来实现 如果是用数组来实现堆栈的时候 数组大小是固定的 所以它存在满不满的问题 那么我们是用链表 我们是通过不断地申请结点空间往里面插 所以这个时候 就不需要判别它是不是满了 在我们做Pop之前 要判别一下是不是空 如果不空 那我们就做一个删除的操作 删除操作跟插入的话就是倒过来 那么我们要做的事情 是这样的 这是我们的S 指到这个地方来 所以从这开始是堆栈的Top 这是堆栈的Top 这是Top下面的一个元素 那么我们要做删除操作 要做Pop操作 那就是要把这个元素把它删掉 这个元素删掉要做什么呢 那么就是把它指针指到这来就行了 但是 我们把这个结点删掉了之后 这个结点的空间必须要释放 那为了释放这个空间 我们就需要先把这个结点值赋值给一个变量 这里叫做FirstCell 所以我们先通过这个赋值语句 S->Next赋给FirstCell S->Next就是指向了这个结点 所以FirstCell就指到了这个地方来 接下来 我们做一件事情 就是FirstCell->Next 赋给S->Next 这干嘛呢 这就是完成这个指针 赋值的FirstCell->Next是这个值 这个值把它赋值给它 那就是让本来是指向它的 现在去指向它 就完成了这个指针 接下来 把这个FirstCell->Element值赋给一个变量 因为我们要return回去的 赋完了之后 然后做free操作 就把这个空间释放掉 然后再return 那么这样的话 就完成了我们的一次Pop的这样的一个操作 那么这是给大家讲的链表的一种实现方法