#### `url组成成分：
协议：//域名：端口号/虚拟路径？参数列表#锚点

**协议**
计算机网络中进行数据交换而建立的规则、标准或约定的集合
**域名**
是由一串用点分隔的名字组成的Internet上某一台计算机或计算机组的名称，用于在数据传输时对计算机的定位标识
**端口号**
包括逻辑端口和物理端口。物理端口是用于连接物理设备之间的接口，逻辑端口是逻辑意义上用于区分服务的端口
**虚拟路径**
当需要访问服务器端真实路径的文件时，需要提供一个虚拟路径供用户访问
**参数列表**
发给服务器的多个参数
**锚点**
“去顶部”到达锚点指示的网页位置

####