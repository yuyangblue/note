今天。同。学们大家好。欢迎同学们选修大数据解析与应用导论这门课程。我是赵春辉，来自浙江大学控制科学与工程学院。这节课我们将学习线性可分支持向量机的模型求解，以及如何将线性可分支持向量机拓展到线性不可分数据以及非线性数据中。

00:49

我们先来学习线性可分支持向量机是如何求解的。首先我们写出线性可分支持向量机的模型如下。大家通过观察可以发现，这个目标函数是一个二次函数，约束条件是线性不等式，所以它是一个凸的二次规划问题。凸二次规划是凸优化领域的问题。对于这种问题，我们往往是通过求解它的拉格朗日对偶问题来获得最优解。对凸优化更多求解方法感兴趣的同学可以课后查阅资料了解一下。

01:36

我们引入n 个拉格朗日算子阿拉法埃，构造出拉格朗日函数。原问题是极小极大问题，那么原问题的对偶问题便成为极大极小问题。我们首先求拉格朗日函数对欧米伽和b 的偏导数，并令其等于零便可以得到下面的这两个等式。

02:08

然后我们可以将这两个公式带入到拉格朗日函数中整理，就可以得到当前这个复杂的公式。根据拉格朗日对偶性可知，要求得拉格朗日函数对欧米伽b 的极小等同于求该函数对阿拉法的极大。于是我们终于推出了原始问题的对偶问题。

02:41

它的表达式一般我们优化习惯于求极小形式，但我们现在却是求函数对阿拉法的极大。因此我们继续整理，在原函数前添加一个负号，于是就将问题转化为求函数对阿拉法求极小，经过整理得到最终的优化目标，如右视所示。

03:12

由于我们的优化问题是强对偶的，强对偶即为主问题为凸优化问题。这可行域中至少有一点使得不等式约束严格成立。这样我们求出了对偶问题的解，就可以得到原始问题的解。根据拉格朗日对偶性，还需要满足一个充分必要条件，那就是拉格朗日函数的几个参数满足kk t 条件。

03:49

kk t 条件在我们写出的拉格朗日函数下的具体表示是这样的，从倒数第二个公式可以看出，由于阿拉法i 大于等于零，所以只会有两种情况。第一种情况，当阿拉法i 等于零时，yi 乘以括号里面欧米伽与xi 的内积加b 减一它的结果没有任何要求，只需要样本被正确归类即可。

04:26

阿拉法i 等于零也代表了该i 值对应的样例不会出现在优化目标中。也就是说，这样一个样本是一个没有存在感的样本，取多大，有没有对支持向量机模型都没有影响。第二个情况是，阿拉法i 大于零时，yi 乘以欧米伽与xi 的内集加b 减一必须只能为零，

05:00

也就是yi 乘以括号里面欧米伽与xi 的内集加b 等于一。由于yi 只能取正负一所以yi 等于欧米伽与x i 的内集加b 必然成立。我们可以得到关于b 的表达式，如这个所示。此时我们再将欧米伽的等式代入，得最终关于be 的一个表达式。

05:36

这样一来，超平面的参数欧米伽和b 就都可以用阿拉法i 来表示了。而阿拉法i 的求解通常可以采用序列最小优化算法来进行求解。具体细节这里就不展开讲了，有兴趣的同学可以课后自学一下。

06:01

下面我们来总结一下线性可分支持向量机学习算法的步骤。首先第一步就是要构造并求解有约束的最优化问题。最优解阿拉法i 通常可采用序列最小优化算法进行求解。求出了阿拉法i 之后，我们就能够利用阿拉法i 来求超平面参数欧米伽和b 有了欧米伽和b 之后，

06:37

我们自然就能够写出超平面的表达式了。最后再通过符号函数求得分类决策函数即可。以上就是求解线性可分支持向量机模型的一般步骤了。我们前面讲了线性可分支持向量机，它是建立在数据集线性可分的前提下的。

07:04

但是如果我们的数据集中有一两个像左图中这样的异常点，使得数据不是线性可分的，这该怎么办呢？此时我们应引入软件格支持向量机，允许我们的模型在这些异常点上出错。相比于硬间隔的线性可分支持向量机所要求的所有样本都要划分准确。

07:36

软件格支持向量机对样本的判别结果给予一定的宽容，那么在某些程度上便可实现对非线性样例的判别。若数据线性不可分，我们要引入一个松弛因子可惜i 使得间隔变为弹性的，其中可惜i 要大于零。于是我们之前的约束条件就可以改写为如下所示。

08:09

相应的，原来的优化目标也可以改写为下面这样的形式。引入松弛变量的线性软件格支持向量机模型可以写成如下的表达式，通过拉格朗日乘子法可以整理得到对偶优化问题步骤与前面线性可分支持向量机类似。求解模型的步骤也可以参照之前的，

08:44

有兴趣的同学可以课后自己去推导一下。前面我们讲的是针对线性数据集的支持向量机模型，但是在现实中可能数据大多是非线性的，对于这种非线性的数据，我们的支持向量机该怎样处理呢？这里我们就需要引入核函数的概念了。

09:14

熟悉机器学习的同学们可能都听过核方法。简单点来说，核方法就是采用一种非线性变换，将原空间中线性不可分的数据映射到更高维的特征空间，然后在新空间中采用线性方法完成模型的训练。像左图中左侧数据集在二维平面中是线性不可分的，

09:45

用核函数将其映射到三维空间中就变成了线性可分的。这样我们就能够利用之前的线性支持向量机找到合适的超平面对其进行划分了。在机器学习中，我们常用的核函数有下面这些，线性核函数，多项式核函数，镜像稽核函数，sigma 的核函数以及拉普拉斯核函数。

10:17

其中最为常用的就是镜像积核函数了。我们通常不会显示的定义映射函数，而是直接定义核函数k 为什么这么做呢？大家可以设想一下，我们如果先将两个样例分别映射到高维甚至无穷维的特征空间中，再去计算高维空间的内积，计算量可想而知。

10:49

为了减少计算开销，我们一般直接定义核函数。那么非线性支持向量机的模型其实也很简单。在之前我们讲过的线性支持向量机基础上，我们的目标函数都只涉及到输入实例与实例的内集。因此我们可以直接将目标函数中的x i 与x 阶的内集用核函数k x i x 阶等于fx i 与fx 阶的内集来代替。

11:32

于是我们的非线性支持向量机模型就可以写成下面这样的形式了。下面我们通过鸢尾花分类的案例来看一下支持向量机的实际效果。鸢尾花数据集中包含三类品种共150个样本，每类各50个样本。每个样本都有四项特征，花萼长度，花萼宽度，花瓣长度，花瓣宽度。

12:07

可以通过这四个特征预测鸢尾花卉属于哪一个品种。首先我们来看一下根据花萼长度，花萼宽度，花瓣长度，花瓣宽度之间的关系绘制的散点图，每种颜色的点代表一类花。从图中我们可以看出，蓝色的跟其他两种花是基本线性可分的，

12:35

而红色和绿色这两种花是线性不可分的。下面我们调用sk 棱中的支持向量分类器对三种花进行训练并分类。由于样本数量比较少，程序运行的速度还是很快的，图中我们可以看出来分类效果还是不错的，分类精度能够达到90%左右。

13:03

大家感兴趣的话可以课后自己调用sk 论库试一试。注意，其中有很多参数需要自己选择，涉及到调参时，大家可以去sk 论官网查阅手册，我这里给大家找了一些文章和书籍。对支持向量机感兴趣的同学课后可以进行自学。以上就是我们本次支持向量机的全部内容，感谢大家的聆听。同学们再见。